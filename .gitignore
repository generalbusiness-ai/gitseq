/bin/
# `go build ./cmd/x` with no -o drops the binary here rather than in /bin/.
/gitseq-github
/gitseq-mcp
/gs
/node_modules/
ui/node_modules/
ui/tsconfig.tsbuildinfo

# Local agent state and temporary work never belong in a public checkout.
/.claude/
/.tmp/
/.vscode/
