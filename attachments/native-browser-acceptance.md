# I5 native browser acceptance — 6 September 2026

Source: 50cf111286e723b73ca615f3f1b80d8788811940 on request/landing-obligation-i5, integrating main80d4fbf79fb41f4eb675000a5d09a4df4ec9ba74. Small-room checks ran the integrated d9667e945e2fb6a9c06940a26020207b5cc39f88 binary; the final large-room repeat ran the corrected 50cf build. The only intervening UI behavior change was the absent-target label described below. Final documentation-only changes do not alter the exercised UI.

Native Chrome was operated through accessibility and screenshots. No browser automation substitute or terminal UI was used. Fixtures were isolated local repositories; the large copy contained no accessible real signing keys. Synthetic fixture actors and approvals are test data, not independent I5 review.

## Small-room controls

- Explicit request-result choice was required. A missing refs/heads/missing-i5 target refused without appending (depth remained 3) and retained the form.
- A held request required a selected roster hold owner. Prose suggesting a hold showed the structured-hold warning before that choice. Keyboard Tab/Down/Return selected the owner and submitted the form.
- After selecting refs/heads/main, the fixture branch advanced from 56302976f1b45d6bf38b249571abfb7d091580da to 70a9aad8a9faf88bfb49900f9e4988efcc4019ee before submission. Signed request #4 (fe72ed7d1a262e7c278a2c34062960c523cddfdf) recorded the new filing-time head, canonical repository and full branch, with the exact fixture hold owner.
- Table and Graph agreed on the one open request, main destination and responsible actor. Keyboard activation opened the selected card's thread.
- Temporarily removing the fixture target branch changed the active inspection to target no longer exists. Restoring it restored observation, without a durable lifecycle mutation.
- A synthetic exact approval at #11 remained approved but unlanded and awaited the hold owner's release. A synthetic release changed the next action to merge into the target. Sealed fixture merge 2f6751dbe6997ad6be595c55cd52a07cae43899d then showed a durable sealed landing and the independent current Git containment observation. The artifact audit cleared.
- After advancing the fixture ref, temporarily withholding its parent merge object made the actual inspection return git.state=unknown and null ancestry fields. The native thread said current target ancestry is unknown while retaining the sealed landing. The object was restored afterward.
- A separate native request explicitly chose no Git artifact. Signed request 68aa5c1734248dc1c2c58c04a32b9461de333856 contained no target_ref/repo/head/landing/hold_owner, and the UI described requester-accepted reporting.

## Large-room controls

The read-only clone contained 19,507 verified records and 2,436 commitments. Table and Graph agreed on open 2, artifact audit 78, completed 1,493 and closed 941. Historical satisfied and cancelled labels were preserved; a historical cancelled request selected from Graph opened its actual request and promise, and returning retained the audit population of 78. The large Graph showed 157 visible cards with the existing bounded 160-relation warning; historical missing-basis/cycle warnings remained visible.

The initial cold read exceeded a 10-second HTTP probe. Native startup showed Verifying durable history with progress (16,160 of 19,508 records) until the verified projection was ready. Warm status took 0.169 seconds. No unverified board state was presented as authoritative.

Native acceptance exposed a misleading fallback: old request bodies may mention a destination while the fold currently projects none. The corrected spine displays No target recorded, rather than asserting an explicit no-artifact choice. Final native Table and Graph both showed this corrected label for I5 #18625. A focused regression test covers absent and legacy projected targets.

## Other validation and limits

All 207 UI tests and the embedded production build pass. Integrated resident request tests pass under the Go race detector. DOM/model tests cover inherited targets, stable retry identity after a lost response, altered target intent, explicit/unheld choices and the selected receipt's carried-artifact audit. Native checks do not claim an inherited-result submission or a separate committed unheld-target request; those use the actual HTTP and DOM suites. The synthetic review does not satisfy the independent exact-head implementation review requirement.

Native input automation needed correction twice (an address lost its first character; one paste supplied unrelated text); each was corrected before filing. One stale accessibility index opened a read-only verdict and was corrected without mutation. These are recorded as harness limits, not product defects.

Both fixture residents were stopped, the merged candidate worktree and branch removed, the temporarily withheld object restored and the owned browser tab closed. Evidence and disposable repositories remain under this directory; private fixture key files are not evidence attachments. No live workroom readers were replaced.
