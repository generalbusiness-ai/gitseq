Review findings at fc77b226c9216da40b058d9751f80bc010ed0436

Request 83769b81; review promise ad6e270d; implementation request 9282c86a and live commitment c800b95e. All three current artifacts were read. The original eleven independent controls pass (0.306s), and all four unchanged spike packages pass (Forge 0.453s). The candidate checkout remains clean. Tests are appended through an owned overlay.

The common numeric validator is still digit-only. Seven additional malformed inputs return no findings: short host 65536; short container 65536; short descending container range 3001-3000; an oversized all-digit host number; two short ranges with unequal widths (6000-6001:3000-3002); long target 65536; long published descending range 6001-6000. All use the otherwise allowed host 127.0.0.1. These are executed expected-refusal assertion failures, not compile errors (0.303s). At-bound short 65535:65535 and host range 6000-6005 with single target 3000 both pass.

Docker go-connections at immutable 9c9e2db764937383effc099843914897c8838056, nat/parse.go:35-57, validates numeric ports within 0..65535 and rejects descending ranges. nat/nat.go:208-214 rejects unequal host/container range widths when the container is a range, while allowing a host range for one container port. Sources: https://github.com/docker/go-connections/blob/9c9e2db764937383effc099843914897c8838056/nat/parse.go#L35 and https://github.com/docker/go-connections/blob/9c9e2db764937383effc099843914897c8838056/nat/nat.go#L208 . The source manifest pins downloaded bytes. This is a source reference; Docker/Compose was not executed and no deployed exposure is claimed.

Replace the digit loop with bounded decimal endpoint parsing plus ordered ranges; use the parsed endpoints for the short-form width condition without expanding ranges or allocating per port. Preserve zero where the existing supported grammar permits it, the optional ephemeral host slot, upper-bound success and host-range/single-target success. Apply common endpoint validation to both forms. This is the original malformed-input refusal condition, not a new general Compose-resolution task. The prior bracket, duplicate-key and empty-container fixes and simplification are correct and should stay.

I should have included numeric boundaries in the earlier review batch. The current findings are recorded together so the correction has concrete complete controls. Keep it on c800b95e, cite the verdict in fresh exact-head artifacts and request independent review again. No full-module run was performed after the blocking failures were reproduced.
