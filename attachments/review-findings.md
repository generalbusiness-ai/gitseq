Review of d0f70a18aeacc7fe43b2cadb8e814abca72b008e

R1: docs/concepts/record.md newly says an unavailable application/fold version does not open at all. OpenObserved at app.go:875-880 stores a selection and returns a workspace when selectHost's outer error is nil. host.go:79-87 stores unavailable-interpreter errors inside selection, rather than returning them as open errors. Existing TestBoundRepositoryIsVerifiableWithoutItsInterpreter and TestBoundRepositoryRefusesAnotherFoldVersion explicitly require opening to succeed. Verification stands; Snapshot and application Act refuse. The unreadable-log case prevents opening. Architecture.md:518-529 distinguishes these facts. Correct the sentence to preserve verifiable-but-uninterpretable behavior.

R2: the unchanged front-matter internal/workroom basis49d2d3d8 is retired, not merely stale; its tree differs from this exact head. Reconcile it instead of preserving a dead behavioral wire. The paged current artifact inventory finds live e0ffb053 (internal/workroom), whose admission_profile.go file blob matches current; live3f96b40f (internal/workroom/fold.go) matches current fold.go. Preserve their ordinary staleness. The new app.go cbe62bb5 and admission.go8a60f768 file blobs both match current; keep them, including admission.go's ordinary staleness. There is no exact host.go artifact, but live covering internal/app7d6f6997 contains identical host.go, so the behavior has an existing owner. No unrelated pointer retirement is necessary. Companion JSON records artifact and file comparisons.

Simplification: use links to the maintained architecture host-binding/submission detail instead of bare code path/line tokens, and cut the conversational lead-in about being plain. Keep the compact distinction between an unwired governance selector, installed admission, and callers of the fold. No runtime or governance implementation is requested.

Confirmed: repository-wide Go reference search finds no production caller of SelectAdmissionProfile or ResolveAdmissionProfile outside their library delegation; Folder.AdmissionProfile belongs to that same library. Existing host-binding/admission controls PASS1.866s; profile-library controls PASS0.253s. The original historical counterexample was read and re-executed against this head through an owned temporary overlay/disposable test workroom: PASS1.230s. A ratified unavailable profile was selected by the library while a subsequent assertion appended effectively under the unchanged interpreter. No actual resident or source checkout was changed. diff --check is clean. The author's documentation package run was reported with a live-frontier retry; I did not independently rerun that whole package after finding the semantic blocker. A passing documentation gate cannot itself establish that current-source provenance is truthful.

Corrections remain on e026ffb1 under current requeste57b90ec. Publish a new exact-head artifact citing these findings and the actual behavior owners, then request independent review. No source, API, dependency, authority or runtime-policy change is needed.
