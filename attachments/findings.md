# Review findings at361316620302bdd4580e68256189d2529c6719d6

Request c3f7ea48, review promise4a02e150, implementing request9282c86a and primary c63e5e75. No source change or container execution was made. The supplied additional Go tests ran through an owned overlay of the exact compose_test.go, appending the same test function; the candidate checkout remained clean.

R1: publishedPorts uses yaml.Unmarshal once. With a safe first document and a second document containing another service and0.0.0.0:9000:9000, unsafeBindings returns zero findings. This violates the assigned requirement that a safe entry cannot hide an unsafe binding. The Compose-go loader at immutable cda18529aca7f43fdcdcfba7aeee9604d4352ef1 explicitly decodes every document through EOF and processes each: https://github.com/compose-spec/compose-go/blob/cda18529aca7f43fdcdcfba7aeee9604d4352ef1/loader/loader.go#L553 . Its decoded documents merge at line520. Earlier ephemeral source line517 referred to the moving main response; the immutable source's decoder loop is553-570. Reproduce from the supplied second-document fixture. The smallest correction can reject additional documents explicitly rather than implement full Compose merging; also reject malformed trailing YAML instead of ignoring it. Require the actual gate path to fail this addition.

R2: the selected short-form grammar accepts malformed/unsupported input as safe whenever its first field is127.0.0.1. Both127.0.0.1:3300:not-a-port and127.0.0.1:3300:3000/not-a-protocol return zero findings. The suffix is discarded without checking it and the ports are never checked. This contradicts the request's explicit malformed/unsupported refusal and the guide/artifact claim. Validate the small supported grammar (including supported range forms) or report unsupported shapes; do not turn the gate into a general Compose implementation. The same review should check mapping field value types so known keys alone are not mistaken for a readable entry. These malformed fixtures are validation gaps, not claims of a running exposed port.

R3: Docker's current service reference explicitly lists unbracketed::1:6000:6000 alongside [::1]:6001:6001. The candidate rejects the former as an unrecognized short form, while the independently specified IPv4 and bracketed IPv6 range controls pass. Since the requested allowed host set includes::1 and the implementation claims both documented forms, support this documented spelling or explicitly justify and document a narrower supported subset. Source: https://docs.docker.com/reference/compose-file/services/#ports . No expansion to other loopback addresses is requested.

Own unchanged-candidate forge suite passes0.247s. Six additional controls run: IPv4 range and bracketed IPv6 range pass; unsafe second document, malformed target, unsupported protocol and documented unbracketed IPv6 all fail their expected assertions. Expanded control run exits1 in0.496s. This demonstrates failed assertions rather than a compile failure. Current shipped compose.yaml remains unchanged and loopback-only. No daemon, image, container, network exposure or Compose runtime test was performed.
