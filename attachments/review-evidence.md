Independent re-review of bb65d89803533232f154aa7b53082838fa93d285.
The prior two actual CLI failures are fixed: same-key different target_ref refuses, and accepted retry after branch deletion returns the original event without append. CLI choice/hold/inheritance/refusal/moved-ref cases passed in 16.754s under race.
Two additional actual CLI controls FAIL in 4.010s under race: (1) accepted signed state@2 request retried via the real CLI refuses for missing result choice; the fresh-key/no-choice refusal control passes. Existing TestCLIRequestReplaysAnExistingLegacyAct exercises kernel.Submit twice, so it does not cover this route. (2) reassign-if-unclaimed without a result choice retires the original open request, then refuses the replacement; original commitment becomes withdrawn with no successor. Both reassign reference pages claim refusal before either append, contrary to this result. Tests and complete logs attached.
Source review also finds SchemaReassignRequestV1 changes accepted schema interpretation while ProfileVersion stays workroom-fold@21; the latter gates app projection caches. No cache-upgrade execution was performed by this reviewer: require a current-profile transition witness rather than claiming the old generic cache test proves this change.
The suspected empty hold-owner fold change is NOT a reproduced defect: both main and candidate reject empty values in schema validation before the fold. The exploratory test failed during fixture construction on both versions; no historical accepted empty-owner record was established.
Documentation examples in gs state, gs batch and MCP state mark Add a changelog / CHANGELOG.md exists as no_git_artifact=true. Use an existing target branch for source work or make the example genuinely report-only. This contradicts the truthful-choice producer condition.
No full 51-path approval or independent full-suite pass is claimed. The negative review covers the shared app/intent/kernel/fold logic, CLI/reassignment code and tests, MCP adapter changes, and affected contract examples/architecture.
