# Notes and exact-content browser acceptance — 2026-09-06

Candidate: request/notes-file-preview, final head cc24d6c43ddbed19997cb4797b0d2002c77b9a93. Base 183211c3. Native Google Chrome through CUA, macOS, normal window at125% browser zoom. Production candidate React UI and internal/service handler, using a disposable verified read-only shared clone of the real Gitseq source and signed sequence at depth19085. Source objects and event attachments were read through the candidate API, without simulated preview responses.

The test-only harness at .tmp/preview-browser/main.go served127.0.0.1:7781. It allowed GET, POST/v0/preview and POST/v0/wait, rejected other POSTs, and supplied one explicitly synthetic non-custodial "read-only reviewer" choice to dismiss the existing join gate. No private keys were copied, no signing/presence credential was issued, no original workroom ref or live resident was changed. This is reading/navigation acceptance, not an authority or live-write test. Cold verification was visibly reported and completed before data appeared.

Observed journeys:
- Opened Notes:1453 note-render statements, newest200 with an honest remaining count. Rows showed number, author, date and stale/retired state. #19016 was among the initial rows.
- Searched #19016:2 notes matched its mention, plus a distinct exact-record result for the actual note. Return opened #19016; request populations were separate.
- Expanded #19016 (21b2806ed4f252e16193552b46606a10ae90eaa7): both actual attachments were listed. Opened gitseq-one-delivery-account-review-20260906.md in the in-app dialog. Read the full note, including recommendations and limits, with owner/repository/exact event revision shown. Screenshot in the session demonstrates readable content and close control.
- Followed the Markdown's reference to gitseq-artifact-prose-repetition-evidence-20260906.json. It stayed in the same preview owner and rendered the actual JSON with line numbers. This check first found and then verified the correction of a source-path/sibling-attachment mix-up. Screenshot shows the original candidate21b7e742 and artifact character-count evidence.
- Reload restored the JSON preview. Tab stayed on the Close control because it was the sole control. Escape closed to #19016. Browser Back reopened the exact JSON; Forward restored the note. These later checks used the corrected close semantics.
- Entered a percent-encoded canonical link for artifact#19011/f9e94d6c94f9be736d2e8da293c2fc8cee510d02. It opened the correct artifact rather than the unknown-record fallback. Its details exposed real linked source paths.
- Clicked artifact path internal/app/request_authoring.go. The dialog showed its exact cited revision21b7e74225885452ab0ad0dc49b33aaf2c326d54 and actual Go source, despite the clone's main being183211c3. A screenshot shows source lines1-23.
- Opened that source preview link with line=42. It scrolled to and highlighted line42 at the center, with the same owning record and revision. Screenshot captured the highlighted line and surrounding source.

Automated coverage complements the browser observation: whole-App encoded routes, malformed focus preserving a valid root, Notes/history, shared-preview dismissal, focus containment and delayed prior responses; inert Markdown and unsafe links; declared note kinds and all-event lookup; SHA1/SHA256 hash-verified source reads, replacement refs ignored, literal-path bounds, symlink/submodule refusals, bounded directory listings, byte/line limits, empty/binary/missing content, evidence ownership, direct-head ambiguity/explicit choice, unknown/foreign records, no source fallback, closed bounded JSON and origin checks. Exact command results are separate attached logs.

The fixture's fresh-process audit and synthetic identity choice are test setup, not claimed improvements to startup or joining. No public deployment, screen-reader audit or instrumented contrast/performance measurement is claimed. Screenshots were observed in the session; this note does not claim separate screenshot files.

Final-head check: rebuilt the same test harness from cc24d6c43, restarted only the disposable preview server, and reloaded the source/line42 URL in native Chrome. The exact source and highlighted line rendered again. Close returned to the owning #19011 artifact. The last candidate corrections (delayed-response isolation, duplicate path metadata removal, and unsafe route segment escaping) were included in this build.
