101:    `go test ./...` reported rc=1 for one package, cmd/gs, at 601 seconds, with
102:    two failing tests: TestFilingDisclosesItsBasesBeforeSigning and
103:    TestBatchProcessReadsItsFileAndReportsFailures. The six-package gate above,
104:    which includes ./cmd/gs/..., passed at the same head, and so did ./cmd/gs/
105:    alone.
106: 
107:    Both failures have the same cause, and it is not this change.
109:    which after one second writes "gs: verifying the durable log; this may take
110:    a while" to os.Stderr. TestFilingDisclosesItsBasesBeforeSigning captures the
111:    subprocess's stderr and asserts it is empty in two subtests;
112:    TestBatchProcessReadsItsFileAndReportsFailures merges stderr into stdout
113:    with exec.CombinedOutput and parses the result as JSON. When the machine is
114:    loaded enough for a snapshot to pass one second, both break. cmd/gs/main.go
115:    is byte for byte identical to the base commit in this head
116:    (`git diff d1ae6ade -- cmd/gs/main.go` is empty), and nothing in this change
119:    Reproduced directly. Running
120:    `go test -count=1 -run TestFilingDisclosesItsBasesBeforeSigning ./cmd/gs/`
121:    repeatedly while a whole-module run occupied the machine produced
122:    "--- FAIL: TestFilingDisclosesItsBasesBeforeSigning/the_genesis_basis_is_quiet"
123:    once in three attempts, at 33.03 seconds, against 20.2 and 20.9 seconds for
124:    the two passes; six further runs after the load dropped all passed, as did
125:    an unloaded run at 9.8 seconds. That subtest's only assertions are that the
126:    command succeeded and that its stderr is empty.
