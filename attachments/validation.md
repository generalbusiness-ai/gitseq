# Publication safety: final validation

Exact candidate: 133cd39c5e955f2fc8edeb7b4b28544848cc5dff, PR26. No live resident or authority activation is part of this source delivery.

At the corrected exact head, GitHub CI34027293040 passed all35 Go packages with `go test -race -count=1 -json ./...`, vet, generated adversarial evidence, both Go binaries, UI tests/embed comparison, tracked-output reproducibility and a main-only clone build. Security34027293027 passed. The attached extracted CI log reports35 package passes and zero failed test records; the complete local copy is /tmp/safe-sequence-final-ci.log. Run: https://github.com/generalbusiness-ai/gitseq/actions/runs/34027293040 .

After the selected-ref/first-import correction, local targeted race controls and vet passed. The additional local full race at133cd39c5 is still running when this artifact is filed; its eventual result will be reported separately, not silently inferred. Before that correction, the full local race, final documentation gate, vet and CI passed at23961eb1e. Those earlier results are clearly separate from final-head CI.

The checked real-Git pre-receive hook clears quarantine object-store variables, fails if the injected CAS fails, records the observed concurrent head, and verifies the receiver got the advertised head. The legacy mapping is an explicit loss control; the supported mapping preserves the local append. Other controls cover both historical mappings (separately and together), custom source rules/configuration, repeated attach, valid signed sibling, unsigned history, saved-frontier rollback, lost canonical ref and concurrent import. Exact selected-genesis fetch refuses remote deletion instead of reusing a retained tracking value.

Real metadata-directory permission failures after import CAS preserve the exact prior configuration and never rewind a newer append. Retry after restoring writes persists the installed head; reopening/auditing a later local head is safe, while retry with an older remote candidate refuses. A losing first-import CAS retains the read-only genesis/format, no verified frontier or signing custody, and the winning ref; retry succeeds from that actual head. Existing concurrent-creation coverage preserves the stored identity rather than deleting another creator's configuration. POSIX permission controls skip Windows/root, and receiving-hook controls skip Windows; both ran here on an ordinary macOS account, and CI ran the Linux controls.

## Review findings addressed

Planner's draft findings in conversation eph:sha256:d1517a1320e9f700226d3af12238c87aed4e966f56e4e51f86e6d210cbaff8e0:3 and :4 led to exact selected-ref fetch and explicit first-import partial-state coverage. Planner confirmed those corrections fit the existing request in :6. The checkpoint partial-write contract was clarified in :1: ordinary refusals precede CAS; a later checkpoint I/O failure is reported with prior memory retained, never compensated by a ref rewind. No cross-store crash-recovery protocol was commissioned.

## Failures retained

The initial hook fixture lacked the hooks directory omitted by the test Git template, so it failed visibly. Adding that directory produced the meaningful pre-fix RED: the supported old mapping lost the concurrent append. The first import integration run passed the worktree path to gitstore.Store; initial attach correctly refused it as not a Git directory. Resolving the common directory fixed it. Logs are attached. No guard was disabled and no retry-until-green procedure was used.

Initial documentation companion filings at23961eb1e were refused because inherited frontmatter named10 retired bases. No override was used. The corrected docs replace them with current implementation bases, including the actual host successor of deleted internal/nexus and an explicit observation artifact for the unchanged current GitHub connector where no live exact successor existed. All25 final bases were inspected: live/effective, none describing a superseded world; nine have ordinary staleness retained for explicit reviewer consideration. Attached inventory records each exact path/head and status. The four docs' signed companion artifacts carry those same semantic dependencies.

Architecture: CLI transport/import ordering changes, kernel VerifyAt reuses the existing scanner, and the application boundary shares the verified-frontier check and existing config lock/CAS. The architecture page states both partial-state boundaries. Kernel signatures/history, apphost custody and Workroom authority remain unchanged. Security controls cover untrusted history, identity, stale local observations, CAS contention and I/O failure. Simplification retains one frontier rule, one scanner and existing storage primitives; no witness reset, new queue, or recovery subsystem.
