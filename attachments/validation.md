# Npm advisory repair validation

Exact candidate:171994bf5eb156a7c7e2f70a29c34b9680e8d8bc, based on main869702241e4d28c9b8599e8caccaf3d01a2ca4d9. Branch request/ui-npm-advisory is pushed. Governing request69e31619, promise69a8602c. Only two paths change: ui/package-lock.json and .github/workflows/security.yml.

The lock changes only nanoid's version, resolved URL and integrity, from3.3.17 to3.3.18. All150 dependency entries and all unrelated package fields remain unchanged; PostCSS's ^3.3.17 range admits the patch. The npm resolver also expanded six existing bundled wasm-package metadata entries; those unrelated additions were excluded. npm ci accepted the resulting minimal lock, and current registry URL/integrity agree. package.json is unchanged.

Node24.21.0 with npm11.19.0 was used for install, UI tests/build and before/after audits. The new Security job uses the repository's existing pinned checkout and setup-node actions, Node24, inherited contents:read permissions, checkout persist-credentials:false and explicit10-minute job timeout. Its only project command is npm audit --package-lock-only --include=dev --audit-level=high in ui. No project dependencies are installed or executed by that job, and no branch-protection setting or main CI step changes. All existing Security triggers apply. npm's audit-level flag changes the exit threshold, not output filtering: https://docs.npmjs.com/cli/v11/commands/npm-audit/ .

Before, the full lock-only audit exits1 with exactly one high advisory, GHSA-2v37-7h3g-55p8 / CVE-2026-67213 for nanoid. Production-only exits0. After, full/dev-inclusive and production-only audits both exit0 with zero reported advisories. The before and after metadata both report prod9/dev142/optional70/total150; these totals describe the graph, not distinct vulnerabilities or an install count. Current ui/src contains no nanoid/custom-generator call. Inspected PostCSS uses nanoid/non-secure with fixed size6. This is maintenance and scanner coverage; no deployed exploit is claimed. Primary advisory: https://github.com/advisories/GHSA-2v37-7h3g-55p8 .

An ephemeral fixture extracts the actual workflow run command and invokes it under bash -e -o pipefail, matching GitHub's shell failure semantics. A stub npm validates the exact arguments, emits a diagnostic and returns1 for a high finding,42 for a scanner failure, and0 for a moderate finding. All statuses and diagnostics survive. Adding || true is detected because the expected high failure becomes0. Separately, the real pre-change audit with an unreachable registry and retries disabled exits1 with visible failure diagnostics. This fixture proves command status propagation; it is not a permanent test of a live advisory database, nor a substitute for the real before/after audit.

Validation at the candidate bytes, unchanged by the commit:
- npm ci:pass; npm test:238/238 pass, no skips.
- make ui-check:pass, including TypeScript and Vite; committed embed is byte-identical, with no changed or untracked bundle files.
- ciguard tests, gofmt over tracked Go files, make vet, make build, git diff --check and clean tracked/untracked status:pass.
- All-module Go1.26.7 race attempt:35 packages pass; internal/docset fails solely when the live citation gate rejects a verified-frontier rollback from concurrently verified22851 to captured22850. The full command exits1 and that failed log is retained. An isolated retry of the complete internal/docset package under -race -count=1 passes in153.456s. Thus every package has a passing race result across these runs; this is not described as a single uninterrupted all-module pass. No test was skipped or disabled and no code was changed to get the retry through.
- Exact-candidate main-only clean-clone verification:pass (vet/build and clean clone); its owned fixture did not exist, so the script's pre-existing rm-based cleanup branch was not entered.
- Existing tracked-content secret scan:pass. No secret-scanner code changed.

The original shared-workroom frontier refusal is a verification concurrency limitation, not hidden by filtering output. UI/audit/gate logs and graph/fixture evidence are hashed below. Independent exact-head review, approval ratification, sealed landing, main/sequence push, actual CI and Security/npm results, and owned worktree/branch cleanup remain required before delivery is complete.
