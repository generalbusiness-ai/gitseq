Sources re-derived by claude on 2026-09-07 for request #21223 (notes/2026-08-07-deployment.md at head 6192b18d).
Fetched independently of the drafting pass; quoted values checked against the note's new posture passage.

1. https://docs.github.com/en/rest/repos/rules?apiVersion=2022-11-28  ("Create a repository ruleset")
   target: "Can be one of: branch, tag, push"
   bypass_actors[].actor_type: "Can be one of: Integration, OrganizationAdmin, RepositoryRole, Team, DeployKey, User"
   actor_id: "If actor_type is DeployKey, this should be null."
   bypass_mode: "Can be one of: always, pull_request, exempt"; default "always";
     "pull_request is not applicable for the DeployKey actor type. Also, pull_request is only applicable to branch rulesets."
     "When bypass_mode is exempt, rules will not be run for that actor and a bypass audit entry will not be created."

2. https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/creating-rulesets-for-a-repository
   targets described: branches, tags, pushes; no other target.
   bypass eligibility list: "Repository admins, organization owners, and enterprise owners";
     "The maintain or write role, or custom repository roles based on the write role";
     "Teams, excluding secret teams"; "GitHub Apps"; "Dependabot".
   deploy keys: not mentioned anywhere on the page.
   bypass modes described: "Always allow" (default) and "For pull requests only".

Neither page documents any ruleset target for an arbitrary ref namespace such as refs/seq/* or refs/seq-mirror/*.
Neither page says whether GITHUB_TOKEN contents:write can push a custom ref namespace.
Nothing here was configured or run; no GitHub setting, ruleset, key, token or workflow was created.
