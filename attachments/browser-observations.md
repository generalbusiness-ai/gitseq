Native Chrome observations, 2026-09-06. Source served by the four workroom residents: Gitseq183211c3b5bff170171c5bfbe9bfbc3aacde08f6. Window1412x768, existing browser zoom125%. Read-only navigation in our own tab; no actor switch or signed UI action. Screenshots retained in tool conversation.

Gitseq (~19,024 events): open board responded, showed5 then4 requests as new Notes task was promised. Table made performer visible. Graph before new claim: headline4open with bounded direct context, many extra cards,5-thread cycle warning; tiny cards occupy left part of large canvas with empty right area and lower content outside viewport. Selecting #18499 highlights its outline, dims unrelated cards and lights connecting edges, but does not enlarge labels or centre them at readable scale. Thus selection emphasis already exists; the existing graph plan's readable zoom/centre and causal-lineage simplification remain applicable.

Gitseq search #19016 in request view: zero matching open requests and no direct event result. Encoded canonical URL with %23 rendered explicit “No record at this address” then request table; typing raw canonical # opened #19016. Expanding note showed full prose, actor/time/IDs, stale flags and relations. No attachment controls were present. Screenshot shows stale_bases as a long raw full-ID list filling most of the viewport below the prose. The note title is essentially the entire opening paragraph and truncates across the top. In-app file/evidence reading and Notes discovery are now separately assigned #19022.

Tailapp(~3890events): changing room requires URL/tab; top bar names repository and links remote. Open1 and row#3890 waiting on builder agreed. Search#3821 yielded open0 and completed3. Choosing completed rendered exactly3 rows: root#3821 and its two review requests#3832/#3848, which mention it. This is substring/related request search, not exact-event lookup. Completed rows say “satisfied / waits on unassigned”, a misleading next-action phrase for completed work. Clicking root opens request→promise→report→merge. Merge clearly says62c0d8f4landed on main, asked of Git at render time. No review station appears in that initial four-station spine, even though the exact approval exists. I5 has separate reviewed/landed work in flight; reconcile there rather than duplicate its source.

Tailapp return and keyboard checks: the in-app Requests button preserved #3821 and completed3. At200%zoom (~706CSSpx) tabs wrap and table remains readable but titles truncate. At400%(~353CSSpx), the viewport has horizontal overflow and fixed STATE/WAITS ON/AGE columns push TITLE and event number off-screen. Starting from Search,14Tabs reached first row with a strong yellow focus outline, scrolling it into view vertically; its title remained off the right side. Enter opened #3821. This is zoom/reflow evidence, not a mobile-device test. Restored original125%zoom. The satisfied root row is red (its retired/world-stale evidence), with no visible row-level explanation; distinguish completed work from current action and explain a warning rather than making “satisfied” look like failure.

Inventory atfrontier502: initial explicit Join chooser required an actor; selected existing Codex (no Builder offered), resulting only in advisory presence. Board showed open0,completed22,closed8 and Nothing is waiting, while actor menu said16for you. Menu contained historical requests including#426/#423. Opening#426 showed its completed request/promise/report/main9c5d9dffmerge; unread count dropped16→15. The number is reading attention/history rather than outstanding work; rename accordingly and show completion status rather than implying16tasks owed. No durable act was filed from the browser.

Chess atfrontier834: open0 and Nothing is waiting while stale,notinflight counted2. That tab renders browser acceptance#720 and public follow-through#506, both waits on Codex. These are obligations still owed, now stale after source delivery, not work that disappeared. The current UI follows lifecycle-stale classification, so proposed correction is accurate tab/empty-state copy and visible stale queue, not an invented client lifecycle. #720's current-basis refile has been requested through the native workroom.

All room changes used a known local URL in the same review tab; the app offers repository/remote identity but no visible room selector. Could add a small explicit known-room switch later, but discovery/authority rules are not assumed and it is outside the first slice. Loaded board/filter/thread interactions were responsive to native actions even in Gitseq's~19K history; no instrumented performance budget, disconnected-network scenario or contrast-ratio measurement was run. Initial brief Loading work was seen on a full navigation; no lasting app hang reproduced. Chrome provider startup delay is tooling and is not charged as app latency.

Chess#720 interaction: Talk expanded3notes; request details showed governing source artifacts. Clicking #685(cmd/chess/web.go) opened that artifact. Native browser Back returned#720, but request details and Talk collapsed. Forward returned#685. The no-report station still said owed by Codex despite the board's empty-active message. These are navigation/read observations only.
