The usability is poor. There should be no explanatory tooltip-type text in windows; necessary explanation belongs in a tooltip, and the UI should be understandable without it. The “Active covers every actor…” paragraph is the example.