Same-frontier evidence for request #20622, head 9be714c075b33deac4f541ba3c115c32c3d3c3cb

A. Real workroom, frontier de694a1d869afcb4760357aed879419555fdadc2 depth 20825
   (three surfaces, one frontier, run with the built ./bin/gs)
   - bounded CLI, verified local fold   cli-bounded-localfold.txt
   - complete CLI (--all, resident)     cli-complete-resident.txt
   - complete JSON (--json, resident)   cli-json-work.txt
   all read: Work open 5 (lifecycle open 1, promised 1, awaiting-review 3),
   completed 1535, closed-not-completed 958, stale 13, of 2511 commitments;
   reasoning moved 3, artifact landing audit 78.
   The bounded CLI against the OLD deployed resident (cli-bounded-resident.txt)
   prints the explicit "not reported by this resident" line instead of zeros;
   http-status-summary-old.txt shows that resident's totals carry no `work`.

B. Frozen projection 71b03a6ac8dfb434149763ff1ea99ad276366363 depth 20820
   (curl /v0/status -> status-full.json, then both languages over that file)
   - Go owner, all four Go surfaces      go-owner-frozen.txt
   - browser row selector (rows.ts)      browser-selector-frozen.txt
   both read: open 5 (open 1, promised 1, awaiting-review 3), completed 1534,
   closed-not-completed 958, stale 14, moved 3, audit 78, commitments 2511.

C. Scratch workroom served by the NEW binary, head dc04cb4a depth 6
   - GET /v0/status-summary durable.totals.work   http-status-summary-new.txt
   - GET /v0/status work, gs status, gs status --all  scratch-resident-surfaces.txt
   - MCP status totals.work                       mcp-status-work.txt
   - MCP wait totals.work                         mcp-wait-work.txt
   all read: open 2 (lifecycle open 1, promised 1), of 2 commitments.

Gates: go-test-all-at-head-2.txt (go test -count=1 ./..., exit 0),
       ui-tests.txt (node --test test/*.test.mjs, 222 pass, 0 fail).
