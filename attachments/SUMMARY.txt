Evidence for request #20622 at exact head 03f7f721cc6e1aead5d6d164f9dd07c6395427ab
(branch request/shared-work-population-counts, one commit over main ad4656dd8).

Everything in this directory was produced at 03f7f721 on 2026-09-07 from 12:27Z.
Nothing is carried over from 1fdf9e6b or 9be714c0; ../evidence-1fdf9e6b/ and
../evidence/ are those earlier heads' traces and are left untouched. The only
file copied forward is B2-browser-selector.mjs, which is the measuring script
rather than a result, and it is re-run here against this worktree. Each file
opens with the verbatim command, its UTC timestamp, and the frontier that
observation actually saw.

The change from 1fdf9e6b is the review's Simplification finding: WorkOf no
longer builds a second `open` map beside OpenLifecycles, it asks OpenLifecycles
itself whether a status is open, and some comment text was trimmed. The counts
are therefore expected to be the arithmetic they were; what moved between the
two runs is the workroom, not the rule.

Binaries
  A0-binary-provenance.txt
    $ go build -o bin/gs ./cmd/gs && go build -o bin/gitseq-mcp ./cmd/gitseq-mcp
    $ go version -m bin/gs bin/gitseq-mcp
    Both stamp vcs.revision=03f7f721cc6e1aead5d6d164f9dd07c6395427ab and
    vcs.modified=false, and the file also records `git rev-parse HEAD` and an
    empty `git status`.
  ./bin/evidence (used only for B1) is a throwaway main compiled against the
    same tree. It stamps the same revision with vcs.modified=true because that
    throwaway main.go was present while it built; its source was deleted
    immediately afterwards and `git status` is clean. It calls only
    statusview.Build/Render, workroom.RenderStatus/WorkOf and
    statusview.BuildActorStatus/BuildWait.
  No source was changed, nothing was committed, no gs write command was run,
  and the live residents were only read.

A. The real workroom through the live resident on 127.0.0.1:7777 (read only;
   that resident runs main ad4656dd8, which predates this change).
   Every A observation, and the B fetch, saw frontier
   43c958eabf13a25fbdb8c7486365cb97a0086a55 at depth 20989, 2518 commitments.

   A1-cli-bounded-localfold.txt
     $ ./bin/gs status --repo /Users/hughpyle/play/gitseq --server -
     Verified local fold, no resident. Frontier 43c958ea… depth 20989.
     Work, workroom: 6 open, 1541 completed, 960 closed and not completed,
     11 stale and not in flight, of 2518 commitments.
     Open by lifecycle: open 1, promised 1, reported 0, awaiting-review 4,
     awaiting-authorization 0, awaiting-landing 0. Overlapping: 4 moved,
     78 landing audit. Approved but not landed: 78.

   A2-cli-complete-all.txt
     $ ./bin/gs status --repo … --server http://127.0.0.1:7777 --all
     Bracketed by two reads of the resident frontier, both 43c958ea… depth
     20989, so the page is pinned to that frontier. Same two Work lines.

   A3-cli-json-work.txt (extract) and A3-cli-json-full.json (raw payload, 52 MB)
     $ ./bin/gs status --repo … --server http://127.0.0.1:7777 --json
     head 43c958ea… depth 20989; top-level keys include `work`;
     work = {scope workroom, commitments 2518, open 6,
     open_lifecycles {open 1, promised 1, reported 0, awaiting-review 4,
     awaiting-authorization 0, awaiting-landing 0}, completed 1541,
     closed_not_completed 960, stale 11, reasoning_moved 4,
     artifact_landing_audit 78}.

   A4-cli-bounded-live-resident.txt
     $ ./bin/gs status --repo … --server http://127.0.0.1:7777
     The bounded page reading the OLD resident at 43c958ea… depth 20989. It
     prints "Work populations: not reported by this resident; ask a newer one,
     or read the verified local fold with --server -." — the disclosure, not
     zeros. Its lifecycle totals and approved_not_landed 78 are unchanged.

   A5-http-status-summary-live-resident.txt
     $ curl -s http://127.0.0.1:7777/v0/status-summary
     Same frontier. durable.totals has no `work` key: this is the resident that
     predates the field, and A4 is what the new client does about it.

B. One frozen /v0/status answer counted independently by Go and by the browser,
   at frontier 43c958eabf13a25fbdb8c7486365cb97a0086a55 depth 20989,
   2518 commitments — the same frontier as every A observation.

   B0-frozen-fetch.txt / status-full.json (46 MB)
     $ curl -s http://127.0.0.1:7777/v0/status -o status-full.json
     Fetched 12:27:32Z. Records the frontier and that this old resident's
     complete answer carries no top-level `work`.

   B1-go-owner-frozen.txt
     $ ./bin/evidence status-full.json
     All four Go surfaces over that one file:
       1 bounded rendering (what `gs status` prints)
       2 complete rendering (what `gs status --all` prints)
       3 workroom.WorkOf (what `--json` .work and GET /v0/status .work carry)
       4 statusview.Build totals (GET /v0/status-summary .durable.totals.work),
         with approved_not_landed 78 read from the owner and the lifecycle
         count commitments["open"] = 1 kept distinct from the population
       5 MCP status and wait totals.work, reported identical to each other
     All read open 6 / completed 1541 / closed_not_completed 960 / stale 11 /
     reasoning_moved 4 / artifact_landing_audit 78 of 2518 commitments.

   B2-browser-selector.mjs (script) and B3-browser-selector-frozen.txt (output)
     $ node B2-browser-selector.mjs <worktree>/ui status-full.json
     Loads ui/src/lib/rows.ts from the worktree at 03f7f721 through vite SSR and
     calls the real workRows once per population. vite is imported by absolute
     path so nothing was written into the reviewed worktree.
     Same frontier, same numbers.

   B4-parity-comparison.txt
     Field-by-field comparison of B1 section 3 with B3: identical = True.

C. A scratch workroom served by the 03f7f721 binary on 127.0.0.1:7995, so the
   new wire fields are observed over real HTTP and real MCP rather than
   inferred. Two requests and one promise; frontier
   8d0786b2649836b427009fdde782d1f91d484729 depth 6 for every C observation.
   The resident was stopped and the repository deleted afterwards; the live
   residents on 7777/7778/7779/7780 were never touched beyond the A reads.

   C1-http-status-summary-scratch.txt
     $ curl -s http://127.0.0.1:7995/v0/status-summary
     durable.totals.work = {scope workroom, commitments 2, open 2,
     open_lifecycles {open 1, promised 1, rest 0}, completed 0,
     closed_not_completed 0, stale 0, reasoning_moved 0,
     artifact_landing_audit 0}; approved_not_landed 0;
     lifecycle commitments {open 1, promised 1}.

   C2-http-status-complete-scratch.txt
     $ curl -s http://127.0.0.1:7995/v0/status
     Top-level keys include `work`; same counts, same frontier.

   C3-cli-bounded-and-all-scratch.txt
     $ ./bin/gs status --repo <scratch> --server http://127.0.0.1:7995
     $ ./bin/gs status --repo <scratch> --server http://127.0.0.1:7995 --all
     The bounded page against a resident that does carry the counts prints the
     two Work lines (2 open of 2 commitments, lifecycle open 1, promised 1);
     the complete page prints the identical two lines.

   C4-mcp-status-scratch.txt
     $ printf '{… "name":"status" …}' | ./bin/gitseq-mcp --repo <scratch> --actor alice
     frontier 8d0786b2… depth 6; totals.work as above; approved_not_landed 0.

   C5-mcp-wait-scratch.txt
     $ printf '{… "name":"wait","arguments":{"timeout_ms":1000} …}' | ./bin/gitseq-mcp …
     Same frontier, identical totals.work.

D. Gates at this head.

   D1-ui-tests.txt
     $ cd ui && node --test test/*.test.mjs
     229 tests, 229 pass, 0 fail. The five work-population tests are named in
     the file: the selector parity, the two negative controls (dropping an
     awaiting-review member; regrouping awaiting-review out of open), the
     ratification separation, and the JSDOM search-scope render.

   D2-go-gates.txt
     $ go build ./... && go vet ./... && gofmt -l .   → exit 0
     $ go test -count=1 ./...                          → exit 0
     35 packages ok, no failures, run alone. The machine was loaded again
     (average 18 at start, 51 at finish), and this time nothing flaked: the two
     timing failures seen at 1fdf9e6b (cmd/gs hitting the default package
     timeout, internal/service wake latency) did not recur, so there is no D3
     for this head.

MANIFEST.txt lists every file in this directory with its size and SHA-256, so a
reader can bind an attachment to the trace named above.
