# Gitseq recovery review — 6 September 2026

The proposed recovery preserves the previously verified history and both signed cleanup events. It needs Hugh’s explicit authorization because it selects one of two signed sibling histories as canonical. Routine resident-management permission does not resolve that choice.

I independently reproduced both controls on the installed Git: the current sequence-to-itself tracking mapping loses a concurrent append after a successful push; a separate tracking destination preserves it. Ordinary non-forcing fetch refused the rewind in a separate control. The observed cause is push, not fetch. Both sibling commits have parent a9ade32a; the saved frontier remains b5c32b72 at depth 19193, while the live ref names cd0e24ab. The incident bundle verifies. The 36 accessible registered worktrees share that saved frontier. My existing adapter verified b5c32b72 and refused cd0e24ab; I have no separate running reader that accepted the sibling. This does not establish what unknown remote readers saw.

The revised plan includes the review conditions: exact preserved binary and restart command; a pause of all sequence writers, direct CLI mutations and overlapping pushes; exact local/remote heads and known saved-frontier checks; refusal on a conflicting witness; and replacement of the unsafe origin mapping before admission resumes. I independently checked the preserved binary matches canonical bin/gs at SHA-256 0b8aeeec3b31a3aefbd0714e7a1d96286f1cd94bf7746748c5f9e6ae7dc978b6. It is the existing dirty183211 build, not a clean rebuilt release. These checks make the plan ready for Hugh’s decision; they do not authorize execution.

Then stop only the Gitseq resident, restore b5c32b72 with an exact comparison against cd0e24ab, verify using the unchanged saved frontier, and resubmit the archived cleanup intent as a new descendant through normal admission. Preserve the old sibling and do not represent its event ID as part of the recovered chain. Publish only through separate tracking refs and verify remote ancestry. File a durable incident and prevention request after admission is restored.

Notes source b2ed9266 is independently confirmed on origin; main CI 34023306460 and Security 34023306466 passed. Its live reader upgrade and final artifact cleanup remain pending. The other three workroom frontiers continue normally. No live ref, verifier checkpoint, custody file, service, or source was changed by this review.

Evidence: [recovery plan](/tmp/notes-sequence-recovery-plan.md), [fork evidence](/tmp/gitseq-frontier-fork-20260906.json), [push control](/tmp/planner-push-tracking-control-20260906.json), [separate-tracking control](/tmp/planner-safe-push-tracking-control-20260906.json), and [saved-frontier inventory](/tmp/planner-gitseq-frontier-locations-20260906.json).
