# Whoami frontier retry fixture: validation

Governing request: git:sha1:5d2622748872b7e2dec3fe5c59e4be73a35e0bc8#git:sha1:a847510108ed78b84444be89109162e2b3c41725. Based on delivered main1b0692eef. Only cmd/gitseq-mcp/main_test.go changes.

The inherited full-race failure at133cd39c5 recorded two resident calls and verified_incremental_tail/degraded=true after4.28s. The producer, MCP and test source were unchanged from Notesb2ed. Source inspection establishes that signing/committing the second event and the second Snapshot ran inside the production orientation deadline. A single instrumented, unmodified-behavior comparison passed0.95s and measured230.560ms for the first handler and25.294ms for the second. This supports contention sensitivity; it cannot reconstruct the exact scheduler delays of the earlier failure, so those delays remain an inference. Temporary timing instrumentation was removed.

The corrected fixture builds a real signed successor in an isolated copy of the same signed genesis, fetches only its objects and audits it in the consumer store with kernel.VerifyAt before the timed call. Both actual snapshot orientations are encoded in advance. The first response performs a checked first-to-second canonical ref CAS and returns the first answer; the second returns the verified successor. The consumer never rewinds its canonical ref or saved memory. The final assertion requires exactly two reads, a current resident answer, no degraded fallback and the exact second head/depth. The existing stall/redirect and deadline tests remain unchanged; production orientationTimeout remains two seconds.

Focused corrected race passed1.40s (package2.792s). A real temporary production omission (whoami loop attempt<1 instead of<2) made the same fixture fail1.44s with one call and truthful local degraded fallback. Restoring production bytes was verified against Git. A separate temporary omission of the checked ref move failed1.32s with one call and a first-frontier resident answer. Both mutants were removed; no mutation is part of the candidate. These controls demonstrate that neither missing retry nor missing concurrent movement can satisfy the fixture. No timeout increase, sleep-based timing, accepted degraded success, or retry-until-green was used.

Architecture: layer7 MCP orientation is exercised with layer1 Git ref movement, layer2 signature verification, and layer6 statusview projections. Layer3 live runtime, layer4 binding/custody and layer5 application contracts are unchanged. There is no production contract change requiring a new architecture page. Security: the test audits imported signed history, checks CAS failure, retains exact frontier and authority/roster validation, and does not access live workrooms. Simplification: keep one test with two prepared responses and existing fixture helpers; add no production seam or scheduler/timeout workaround.

Full local MCP package race: ok  	github.com/generalbusiness-ai/gitseq/cmd/gitseq-mcp	52.890s. Repository go vet ./... passed. Exact candidate CI will be recorded separately.
