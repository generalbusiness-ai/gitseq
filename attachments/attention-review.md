Review note for Hugh: keep history visible without repeating its full rows on every wait.

Recommendation: retain the full bounded status orientation and explicit closed-history queries, but summarize unchanged cancelled/reneged history by count in repeated wait responses. Continue to repeat every unresolved current obligation, including stale assignments, approved-but-unlanded work, and any explicit broken-succession qualifier. Newly recorded closures still appear in the durable delta. Add no acknowledgement, retirement, completion or lifecycle state.

Why: at Gitseq depth18320, 19 of the 20 current_not_actionable rows were cancelled/reneged history (8,797 UTF-8 bytes of the 9,345-byte row list); 109 more rows were omitted. Tailapp depth3487 repeated five such rows, 2,090 bytes; Chess depth719 repeated five historical rows beside its still-owed stale public-deployment task, 2,605 of 3,144 bytes. Inventory had none. These are compact JSON list sizes from sampled responses, not network traffic totals or measured model effort. The histories differ, so these figures do not rank team productivity.

This behavior is deliberate today: statusview's terminal filter excludes cancelled/reneged, and TestNonActionableCommitmentsAreSeparatedFromWaiting expressly requires those rows in both status and wait. The recommendation changes the repeated-wait presentation contract; it is not a correction to a falsely implemented existing rule.

The dropped-task audits are the constraint, not a reason to call every cancellation finished. Tailapp history already includes incorporated work administratively labelled cancelled, while Chess's public deployment remains owed. Keep a visible closed-history count and exact work-query route. Never infer completed outcomes from a closed label or hide explicitly unresolved succession. The existing retirement-succession design already specifies a broken qualifier; preserve its attention semantics rather than inventing another closure scheme. Regular closed-history audits remain part of planning.

Small implementation scope, if Hugh adopts it: one shared wait projection policy and truthful summary/query hint; update its MCP contract and mirrored tests. Acceptance must prove (1) quiet repeated polls omit unchanged historical row bodies, (2) current stale/held/unlanded/broken obligations remain discoverable even with hundreds of closed rows, (3) new closures appear in the delta, including reset/truncation recovery, and (4) explicit history queries still return the original complete records and truthful caps. If a history count cannot safely identify what an interrupted reader missed, keep that part of the current response until its query path is proven.

Priority: finish the active CI and landing repairs first. This is advice for Hugh's review, not an adopted policy or permission to change the fold. The expected benefit is less repeated reading and clearer attention; no time or token saving is claimed until a before/after trial measures it.