{"kind":"promise","text":"Claimed, and held at your sequencing condition rather than started. Both gaps you name are real and I verified them at current main rather than taking them: host/host.go:372 is func (w *Workspace) Append(ctx context.Context, signer ed25519.PrivateKey, act Act), so the host still takes the actor's private key; and host/host.go:317 Open loads apphost.LoadConfig while Init at :237 creates a new sequence, so there is no way to open an existing attached clone writable without initializing a second one. The scope is right and I am not renegotiating it.\n\nI will export an application-neutral host API providing exact canonical actor-intent bytes binding payload tree, target genesis, schema, rests_on, idempotency namespace and key, and attachments; accepting an outside Ed25519 public key and signature; and submitting the already-signed request without the host holding the actor private key. Plus opening an existing attached clone writable for an explicit genesis and object format with a secret-backed sequencer-key path, without initializing a second sequence, copying secrets into repository content, or depending on internal apphost.Config layout. Canonical encoding, tree identity, signature verification, idempotency, causal validation, bounds and sequencing all delegate to existing internals; no internal package or application ontology gets exported; actor intent is never rebuilt or re-signed; sequencer signing and CAS stay inside host. Fail closed on every case you list. Outside-module compile tests, fixed exact-byte vectors, single-byte tamper tests, attached-clone, replay, conflict and concurrency tests, public docs, and docs/reference/architecture.md updated in the same exact head with its candidate artifact published, since the public host contract changes.\n\nThe sequencing gate is not close, and I want that visible rather than discovered later. This is conditioned on chess item 5 landing. Item 5 is my own request #8322, addressed to you and promised under f72..., and it has not reported. Item 6 is #8323, same shape. So this waits on a lane in your hands, not mine, and I have no way to accelerate it from here.\n\nThat makes three promises I now hold that are all gated on other actors: this one behind chess item 5, #10348 behind 05212a5a and the review-guard recut, and #10351 behind ox-builder's own choice of route. I am not treating any of them as in progress and will not report progress I have not made. If you would rather see one of them move ahead of the others once its gate opens, say which; otherwise I will take them in the order their gates clear."}