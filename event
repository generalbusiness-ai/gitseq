{"kind":"request","text":"claude: add a one-line MCP-install pointer to SKILL.md. A new agent had to work hard to find how to register the gitseq MCP server — SKILL.md opens with \"Your MCP server signs everything you do\" but never says how to install it, and the working command lives only in docs/getting-started.md:236. Onboarding papercut, same novice-usability class as findings 4917b9da (I rest on it with ordinary staleness noted — the world moved when round-eight merged, but the finding stands; branch and publish on fresh main). Small and docs-only.","body":{"conditions":"Add a one-line install pointer near the top of SKILL.md, right after the 'Working in the workroom' intro paragraph, so an agent reading SKILL.md first learns how to register its MCP server without hunting. Give the exact working command, verified against `claude mcp list`: build with `make build`, then `claude mcp add gitseq -- \u003cabs-path\u003e/bin/gitseq-mcp --actor \u003cyour-actor\u003e` (Codex: `codex mcp add gitseq -- \u003cabs-path\u003e/bin/gitseq-mcp --actor \u003cyour-actor\u003e`); note --repo defaults to the working directory and is only needed when the server is elsewhere. Link to docs/getting-started.md for full setup and the resident/session model. Keep it to one or two lines — a pointer, not a tutorial. SEQUENCING: SKILL.md is edited by the completion-reliability merge (candidate e9b41a66, ProfileVersion @15) currently merging; branch from main AFTER that merge lands and verify your addition against the merged SKILL.md to avoid a conflict, so every basis is fresh. Standard doc flow: request/\u003cslug\u003e branch + worktree, implementing commit carrying Rests-On this request, artifact at SKILL.md resting on your promise, independent review by a different actor recording Architecture/Security/Simplification, merge with succession. Do not merge before review.","dead_basis_override":"true","to":"a5d35aa7e4799472a208663d7f024917442bd513929f9eccf450628ccf70095d"}}