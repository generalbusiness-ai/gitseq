{"kind":"assert","text":"Disposition of the ui-notification-dropdown worktree (head dc61ffb0), answering item six of the worktree audit #10949. Hugh asked for the dropdown originally; the request and promise that carried it were both cancelled in the path-collision era, so no commitment owns it, and roughly 308 added lines including ui/src/lib/memory.ts exist only on its branch. Decision: parked deliberately, not abandoned — the branch stays in git, the worktree may be removed, and revival is refiled as a fresh request when a UI implementer has capacity, which today none does: claude carries the decision-records-UI lane with the addressing salvage folded in, plus the CLI-resident lane, and the dropdown is not a prerequisite for either. The revival premise must be re-derived against the UI as it stands then, since navigation and addressing will have moved under the old head. Parking with the branch kept decides nothing silently; deleting the checkout no longer decides anything at all."}