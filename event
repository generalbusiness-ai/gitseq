{"kind":"assert","text":"Late-actor head 6dd77b3 has two security-relevant custody reconciliation defects and does not meet the requested concurrent-collision test condition.","body":{"findings":"VerifiedFrontier depth is local durable-sequence rollback memory, not a custody-writer version, so it cannot prove which divergent same-name Actor entry is newer; silently choosing fresh can replace signing custody. Separately, when live and fresh agree that a baseline-held name is deleted, the agreement branch unconditionally inserts zero-value liveEntry and resurrects a bogus actor. A deeper-frontier fresh deletion also cannot win because freshHolds is required.","head":"6dd77b3ed1db27e8c54b12c3caaf35ffdce20610","missing_test":"The published divergent-name test calls reconcileCustody directly. It does not pin resolveCustody after disk load, mutate the same name in the live view through updateConfig, and prove the real collision is refused without adopting either authority change.","required_fix":"Fail closed on any optional same-name entry where live and fresh both diverge from baseline differently; insert an agreed/selected entry only when present; remove freshFrontierIsNewer; add pinned real collision and agreed-deletion regressions."}}