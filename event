{"kind":"artifact","text":"spike/forge/compose_test.go at exact 361316620302bdd4580e68256189d2529c6719d6 on request/forge-compose-port-regression (pushed; one commit over main 86970224), the reporting artifact under promise c800b95e on request 90243449 (#21314). The port gate matched entries with a regular expression anchored to the end of the line, so it read Docker's short form and silently dropped everything else, including the long form; a mapping carrying host_ip 0.0.0.0 could therefore sit in the ports block and pass the check whose only purpose was to catch that. The gate now decodes the file with gopkg.in/yaml.v3, already a direct module dependency, and reads the ports of every service by name. It understands the short form including a bracketed IPv6 host and a protocol suffix, and the long form's seven documented fields; an entry it does not recognise is reported as a finding rather than skipped, and a missing host address is reported alongside the wildcard because Docker binds every interface in both cases. Conditions met: silent omission is replaced by a small service-scoped structured check using the existing YAML dependency; both documented forms are read, including entries after a valid safe one; malformed and unsupported entries are rejected explicitly; every published binding must name 127.0.0.1 or ::1, so missing, wildcard and non-loopback values fail; the loopback configuration, the first-run install lock, the commented-out-lock and other-service scoping regressions and the documented administrator workflow are all preserved unchanged; and nothing here is a general Compose implementation or adds a Docker or network requirement to the ordinary suite. Thirteen fixtures written independently of the shipped file sit on the boundaries: both safe forms, the wildcard and host-less short forms, a container port alone, a wildcard long form appended after a safe entry, a long form omitting host_ip, an unsafe mapping hidden behind a safe one in a second service, an undocumented long-form field, a sequence where an entry belongs, and a short form with too many fields. The attached fixture is asserted to be rejected exactly once, named by its wildcard address. One test reintroduces the parser this replaces and asserts what it did with that same file - it found the safe entry, found nothing else and reported no error - which is what makes the gap a demonstrated syntax blind spot rather than a claim, and it fails by assertion rather than by failing to compile. Gates at this head: gofmt, go vet, go build and git diff --check clean; the whole spike tree passes, all thirteen subtests ran and passed, and internal/docset passes. Mutation evidence: replacing the shipped spike/compose.yaml with the attached long-form addition fails TestEveryPublishedPortBindsLoopback naming 'published 3301 to target 3000 on 0.0.0.0', and separately changing its short-form host to a wildcard fails naming '0.0.0.0:3300:3000'; the tree was restored from HEAD after each and the committed compose.yaml is untouched. No container was started and no Compose runtime was exercised, matching the request's own stated limit. First publication at this path.","body":{"branch":"request/forge-compose-port-regression","commit":"361316620302bdd4580e68256189d2529c6719d6","path":"spike/forge/compose_test.go"}}