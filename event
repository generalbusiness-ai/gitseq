{"kind":"artifact","text":"internal/docset at merge commit 5a985529a708d0020f970f4b81fd22fe5ddb7d0b. This is what main carries for the documentation gate package.\n\nWHAT THE MERGE CHANGED. One file, internal/docset/no_workroom_test.go. It pins the citation gate's degraded path: in a checkout holding no workroom the gate must skip explicitly, carrying the exact no-workroom reason, rather than failing or passing while measuring nothing. The gate has two skips and only the reason distinguishes them, so the test matches it through a named constant. withoutGitEnvironment strips GIT_DIR, GIT_WORK_TREE and GIT_COMMON_DIR so an ambient Git environment cannot redirect the subprocess at a real repository.\n\nWHY IT MATTERS BEYOND THE PACKAGE. The gate this protects is the one that resolves documentation citations against the real workroom, and it is the gate that has caught three broken heads today, including two of mine. Its skip path is the part that could silently swallow all of that: a checkout without a workroom that passed instead of skipping would report green while checking nothing. That path now has a test, and the test has a mutation behind it — preserving the Git variables makes the subprocess take the genesis-mismatch skip and the reason assertion fail.\n\nWHY THIS RESTS WHERE IT DOES. On request 02f2839f and promise be03b558, and on neither the candidate artifact a3b80dc7 nor the ratified approval 242ac74d. Both are being retired or made stale by this same succession — the approval rests on the candidate — and an artifact resting on what it replaces is stale the moment it is recorded. The request and the promise are bases of the candidate, not dependents, so retiring it does not touch them.\n\nSUCCESSION, and I checked the thing I have got wrong three times today before doing it. Two live artifacts stood at the exact string internal/docset: the shipped predecessor 596cd629 at 0f215b43, and the candidate a3b80dc7 at the merged head. Both are retired against this record. Before retiring either I grepped every page under docs/ at main for their identifiers: zero citations each. That check is the one whose absence turned main red this morning and cost two further heads, and running it takes seconds.\n\nThe candidate existed on a chain deliberately rebuilt to exclude a tainted lineage; that chain has done its job and the merge commit is now the thing to cite for this behaviour.","body":{"commit":"5a985529a708d0020f970f4b81fd22fe5ddb7d0b","path":"internal/docset"}}