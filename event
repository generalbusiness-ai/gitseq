{"kind":"request","text":"Claude: finish the non-conflicting repository-owned security gates while preserving the ratified disclosure policy. This child narrows parent request 6ace1fbe after assert cba54293 proved its SECURITY.md condition contradicted decision 27ad16c2. It also makes the operator constraint explicit: no rm -rf in scanner tests or scripts.","body":{"branch":"request/repo-security-gates","conditions":"Preserve the ratified existing SECURITY.md disclosure policy and its public_surface_test gate; withdraw the parent request's conflicting advisory-URL condition. On current main, change CI from make ui to make ui-check; add pinned, reproducible Go advisory scanning and a repository-owned committed-secret scanner; add Dependabot configuration for Go modules, npm under ui, and GitHub Actions with bounded cadence. Keep workflow permissions least-privilege, actions and tool versions pinned, generated UI reproducibility intact, and do not duplicate existing vet/race/test gates. The secret scanner must inspect the intended Git history or state clearly and testably that it scans the tracked current tree; its own fixtures must not trigger a real run. No script or test may contain or execute `rm -rf`; use t.TempDir, mktemp with scoped cleanup that does not invoke rm -rf, or repository-native test cleanup. Validate actual YAML semantics rather than only substrings where practical. Run focused scanners/tests, full Go tests, vet, docs, ui-check and diff checks; commit with canonical Rests-On this child; publish exact-head artifacts separately at .github/workflows, .github/dependabot.yml, and .github/scripts (plus internal/ciguard if retained); request independent Architecture/Security/Simplification review.","to":"a5d35aa7e4799472a208663d7f024917442bd513929f9eccf450628ccf70095d"}}