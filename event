{"kind":"artifact","text":".github/workflows/ci.yml at exact 46e09ffde115d8d44088053a456ba97eff0e694d on request/ci-evidence-retention (pushed; one commit over main 5cc098a8; sole changed path), the implementation report under promise 4550102974 on request 876ee128 (#21346, recover the CI evidence-retention outcome of closed #2488). The defect: actions/upload-artifact at the pinned v7.0.1 commit skips any visited entry whose own name begins with a dot unless include-hidden-files is on, and for path spike/.spike/ the directory itself is the first entry visited, so every failed run stored nothing (the attached failing-run log shows 'No files were found'). The correction changes one value: path now names the two generated files, spike/.spike/evidence.json and spike/.spike/SPIKE-RESULTS.md, one per line. A named file is itself the search path and is never the skipped entry, so no hidden-file opt-in is added; naming the files also bounds the upload to the intended evidence. if: failure(), the pinned action, retention-days 30, if-no-files-found: warn and the read-only permissions are unchanged; the step comment, shortened as Codex's ratified verdict f970d276 required, says the two exact paths avoid the hidden-directory filter and keep unrelated files out, that the step warns and creates no artifact when it runs with no generated files, and that a lost runner can prevent upload altogether. This head replaces f09a9b32 after that verdict: the YAML value is byte-identical, only the comment changed, so the selection demonstration and the controlled fixture run (executed on f09a9b32's identical upload configuration) remain the evidence for this head; bd5b2dac is retired naming this artifact. Selection demonstrated with the action's real implementation (selection-driver.mjs imports the pinned action's compiled search and calls its findFilesToUpload with the action's own arguments) against a fixture holding the two files, a hidden sentinel in the same directory, another hidden file and two ordinary files: old selector zero files; whole-directory with include-hidden-files true three files including the sentinel, which is why that alternative is unbounded; corrected explicit paths exactly the two files with their exact bytes (sha256 60248523… and 4810fd67…), sentinel and others excluded, with include-hidden-files at its default false (selection-output.txt, selection-results.json). Controlled fixture: ci.yml triggers only on push to main and pull_request, so a temporary branch fixture/ci-evidence-retention-20260908 (861a9a01, the corrected head plus one step 'Fixture failure after evidence (temporary)' running exit 1 immediately after the evidence step) was opened as draft PR #31; run 34208177412 concluded failure with the evidence step success, the fixture step failure and the upload step success; the production upload configuration is verbatim in fixture-upload-step.log (two files found, 2632 bytes uploaded); artifact id 10048836461 named adversarial-evidence-34208177412-1, created 2026-09-08T09:11:57Z, expires 2026-10-08T09:11:57Z (30 days); downloaded with gh run download: exactly evidence.json (parses, schema gitseq.spike-evidence.v0) and SPIKE-RESULTS.md (2122 bytes), sha256 a0c2e306… and d52f1215…, no sentinel; the PR was closed and the fixture branch deleted on origin and locally, and the artifact still reads unexpired afterwards (fixture-download-and-cleanup.txt). Main was never failed; the full race suite ran once, on that fixture run only. Checks at 46e09ffde115d8d44088053a456ba97eff0e694d (local-checks.txt): reporter tests, internal/ciguard (admissible automation, UI embed, captured-run evidence projection, dependabot coverage), public repository surface, local Markdown links, performance workflow refs, docset gate, go build, git diff --check and the secret-scan script cases all pass; no test or doc asserts the old path string, and the architecture page describes no CI-upload contract, so no other path changes. Conditions met: a failed job that produced the two files retains both as a downloadable readable artifact for 30 days; selection limited to intended evidence and excluding the hidden sentinel; failure-only upload, captured exit status, quiet passing runs, pinned action and permissions preserved; old and corrected selectors demonstrated with the action's real selection; one controlled failed-job upload and download recorded with run, artifact and retention identity and the fixture removed; the before-evidence failure behaviour stated. New path; no live artifact at this path or its directories. Still owed on this promise: independent Codex review, merge with succession, push, CI and cleanup of the owned branch and worktree.","body":{"branch":"request/ci-evidence-retention","commit":"46e09ffde115d8d44088053a456ba97eff0e694d","path":".github/workflows/ci.yml"}}