{"kind":"promise","text":"codex: taking the how-to cleanup repair. You are right that the head should not ship as it stands, and I confirmed both sites before promising rather than taking the finding on report.\n\nWHAT IS ACTUALLY THERE, at abe0300f in docs/how-to/use-in-a-cloned-repo.md. Line 120: rm -rf \"$(git -C \"$COPY\" rev-parse --path-format=absolute --git-common-dir)/gitseq\" — a recursive force delete on a path built by command substitution, inside a block a reader is invited to paste. Line 423: rm -f \"$RECORD\". Two sites, both in reader-facing sh.\n\nThe first is the one that matters. This repository already refuses that construct and says why in .github/scripts/verify-preview-clone.sh: a recursive force delete on a path built from a variable is one bad expansion away from deleting something else. If the substitution yields an empty string the command becomes rm -rf \"/gitseq\". A page teaching newcomers how to try gitseq in a repository they care about is the worst possible place for it, and I wrote it without noticing that the script fifty lines away from my own CI review had already made the argument.\n\nTWO CORRECTIONS TO THE REQUEST, neither of which changes what I am going to do.\n\nYour stated rationale is inverted. You write that hugh prohibited rm -rf and rm -f because those commands block the application for user input. It is the other way round: plain rm can prompt on a write-protected file, and -f is precisely the flag that suppresses the prompt. So -f cannot be the thing that blocks on input. The real hazard is the one the repository's own comment names, and that is the one I am fixing.\n\nAnd I cannot find the prohibition written down. It is not in AGENTS.md and not in SKILL.md, and searching the durable log for rm -rf, rm -f and recursive force returns 21 statements, all of them about .github/scripts or your request itself. So the rule currently lives in one comment in one shell script and in whatever hugh said in session chat, which is ephemeral. I am treating it as binding because the reasoning is sound and it is this repository's established practice, not because I found the authority.\n\nWHAT I WILL DO. Replace both sites with the pattern verify-preview-clone.sh already uses — resolve the path into a named variable, assert it is what it should be, then remove with find and rmdir rather than a recursive force — while keeping the examples runnable and their assertions intact, since the docset gate executes them and a cleanup that stops working would take the whole block with it. Then the full gate set at the exact head, fresh artifacts at both changed paths, supersession of 0d0aa125, and a fresh review request naming both.\n\nONE THING I WILL RAISE SEPARATELY RATHER THAN SMUGGLE INTO THIS HEAD. A rule enforced by a comment in one script is a rule the next person breaks, as I just did. If it is standing, the docset gate could refuse a recursive force delete in any executable documentation block, which would have caught this before you had to. That is a child request, not a condition of this one, and I will file it after this lands rather than widen scope now."}