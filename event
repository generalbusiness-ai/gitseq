{"kind":"artifact","text":"docs/getting-started.md at exact clean pushed head 245ee44d7ea8c801e5d4a876ef8709c78f83b5d7 on request/getting-started-runnable, based on main 9ec7109c, fulfilling planner's 23310281 under promise d5cf4482. It replaces the artifact at fe8aa8ba, which my own retirement of twenty-six dead temporal-lane artifacts made world-stale minutes after I published it.\n\nWHAT THE PAGE DOES, unchanged from fe8aa8ba except its citations. It initialises a workroom and says what init writes, what it leaves alone, and that it is neither idempotent nor possessed of a default identity. It adds planner, builder and checker and shows the roster. It starts a resident and states the boundary that comes with it. It registers one gitseq-mcp server per actor with a worked example for claude-code and one for codex, including where each client records the registration and how to install the workroom skill. It then proves the binding without starting a client, by piping a whoami call into the builder adapter and matching the fingerprint against the roster row. It files a first request so the loop has work, and starts one session per actor with Hugh's prompt shape, spelled workroom, naming that session's own MCP.\n\nWHAT WAS EXECUTED, and what was not. Every sh block was run from an empty directory, in order, under set -euo pipefail. Both clients were registered for real and both listed their servers connected. Identity binding was proven non-interactively in both: claude -p calling the builder MCP's whoami returned builder and its roster fingerprint, and codex exec calling planner's returned planner and its own. The three indefinite interactive loops could not be run, because they are interactive and unbounded; the one-shot sessions are the non-interactive maximum and they exercise the binding those loops depend on. Codex exec runs with approvals never, so its first whoami was refused until the tool was pre-approved, whereas an interactive session prompts once, which is what the page tells the reader.\n\nWHY THE CITATIONS CHANGED, which is the whole reason this artifact exists. I first cited the freshest live artifact at each path, and three of the four stood on unmerged candidate heads. Planner traced what that costs and ruled: prefer the merge-published artifact even when it is ordinarily stale. Ordinary staleness is recorded in the receipt and the merge lands; world staleness inherited from a candidate whose head dies is refused outright, so an approval on the affected artifact is unmergeable however good the page is. All four bases are now merge-published artifacts on current main. Three of them are ordinarily stale and that is the survivable kind. I re-cited all four rather than only the one that broke, because the other three stood on my own custody candidate and would have failed the same way if that head dies.\n\nGATE: go test -count=1 ./internal/docset green at this exact head, 49.6s, and the docset gate executes the page's sh blocks itself, so the page is machine-verified. git diff --check clean, worktree clean, origin equals the head. No Go files touched.","body":{"commit":"245ee44d7ea8c801e5d4a876ef8709c78f83b5d7","path":"docs/getting-started.md"}}