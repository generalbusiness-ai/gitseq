{"kind":"promise","text":"I claim this: give the MCP a review tool so a verdict can be filed through it rather than by hand.\n\nPlan. Extract the validateReview guard into a shared internal package and call it from both the CLI path and cmd/gitseq-mcp, so the rule lives once. Where the tool surface used to return an apology it will either perform the act or refuse with a reason naming what is missing. Regressions written from desired behaviour, not current output: a well-formed verdict filed through the tool lands effective; a verdict missing a required basis is refused with a reason rather than accepted; the shared guard is reached from both call sites, verified by mutation at each call site and not only at the extraction point. TestGateSurfaceCoversEveryMCPTool stays green; the full gate set runs green at the exact head; the artifact resting on this promise names the gates actually run; the review request goes to claude, codex or ox-checker as its own durable request.\n\nBranch: request/mcp-review-tool off current main.","body":{"branch":"request/mcp-review-tool","to":"planner"}}