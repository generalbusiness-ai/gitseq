{"kind":"artifact","text":"internal/app implementation at exact clean pushed head 53b5f2c638ca1636e10de087cb2ab4b9e681f6f7 on request/report-lifecycle-profile, fulfilling request b4414c05 under promise 4348b70d and the parent request b9f65395. Succeeds 2f06451f, which codex refused. Codex reverted ProfileVersion from workroom-fold@9 to @8 at that head and the witness still passed, so it proved nothing about the transition. Two causes, and the review named the first: the stored profile is the application and the fold version joined by NUL, so seeding a bare workroom-fold@8 could never match what the build holds and the cache was dropped for an unrelated reason. The second is that the seeded object was never consulted at all -- snapshotFlight refuses to join a completed flight, so the installed stale flight was discarded, and what gates the answer is snapshotCache with snapshotProfile at the head of snapshotWithSource. The witness now seeds snapshotCache with the lifecycle-free projection at the current head, so the profile is the only thing that can force a replay, and asserts the exact composite going in and coming out. Reverting @9 to @8 now fails it with 'rebuilt projection carries 0 lifecycles, want 2: the workroom NUL workroom-fold@8 cache was served instead of replayed'. Permissive contains checks replaced by exact oldProfile and wantProfile, and the doc comment names TestProjectionProfileChangeRebuildsFromTheSameKernelCheckpoint. gofmt clean over every tracked Go file, go vet ./... clean, go test -count=1 ./... pass, race on internal/app, internal/workroom and internal/wireparity pass, internal/docset pass in 46.043s, 83 UI tests pass with 0 failures, git diff --check clean, worktree clean, origin equals the head.","body":{"commit":"53b5f2c638ca1636e10de087cb2ab4b9e681f6f7","path":"internal/app"}}