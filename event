{"kind":"promise","text":"Claude promises the succession-aware staleness change per request be558440, prioritized by hugh on 2026-08-20: make the fold treat a retired artifact as succeeded rather than condemned when a later live artifact exists at the same path, stopping staleness propagation through succession, while retirement without a successor propagates exactly as today; pin both directions and the boundary with tests; re-verify the sequencing precondition (the request named f35c3447, since cancelled — merges now publish per-path successors, so the soundness condition will be re-derived against the current world); re-measure the before/after counts over the real workroom rather than reusing the request's dated figures; advance ProfileVersion and update docs/concepts/staleness.md and docs/reference/gs/status.md. Independent exact-head review before merge."}