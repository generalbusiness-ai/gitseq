{"kind":"request","text":"ox-builder: a proposal awaiting ratification opens an unrelated satisfied request, and hides once it gets there.\n\nHugh hit this on proposal 2147, which the awaiting-ratification tab lists correctly -- it is an effective, unratified propose by claude, and only hugh holds ratifier. Clicking it lands on request 2001, a request satisfied on 2026-08-10 that has nothing to do with the proposal, and 2147 is not visible on that page.\n\nThree layers, each defensible alone.\n\nui/src/App.tsx 31-33 discards the row identity on navigation: openThread rewrites the clicked event to index.threadRoot(event) unconditionally. ui/src/components/RequestList.tsx 184 passes the proposal own event, and it is thrown away before the screen is set. The comment above openThread explains the intent -- \"a promise, report, act, artifact or assert lands on its request\" -- and that list does not include proposals, because a proposal belongs to no commitment at all.\n\nui/src/lib/records.ts threadRoot walks the first-basis chain until it finds a request. For 2147 that chain is 2147 propose, then 2013 artifact, then 2001 request. The proposal cited an artifact for context; the artifact happened to answer 2001 two weeks earlier. Any record citing an artifact inherits that artifact request as its thread, however unrelated. threadRoot has a correct path for a record with no request above it -- it returns itself and the thread screen reports not a thread -- but two hops of citation reach a request before that can fire.\n\nui/src/lib/spine.ts buildExpanders classifies the propose into the chatter fallback, because it matches no station kind and no expander predicate, so it lands in the Talk expander labelled \"notes and asserts\", collapsed. The record is on the page, behind a control whose label denies it exists.\n\nI verified each of these by reading the code and re-deriving the 2147 chain from the projection, not from the UI.\n\nA companion request follows this one, for shareable URLs and working browser-back, touching the same App.tsx. Land this first; I am addressing both to you so the two do not collide at that file.","body":{"conditions":"Clicking a row in any list opens the record that row names. A record that belongs to a commitment still opens in the thread context of that commitment, and the record the user clicked is visible and identified on arrival without expanding a control. A record that belongs to no commitment -- a proposal, a free-standing assert -- does not get filed under an unrelated request; it opens as itself. The propose kind is no longer classified by the chatter fallback: either it earns its own place or the fallback is renamed to say what it actually holds. A regression test written from the desired behaviour fails at current main and passes at the head, covering at least a propose whose first-basis chain reaches an unrelated request; verify by mutation that reverting the navigation change makes it fail. The head satisfies the standing UI review bar: no per-row projection scans, disclosure through real buttons, every reference resolved, and at least one handler mutation shown to be caught rather than survived. make ui is re-run and the regenerated internal/service/uidist embed is committed in the same head, so make ui-check is clean and gs serve does not keep serving the old bundle. The exact head names its tests and the affected ui contract, publishes a successor artifact at ui retiring the live predecessor 24deafc4, and requests review from an actor other than planner.","to":"9882d3192ef13436b037ff5bdd0347c28c6447ebdbdf434aa62f89398577ab90"}}