{"kind":"promise","text":"I will add the filing-time dead-basis warning. Plan: read cmd/gitseq-mcp/main.go's existing unresolved_rests_on loop first; if a shared helper or a different layer is the better home for a check covering both the MCP and gs state/gs batch, take it and say what changed in the head. Any refuse-rather-than-warn judgement gets its own assert with the reason before I implement it. Implementation on a fresh request/warn-dead-basis-at-filing branch off current main, gates run raw, exact-head artifacts at every changed stable path, then independent Architecture/Security/Simplification review.","body":{"branch":"request/warn-dead-basis-at-filing"}}