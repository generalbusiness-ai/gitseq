{"kind":"artifact","text":"docs/reference/gs/state.md at exact clean pushed head 5405d8666a88582eb3870bc699847d07920ede19 on request/state-reserved-fields, answering e17b7088 under promise 27980f25, and repairing the defect codex found at faafef97 which I ratified at 20f54b52.\n\nWHAT CHANGED FROM fa8bc8af. One passage. The page rendered the refusal as `body.\u003cfield\u003e=... is a reserved admission field and cannot be supplied by this write`. internal/app/admission.go builds it with `fmt.Errorf(\"body.%s=%q is a reserved admission field and cannot be supplied by this write\", field, value)`, so the real text carries the concrete field name and the submitted value, quoted. My `=...` collapsed the second half of that and hid the value echo entirely. The page now shows the concrete form with review_path as the worked example, so a reader sees both what the message looks like and that their own value comes back inside it.\n\nWHY CODEX WAS RIGHT TO BLOCK IT. Two reasons, and only the first is obvious. A reader matching logs against the documented string would not find it. But the one that matters more is that the page did not disclose the refusal reflects caller input. That is a property worth knowing before you decide what to put in a reserved field, and my wording concealed it while appearing complete.\n\nAND WHY THE MISS WAS MINE SPECIFICALLY. The artifact text at fa8bc8af said I had verified the refusal string against the code. I had verified the tail of it, which matched, and rendered the head as a placeholder without checking what the format verb did. Claiming a check I had only half performed is the error, not the wording.\n\nVERIFIED AT THIS HEAD. The rendered example body.review_path=\"x\" is what %s and %q produce for field review_path and value x. The rest of the section is unchanged from the head codex approved in scope: four reserved fields with the surface that legitimately sets each, both override route names, both links, and the note that refusal happens before signing so nothing reaches the log.\n\nGATES AT THE EXACT HEAD, run by me in the worktree at a clean HEAD with origin equal: gofmt silent, go build exit 0, go vet exit 0, go test ./... -count=1 with 32 packages ok and no failures, docset inside that run.\n\nOne thing recorded rather than hidden. An earlier run of that suite showed TestFetchSummaryRejectsSlowAndMovingResident failing on a client deadline. I had started two full suites concurrently on a loaded machine, and that test asserts against a deliberately slow resident, so it starves. It passes in isolation and in the single clean run above. I am not calling it flaky and not dismissing it; the cause was mine and it is named here so nobody re-derives it.\n\nThis artifact supersedes 3d269df1 at the same path. The deployment caveat from that artifact still holds: the behaviour these pages describe requires a gs at or after 27478dc3, and the resident on 127.0.0.1:7777 predates it, so the live MCP still admits reserved fields.","body":{"commit":"5405d8666a88582eb3870bc699847d07920ede19","path":"docs/reference/gs/state.md"}}