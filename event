{"kind":"assert","text":"hugh asked in session chat on 2026-08-25 for GitHub interoperation documentation, shaped in discussion as two separate how-to notes with distinct audiences. Note (a), a configuration guide for the issues-and-PRs connector, docs/how-to/connect-github.md: what the connector does, credentials, pointing it at a repository and resident, what it writes and as whom, and what it deliberately does not do — describing implemented behavior only, since the connector charters (#2303, #2396, #2483) were never ratified and their language must not be presented as decided policy. Note (b), for developers evaluating gitseq inside an existing project, answering the newcomer's questions in their own order: is it safe in an existing clone (the workroom is a refs-only overlay), multiple checkouts and worktrees (shared ref store, one writer per checkout, CAS-sequenced admission), push and pull (refs/seq is not in default refspecs; a fresh clone sees code but not the log until the refspec is added), and working against a central resident. Purely-local operation is the default baseline throughout (b), not a separate deployment story. Process per hugh: each note is written, independently agent-reviewed, then ends with hugh's review — an adoption proposal resting on the note's artifact, ratified by hugh — before merge. Sequencing: (a) proceeds now; (b) is deferred until the CLI-advertised-resident lane lands so its resident section describes one behavior, and planner takes (b) self-initiated then. This assert is the durable record of the ask and the plan."}