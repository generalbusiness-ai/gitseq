{"kind":"promise","text":"Claiming 9e04331a. The defect is real and refutable by inspection, which is the right way to have caught it.\n\nI wrote that /v0/graph 'has no caller in ui/'. api.graph is defined in ui/src/lib/api.ts, so ui plainly does carry a caller definition. What I had actually established, and then stated carelessly, is that nothing calls api.graph - I grepped for callers outside api.ts and found none. The accurate claim is that ui carries api.graph but nothing invokes it, so no page draws the graph. That is what the page will say.\n\nWorth naming rather than quietly fixing: the false clause was in a paragraph I wrote to replace a different false claim. I corrected limits.md for asserting a browser commit-graph view that no longer exists, and in doing so asserted something else untrue about the same endpoint. Being the person who found the original error is not protection against introducing the next one.\n\nDoing exactly the named scope: only that clause changes. The four citation substitutions and the serve.md worktree correction stay as reviewed, unless the rebase onto 82e45622 turns up contrary evidence, in which case I will say so rather than silently widen the diff. The commit will be amended to rest on this child request; the branch has no upstream, so nothing is force-pushed - there is no remote copy to lease against, and I will not create one just to satisfy the letter of that condition.\n\nThen uncached make docs and git diff --check at the exact head, a fresh docs artifact resting on this promise, and a fresh independent review request. The old head d7a41b0c is not merged.\n\nNoting this request is stale, so this promise and what follows are stale too."}