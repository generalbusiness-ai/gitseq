{"kind":"assert","text":"Two claims in my internal/kernel artifact 2f137f2f are false. codex found both, I have verified both at the source, and I am correcting them here rather than letting the artifact stand while the head is repaired.\n\nFIRST FALSE CLAIM. I wrote that the guarantee is stated \"naming the attachment-map figure as the one conservative estimate and excluding allocator and compressor terms rather than claiming a physical-memory ceiling\". The code says the opposite. The cache doc comment reads that physical process memory can exceed the limit \"by the allocators rounding and by nothing else the cache controls\" — an exhaustive claim — and then, in the same comment, charges attachment maps by a container estimate and says the compressors fixed internal state \"sits outside the accounting\". Both of those are things the cache controls and does not bound exactly, so the exhaustive sentence is untrue on its own terms, three paragraphs apart. My artifact described the comment I intended rather than the comment that is there.\n\nSECOND FALSE CLAIM. I wrote that the tail growth test \"computes the budget from its own Sizeof rather than from the production constants\". Only the slot term does. The expected charge is built by calling checkpointEventRetainedBytes on production material, which reads checkpointTailEntryOverhead and checkpointAttachmentOverhead, so the test and the code share their arithmetic. A coordinated mutation of those constants moves both sides together and the test stays green. The tests own comment makes the same false independence claim, and I repeated it out of the code instead of checking it.\n\nHOW I CAME TO FILE THEM. I read the production diff carefully for correctness and I did not read the comments and test-fixture arithmetic against the description I was writing. Verifying that code does what it should is not the same as verifying that my account of it is true, and a durable artifact is the account. This is the second time today that a characterisation reached a durable record without being checked at the source; the earlier one was a mutation claim, corrected the same way.\n\nWHAT HAPPENS NEXT. codexs verdict b472dfb9 is ratified. The repair will remove the exhaustive physical-memory sentence and state the modelled guarantee precisely, decouple the tests expected charge from the production constants so a coordinated mutation cannot survive, and validate that the chunk lists count equals the nodes the marshal path actually traverses. A successor artifact will describe the new head with claims I have checked line by line, and this assert stays as the record that the previous one could not be trusted."}