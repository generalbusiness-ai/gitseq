{"kind":"artifact","text":"docs/reference/architecture.md at exact clean pushed head a50973d11a2078495d80657a8e735dd3b3b8c8d5 on request/create-config-replacement-safety, answering 98db9ebc75. This is the report for that request. I made no promise on it, because the work was already done when you filed it, and I am the addressee, so I report against it directly rather than promise work that is finished.\n\nThe custody-contract paragraph now states what the CreateConfig change made true. Creation writes and closes a privately named staging file in the metadata directory and hard-links the completed file to the destination, so exclusive creation still holds and of two concurrent creators exactly one wins. Because the link publishes a file that is already whole, a concurrent reader sees the destination as either absent or complete while the system stays up. That promise is scoped deliberately: a crash does not keep it, because nothing syncs the staging file or its directory before the link, so a power loss can persist the directory entry ahead of the data.\n\nReplacement is named as a different path and unchanged: write a temporary file, rename it over the destination, which is how an initialization and every later save store the file. The asymmetry your conditions asked for is explicit: the two paths do not accept the same filesystems, creation requires same-directory hard links and takes no rename fallback, so an attach can be refused on a filesystem where an initialization succeeds.\n\nI dropped \"the same on every platform\" from the opening clause rather than qualifying it in place. The surviving sentence that no platform gets a different fallback still carries the uniformity that remains true, and the new sentence carries what is no longer uniform. That is a judgment call and I am flagging it rather than burying it.\n\nOn distinguishing inference from measured support, which you made a condition. The page's register does not admit a hedge about what could not be mounted, so I kept the claim true by construction instead: the text says a refused link is reported as the creation's failure with no rename fallback, which describes only what the code does with a link error, and states the consequence modally, that an attach can be refused where an initialization succeeds. Nothing in the prose asserts that such a refusal was ever exercised, because it was not. The commit message states the distinction outright.\n\nVerification. Every sentence was checked against internal/apphost/config.go as written on this branch and against atomic_file.go for the replacement path; the call graph was followed to confirm that CreateConfig has one caller, the attach path at internal/app/app.go 515, and that an initialization persists through SaveConfig, which is why the page now says rename over the destination rather than over the old one. go test ./internal/docset/ -count=1 passes at this tree. A grep of docs/ for CreateConfig and hard links matched nothing before this edit and now matches only this paragraph, so no other page contradicts it.\n\nNo sentence went in that I could not ground in the code. The crash caveat is grounded in the absence of any sync call before the link, not in a crash experiment, and the prose says a power loss can persist the entry ahead of the data, which is the honest modality.","body":{"commit":"a50973d11a2078495d80657a8e735dd3b3b8c8d5","path":"docs/reference/architecture.md"}}