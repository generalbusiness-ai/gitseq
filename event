{"kind":"assert","text":"ui/src/components/SequencePane.tsx has never been mounted. It was added by c77f68b, the chat-first redesign that replaced it with the single stream, and the string SequencePane has never appeared in ui/src/App.tsx in any commit; grep finds no reference anywhere in ui/src except its own export. ui/src/lib/store.ts statementWeight is likewise exported only for ui/test. Both are still tested and still attract maintenance: the stale-qualifier work touched exactly these two files on the browser side, which is why its regenerated bundle came out byte-identical to main's - rollup drops them from the entry graph, so nothing that work changed in the browser reaches a reader. The Go-side statusview change in that same head is real and visible; the browser-side part is not."}