{"kind":"request","text":"claude: your gitstore finding becomes its own request — this one — and it is yours because you hold the two-command reproduction. (I anchor this on the live remote-link parent 48ec44db, the lane where the finding surfaced; your finding assert ed272e0b was staled by the signing merge before I could rest on it, which is precisely the procedural-staleness churn the audit's b4ddefcd fixes — so I restate the finding self-contained here rather than lose it.) WHAT YOU ESTABLISHED, preserved: internal/gitstore pins --git-dir and strips the GIT_CONFIG family in hermeticGitEnvironment, but leaves the routing family; GIT_COMMON_DIR redirects object-store reads (cat-file returns the attacker's object) even with --git-dir pinned, while refs did not redirect in your test. The config/routing asymmetry looks unintended, since hermeticGitEnvironment exists to close ambient injection. You were right not to take it inside the remote-link row — it is a kernel-boundary change that row does not authorize. THE ONE THING TO SETTLE FIRST is a threat-model question only hugh can answer: is the operator's process environment in the threat model, or trusted? Sequenced after your remote-link (3b06d237) and MCP-advert (467ad483) lanes; not urgent, and gated on hugh's ruling before any implementation.","body":{"conditions":"First, file an assert (resting on this request) putting the threat-model question to hugh: is the operator's process environment in-scope for ambient-injection defense, given hermeticGitEnvironment already strips the GIT_CONFIG family? Do not implement until hugh rules — a ratifier/operator confirmation, or an explicit decline that closes this row. If confirmed in-scope: extend hermeticGitEnvironment (internal/gitstore) to strip the routing family (GIT_DIR, GIT_WORK_TREE, GIT_COMMON_DIR, and any sibling that redirects repository resolution) alongside the config family, keeping the single sanitization model rather than adding a second resolver. Prove by regression that object reads (cat-file) AND ref resolution (rev-parse) through the gitstore path cannot be redirected by any of those variables; show mutation sensitivity by removing each strip and watching the named assertion fail, then restoring to a clean tree. Exact clean pushed head; gofmt, go build ./..., go vet ./..., git diff --check, go test ./... -count=1 green from watched runs; per-path artifacts resting on your promise; then independent review recording Architecture, Security and Simplification. Do not merge. If hugh rules the operator environment trusted and out of scope, report that ruling on this row and it closes with no code change.","to":"a5d35aa7e4799472a208663d7f024917442bd513929f9eccf450628ccf70095d"}}