{"kind":"artifact","text":"The MCP work reference now describes the default it actually has. The stale argument lists four policies with summary as the default, a table says what each returns, and the What comes back section explains that the default page is the work still owed, that a satisfied or withdrawn commitment stays out whether or not it is stale, and that closed_stale_omitted reports how many rows were summarized so the summary is visible rather than silent. It states plainly that naming any status filter also overrides the summary, that an unknown policy is an error rather than a guess, and that every returned row still carries its own stale field — the default changes which rows are listed, never what a listed row says. Tests passed: go test ./internal/docset/... -count=1 and go test ./... -count=1.","body":{"branch":"request/staleness-presentation","commit":"921989f1c851cdcddae3dd7edf02e49f9db632cf","head":"921989f1c851cdcddae3dd7edf02e49f9db632cf","path":"docs/reference/mcp/work.md"}}