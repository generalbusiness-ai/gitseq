{"kind":"report","text":"READY-FOR-REVIEW at exact clean head f5a933ffaafbd3d4fe488dd02af6eeadc1698db3, answering your three findings. (1) Step 5 now says supersede every live artifact at that exact path, however many are live, and a path whose first artifact this is needs no supersession; the same wording is carried in AGENTS.md, SKILL.md, docs/getting-started.md and docs/reference.md, and AGENTS.md line 44 now reads 'the live predecessors'. (2) All four files now state that paths match as exact strings with no normalising, prefix matching or globbing, require reusing the exact path an area already uses, allow a new granularity only where no live artifact covers the change, and forbid comma-joined pseudo-paths by name. (3) The frozen-record passage is replaced by a four-step migration: list the live dot artifacts with a gs status --json and jq selector, retire them in one pass, re-read the flares, and repair each flared document onto the live artifacts at the exact paths whose behaviour it describes. I ran that selector against this workroom and it returns live events — 35 of them at the time of filing, up from 33 earlier today because sidecars are still merging, which is itself an argument for doing the migration rather than deferring it. Your confirmed-correct case is preserved: a merge that changed nothing in an area publishes and supersedes nothing there. Gates: make vet, make test across all 16 packages, make build and git diff --check all pass; the diff touches only those four Markdown files. The branch was then carried onto current main at ebd44c6 with no conflict. Four cases the guidance still does not settle, which I would rather name than have you find: coverage is a human judgement, since string equality cannot decide that a live artifact at internal/workroom covers a change to internal/workroom/fold.go; overlapping live paths at different depths are handled but never shown as a worked example, and no rule converges an area on one granularity; a path whose file is renamed or deleted has no natural successor artifact yet still needs its predecessor retired; and migration step 4 has no mechanical stopping test.","body":{"head":"f5a933ffaafbd3d4fe488dd02af6eeadc1698db3"}}