{"kind":"artifact","text":"Closes the sole blocking finding of review report e21f2938 on head d52dfbfa: the head changed a projection contract without updating this page. docs/reference/architecture.md now records both changed contracts at the exact head, in the register the page already uses for layer contracts, and nothing else was touched. Layer 6, Projections and queries: a new paragraph states that the omission rules are part of the projection contract rather than presentation detail; that ordinary reasoning staleness qualifies a status and does not reopen a finished commitment, so a satisfied or withdrawn commitment is omitted from every default lane whether or not it is stale and the per-status counts carry it instead; that the staleness policy on a work query is a named value and not an absence, so an omitted policy means summary rather than include while explicit include, only and exclude each return what they always returned, and naming any lifecycle status also overrides the summary; that closed_stale_omitted reports what the summary left out and is omitted when zero; and that retirement and a superseded world stay individually visible and are counted apart from ordinary staleness in retired_artifacts and world_stale_artifacts, both always present. Layer 7, surfaces: the cmd/gitseq-mcp entry records that the work tool's stale enum admits summary, include, only and exclude, that summary is what a call naming no policy receives, and that the tool schema is the surface contract for the default while the selection it names belongs to the projection above. The change is additive, 21 lines, and the page's rests_on frontmatter is byte-for-byte identical to main a08c32f0. No kernel, storage, host-binding, fold or authority semantics changed, matching the reviewer's own conclusion. Tests passed: git diff --check clean, gofmt clean, go vet clean, go test ./... -count=1 exit 0, all 25 packages ok including internal/docset uncached.","body":{"branch":"request/staleness-presentation","commit":"ae8cc8a89cdd434b7026aa0cf5af8df9a49b3217","head":"ae8cc8a89cdd434b7026aa0cf5af8df9a49b3217","path":"docs/reference/architecture.md"}}