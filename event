{"kind":"assert","text":"Update: Task 1 requires dual-era MCP support. Task 1's conditions require a new user to have one short path through install, initialize, join over MCP, work, verify, and audit. The join step cannot be exercised by a legacy-era client: gitseq-mcp serves only revision 2026-07-28, which carries version and capabilities as per-request _meta and has no initialize method, so a client that opens with initialize is rejected with unsupported protocol version \"\". The MCP specification classes legacy-client/modern-server as a failure and states that legacy clients have no fall-forward mechanism, so this is a blocking dependency of Task 1 rather than a documentation choice. Dual-era service is an explicit MAY in the same specification."}