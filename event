{"kind":"report","text":"READY-FOR-REVIEW at exact clean head faac00510dfedada82e2ab4cc45a48a216893c86 on task/gs-batch-repair. This replaces the readiness claim at 1797a4d31431a0cacfc04b503c4c849aa70182b7. Conditions met: the invalid decoder.More check in readBatch is gone, replaced by a second Decode that must return io.EOF, so a valid array followed by anything but whitespace is refused before the workroom is opened and before the first append. TestBatchRefusesTrailingInputWithoutLanding feeds a stray ']', a stray '}', a delimiter on its own line, a second JSON value, an unterminated value, and plain malformed bytes; each must fail with a typed input error, print no report, and leave head and depth unchanged, and a final case proves trailing whitespace still lands normally. That test is mutation-sensitive: restored to the old decoder.More check, the three stray-delimiter cases fail. TestBatchRetryAfterPartialLandingReplaysPrefixAndLandsSuffix covers recovery from a partly landed chain - the second act names an actor not yet on the roster, so the first run lands only the prefix and stops; after the actor is admitted the same file replays the prefix under its idempotency key, resolves the intra-batch label to the event already minted, lands only the suffix, and the prefix statement appears exactly once. That test is mutation-sensitive too: dropping the per-act idempotency key makes the retry land both acts again. The one-workspace kernel path is untouched - one app.Open, one resident submitter and actor key, and every write still goes through kernel envelope and signature checks, the payload ceiling, the admission hook, dedup, sequencer signing, and compare-and-swap. All prior conditions stand: whole-input and label preflight, typed per-act outcomes, honest exit code, --server parity, and the four original TestBatch tests. docs/reference.md now says the array must be the whole input. Verified: make vet clean, make test green across all packages, go test -race ./cmd/gs/... green, git diff --check clean, worktree clean at the head. Also reproduced codex's exact case end to end with a built binary in an isolated workroom: a one-act array followed by an extra ']' on stdin now exits 1, prints no report, and leaves refs/seq head and depth unchanged, while the same array without the delimiter still lands. Deliberately not done: the branch is task/gs-batch-repair, not task/gs-batch, because task/gs-batch was 37 commits behind main; the original batch commit was cherry-picked onto current main so the head merges cleanly. Nothing is merged. gofmt reports spike/internal/nexus/session_privacy_test.go unformatted; that is pre-existing on main and out of scope here.","body":{"artifact":"git:sha1:5d2622748872b7e2dec3fe5c59e4be73a35e0bc8#git:sha1:cd202c7103d4e5637f1a95b8f6d48eeb34690b52","branch":"task/gs-batch-repair","head":"faac00510dfedada82e2ab4cc45a48a216893c86","status":"ready-for-review"}}