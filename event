{"kind":"artifact","text":"The committed embed is rebuilt from the repaired browser source, so the resident serves the authority gate rather than the build that predates it. Exact clean pushed head 8ad3c0c20b6d374b036f3e4b3dd86ef0ad4f8627 on request/decision-records-ui-recut; local and origin identical by commit SHA and tree hash 1f7e7907, checkout clean. Successor to 3aa6cd46.\n\nWHY THIS PATH IS REPUBLISHED RATHER THAN CARRIED. It genuinely changed. The authority repair at e6b46aae edits ui/src, so the bundle here is rebuilt: the hashed JS bundle is replaced and index.html re-points at it. That is not bookkeeping. The resident serves this committed directory, not ui/src, so a source-only fix would leave the running workroom still offering publish to a departed principal while every test over ui/src passed. The failure would be silent and would contradict the artifact describing the fix.\n\nWHAT I VERIFIED, at this exact head, rather than trusting the build. I ran make ui and then git status: zero entries. The embed is byte-reproduced from the committed source, so the bundle here is the one this source produces, and the head SHA is unchanged by the rebuild — had make ui altered anything the tree would have moved and this artifact would name a commit that no longer describes the build. make ui-check exited 0.\n\nGATES RUN BY ME AT THIS EXACT HEAD. make ui exit 0 with an empty git status; make ui-check exit 0; npm test --prefix ui 99 tests, 99 pass, 0 fail, 0 cancelled; go test ./... -count=1 exit 0, 33 packages, zero failures and zero cached; gofmt -l . empty; go build and go vet exit 0 with no output; git diff --check exit 0.","body":{"commit":"8ad3c0c20b6d374b036f3e4b3dd86ef0ad4f8627","path":"internal/service/uidist"}}