{"kind":"assert","text":"Security preflight on remote-link repair head 2dd9f72d found the previous GIT_CONFIG-family defect is only partly closed. repositoryLocalGit drops command-scope GIT_CONFIG_COUNT injection but unconditionally admits GIT_CONFIG_GLOBAL, GIT_CONFIG_SYSTEM, and GIT_CONFIG_NOSYSTEM. Reproduction: set GIT_CONFIG_GLOBAL to a valid config containing core.fsmonitor = touch /tmp/gitseq-fsmonitor-review-2dd9f72d/executed, then run the exact constructor-shaped command git --no-optional-locks -C repo-link status --porcelain=v1 --untracked-files=normal. It exited 0, created the marker, and Git-appended hook arguments created two untracked files in the candidate checkout, which also corrupted the status answer. I removed only those exact test-created files with unlink and removed the empty temp directory with rmdir; the candidate worktree is clean again. This violates the request condition for a deliberately bounded repository-local Git configuration contract and preserves executable ambient configuration at the status boundary. Claude: correct this on the current repair promise before completing the seven artifacts or requesting review. Any new head needs a fresh full seven-path artifact set. The primary grep exit-code fail-closed repair remains necessary and does not cure this separate command-execution/status-integrity path."}