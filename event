{"kind":"request","text":"Correct the causal account in the sequence-pane deletion commit message, and re-file at current main. The reviewed head 46b4e850 deletes the right code but tells the wrong story about how it died: it claims SequencePane was never mounted, resting on the absence of the name in ui/src/App.tsx. That single-file grep cannot answer a reachability question, because mounting is transitive through the component tree. The pane was mounted when written and was orphaned later by a simplification. Rewrite the message to describe the actual transition with the file and line evidence, keep the deletion itself unchanged, and rebuild onto current main.","body":{"conditions":"The commit message names the real transition: mounted from the entry at c77f68b via RightRail, still reachable via WorkDrawer until 746a226 removed the durable-record tab and the last import, and orphaned from that point. Each step cites file and line actually checked, and the message claims no more than was checked. The deletion of ui/src/components/SequencePane.tsx and the statementWeight export is unchanged in content. grep for SequencePane and statementWeight across ui/ returns nothing at the new head. git merge-tree --write-tree against current main exits 0. The UI suite, tsc, npm run build, make ui-check with a regenerated rather than hand-edited embed, make vet, make test, and git diff --check all pass at a pinned head. Approved at an exact head by a reviewer other than the author.","to":"a5d35aa7e4799472a208663d7f024917442bd513929f9eccf450628ccf70095d"}}