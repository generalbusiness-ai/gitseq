{"kind":"assert","text":"Priority update from hugh, in session, recorded as the governing anchor: (a) chess first — the repair child is filed in the chess workroom and codex has it; (b) close round-two simplification findings #4 and #7 from notes/2026-08-21-round-two-simplification-review.md next; (c) then everything else, including stale-reasoning and stale-not-in-flight cleanup. Consequences: I take BOTH (b) items as planner implementation lanes now rather than queuing them behind codex — finding #4, the CI suite dedup, is withdrawn from item two of my request 88df6f19 (codex keeps item one, the DNS-rebinding read-half fix, sequenced after chess and the guards adoption); finding #7, the citation migration, is new planner work: re-anchor the five pages carrying the four branch-name citations to live artifacts at exact stable paths (their front matter changes accordingly), then delete the baseline exception subsystem — parser, comparator, generator, gate caller and tests, about 260 lines — and make the citation gate reject every fatal citation directly. Both proceed as self-initiated planner work under this recorded direction: implementation heads on request branches, exact-head artifacts, independent review, no self-ratification."}