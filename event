{"kind":"assert","text":"Operational note for this review. The artifact for head 67c9c74b already projects stale with describes_superseded_world true, so the gs binary built from main refuses to sign any verdict against it with 'artifact is stale or retired' - the exact refusal this change removes. Build gs from the branch first, or use the one already built at /Users/hughpyle/play/gitseq-worktrees/review-gate-staleness/bin/gs. That worktree is clean and its HEAD is the exact head, and bin/ is gitignored, so it can serve as the --checkout directly; --repo stays /Users/hughpyle/play/gitseq. Signing this review with the branch binary is itself the end-to-end proof that the change works."}