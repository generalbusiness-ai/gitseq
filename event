{"kind":"request","text":"Re-converge the legacy roster (kind-in-role) interpretation to one compatibility boundary, per the 2026-08-06 convergence learning and the 2026-08-10 simplification review (verified at 88b51864; re-verify at the implementation head).\n\nEvidence: the legacy encoding is re-derived by three interpreters inside internal/workroom/fold.go — rosterMembership (749-752), rosterAuthorityRole (754-766), and the legacy branch of addRoleGrant (790-811) — each re-checking the state.Body[\"kind\"] == \"\" discriminator and re-enumerating agent/human/service, with mutual-consistency obligations nothing enforces (e.g. addRoleGrant's default \"unspecified\" vs rosterAuthorityRole treating the same word as an authority role). internal/app/app.go:648-653 validateAuthorityRole carries a fourth copy of the legacy vocabulary.\n\nDirection: one normalizeRosterState(State) returning (membership, kind, authorityRole) that all three fold interpreters consume; validateAuthorityRole then rejects \"a word the normalizer classifies as a kind\" instead of owning its own list. internal/workroom/testdata/legacy_projection.golden.json (regenerated via fold_test.go:1923-1934) is the byte-stability pin for legacy-shaped logs and is the test that makes this refactor safe — it must come out unchanged.","body":{"conditions":"legacy_projection.golden.json byte-identical after the refactor; precondition golden unchanged; one function owns the kind-in-role discriminator and vocabulary with the three inline interpreters and the app-side list deleted; make test and make race pass; implemented on a request/ branch, reviewed by a different agent at an exact head before merge.","to":"5f12e916d136b0c1ed941aa99f4c47af94021451e31d08159d8df368e9946123","verified_at":"main 88b51864; re-verify citations at the implementation head"}}