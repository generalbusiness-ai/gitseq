{"kind":"artifact","text":"Implementation report for d8b2b7a6 (staleness write-boundary fix, revived from 8f7d36fd) at exact clean pushed head b1caf4596769315d5aefb946591d8b7e8689fe15 on request/stale-bases-admissible: implementing commit 5d7a2c0f (Rests-On d8b2b7a6), a merge of main defbae75, and bump commit b1caf459 (same trailer). Condition 1: internal/app/admission.go refuseDeadBases now splits by class. A retired basis still refuses with the same message and the same explicit --allow-dead-basis / body.dead_basis_override escape. A merely-stale basis is admitted and the staleness is written onto the row as body.stale_bases, the same one-line note gs merge writes into a receipt (reviewguard.StalenessNote with describes-superseded-world derived from the projection); StaleBasesField joins ReservedBodyFields so callers cannot supply it, and the builder's own stamp is honoured at sequencing like dead_basis_override. Condition 2: internal/workroom/fold.go drops the staleness precondition from the reassign_if_unclaimed guard; the claim guard (zero admitted direct promises, zero direct completions) and the retired-request refusal are untouched. Condition 3: docs/concepts/staleness.md, docs/reference/gs/state.md, docs/reference/mcp/state.md (new \"Retired bases and stale bases\" section, five reserved fields), both reassign_if_unclaimed references, docs/reference/architecture.md, SKILL.md, plus the gs --allow-dead-basis usage and the MCP state and reassign tool descriptions, now state the new rule; no doc names the old one. Condition 4, mutation-sensitive tests: retired-basis-still-refused fails TestAdmissionRefusesDeadBasesUntilTheEscapeIsAskedFor when retired falls into the stale bucket; TestAdmissionAdmitsAStaleBasisAndRecordsTheStaleness fails if stale refuses again or the stamp is removed; TestAdmissionRejectsCallerSuppliedStaleBases and TestStateToolRefusesReservedBodyFieldSpoofing (hard-coded field name) fail if stale_bases leaves ReservedBodyFields; TestReassignIfUnclaimedActsOnAStaleUnclaimedRequest and its application twin fail if the staleness precondition returns, and their stale-and-promised subtests fail if the claim guard is removed; two cmd/gs tests updated to the new rule. Condition 5: gofmt -l empty, go build, go vet, git diff --check clean, go test -count=1 -timeout 30m ./... run alone: 35 ok, no FAIL. ProfileVersion: fe36ff06 took workroom-fold@16, so this head moves to workroom-fold@17 (schema.go, the app_test literals, the architecture.md sentence explaining the two steps); the resident must be redeployed on the merged binary before gs is rebuilt. Two adversarial read-only passes preceded this: the first found the unpinned reserved-field spoof, two leftover \"fresh\" statements and the @16 collision, all fixed here. Known recording gap, not a blocker: a guarded replacement body and a locally-degraded signing path do not pass refuseDeadBases at signing, so such an act resting on a stale basis lands without stale_bases. This artifact itself rests on a promise made stale by my own merge of fd8a12ef (assert 3d37229b); planner confirmed carrying on under the recorded override. Co-signed artifacts follow at internal/workroom, cmd/gs, cmd/gitseq-mcp, SKILL.md and the six docs.","body":{"branch":"request/stale-bases-admissible","commit":"b1caf4596769315d5aefb946591d8b7e8689fe15","dead_basis_override":"true","path":"internal/app"}}