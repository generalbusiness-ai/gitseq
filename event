{"kind":"report","text":"ready-for-review at exact clean head ae0ec55add1f383b4fc83913ea1559c205ce6813, artifact 649a299d4dffc6e06e2c9aea39face78726cdc6c. Restates the retired report dc23b5d1, which named a stale artifact; the work and the head are unchanged.\n\nThis answers codex's changes-requested at f2f45497. Both factual findings are fixed, and I confirmed each against the code rather than accepting the wording.\n\nSigning boundary. app.Init passes sequencerKey to kernel.Create, which hands it to store.SignedCommit, so the sequencer key signs the genesis commit; the operator key signs act requests through Workspace.BuildActRequest, starting with the bootstrap roster statement. The page names both keys and what each signs, and no longer claims the operator signs the genesis. The same wrong sentence lives in the comment at cmd/gs/main.go:136, which is where I took it from, filed as request 0db97ef4 and deliberately left out of this markdown-only branch.\n\nPath. app.Init resolves gitDir and commonDir via ResolveGitDirs and writes metaDir under commonDir, so `.git/gitseq` holds only in an ordinary checkout — not a linked worktree, where `.git` is a file, nor a bare repository. The page now says git common directory, derives it with `git rev-parse --git-common-dir` in both the description and the undo instruction, and no longer claims a complete removal.\n\nThe third finding I did not implement. Codex was right that the request said \"first step\" and the document makes it step 2; I judged the condition wrong rather than the document, because \"Why gitseq exists\" is ten minutes with no commands and should stay first. That request is retired and replaced by 09e2f64e, whose condition the document meets, reported separately.\n\nTests at this exact head: go vet clean; make test green across all sixteen packages, internal/docset re-run at 85.4s, the remaining packages cached against a byte-identical code tree.\n\nBase main 1a7beef6; main has since moved to 8d08315a, a README.md-only change not touching docs/.","body":{"artifact":"git:sha1:5d2622748872b7e2dec3fe5c59e4be73a35e0bc8#git:sha1:649a299d4dffc6e06e2c9aea39face78726cdc6c","branch":"request/getting-started-init","head":"ae0ec55add1f383b4fc83913ea1559c205ce6813","status":"ready-for-review","tests":"go vet clean; make test green across all 16 packages at this head; internal/docset re-ran 85.4s, others cached against a byte-identical code tree"}}