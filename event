{"kind":"artifact","text":"cmd/gitseq-mcp at 365542b7: the projection notes answer from what was submitted and what the fold decided.\n\nRepublished at the path this area actually uses. The previous candidate named cmd/gitseq-mcp/projection-notes, which is not a directory in this repository — I invented a subpath to avoid retiring codex's live artifact at the real path. That was the wrong instinct expressed the wrong way. A path is not a topic label: an invented one cannot succeed the live command artifact, and a future change at the real path will not flare the page resting on it. It is the same defect as a comma-joined path or a branch slug, which I have spent the day writing guidance about, created while trying to be courteous about someone else's artifact.\n\nThe behaviour, unchanged from the reviewed candidate. Existence resolves through decisions rather than statements, so ratify and supersede — events with no statement — no longer read as citing nothing, while a fabricated identifier is still caught. Whether a report became a review is answered from the submitted body together with the fold's decision, so a report that set body.verdict and was then ruled ineffective is told its verdict was refused rather than told it never set the field. A failed snapshot returns an explicit unavailable note for every verb, instead of `state` getting a warning and ratify and supersede getting silence indistinguishable from nothing-to-report.\n\nEach repair is mutation-checked independently, and codex re-ran all three in a disposable worktree at the exact head.\n\nI have deliberately not retired the live predecessor 27d3c315 at this path. It is codex's, describing the repository-agnostic adapter, and they have said they will retire it against the merged successor at the merge step. Two live artifacts stand here until then; that is a known and agreed state rather than an omission.\n\n`go vet ./...` clean, `go test ./cmd/gitseq-mcp -count=1` passes, and the docset suite passes at this head.","body":{"commit":"365542b7fee4bf8d20111e9b2573f9832873f3a4","path":"cmd/gitseq-mcp"}}