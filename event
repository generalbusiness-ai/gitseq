{"kind":"report","text":"CHANGES-REQUESTED for exact head 8160d8a5c6f39eb7e2c9bf1309da064cdce0db5d. The implementation and authority-path repair pass, but the revised trust-boundary paragraph still overstates and misplaces verification. It says a `/v0/act` event is “accepted by the fold” because “the fold checks that a signature is valid.” The workroom fold receives already-decoded `workroom.Record` values and never checks a signature; `kernel.Load` verifies event/sequencer signatures before projection. Moreover, a correctly custodially signed event can legitimately fold `ineffective` or `disputed`, so “accepted by the fold” reads as force the signature does not confer. State the two layers separately: possession of the session makes the custodian produce a genuinely actor-signed durable event which the kernel can authenticate and retain; the profile fold then judges its semantic force independently. The nearby description still calls the newly random handle “one-way”; “opaque minted handle” is more exact because there is no directional derivation now. Code/lifecycle checks pass: random 128-bit per-lease handles remained distinct across 1,000 simultaneous live leases; renewal preserved the handle; departure and expiry each caused re-creation under a new handle; the live change stream exposed the old/new handles in order and no raw identifier; a published handle received HTTP 400 at `/v0/act`; the owner received HTTP 200 and appended the sole post-genesis act. Existing adapted tests retain their assertions, the new service-level authority test is meaningful, and the code correctly preserves owner behavior. make vet/test/build, `go test -race ./...` from spike, diff --check, exact-head cleanliness, and the repair trailer all pass. Temporary service/repository removed. No merge performed.","body":{"finding_count":"1","head":"8160d8a5c6f39eb7e2c9bf1309da064cdce0db5d","verdict":"changes-requested"}}