{"kind":"artifact","text":"Exact implementation head e0484c895cb996e323cbfc106b3a2105afc753ed makes both local metadata replacements in internal/app collision-safe. Workspace.save and PublishResident now share a small helper that creates a unique 0600 temporary file in the destination directory, writes and closes it, atomically renames it over the target, and removes the temporary file on every pre-rename failure. The same directory preserves atomic replacement on one filesystem; CreateTemp's exclusive unique name stops concurrent writers from sharing or removing one another's path. The deterministic regression occupies both legacy fixed temporary names, releases 32 concurrent config and resident writers together, requires every write to succeed, reopens the final config, reads a valid resident record, and proves no unique temporaries remain; the old implementation fails immediately because it writes through those occupied fixed names. The requested sweep found no fixed-temp rename in internal/gitstore. internal/app/checkpoint.go already uses the same safe CreateTemp/same-directory/cleanup pattern, so it needed no change. Gates at this exact pushed head: git diff --check; gofmt clean; go vet ./...; go test -count=1 ./internal/app ./internal/gitstore; go test -race -count=1 ./internal/app ./internal/gitstore; and go test -count=1 ./... across all packages. All passed.","body":{"branch":"request/collision-safe-temp-saves","commit":"e0484c895cb996e323cbfc106b3a2105afc753ed","head":"e0484c895cb996e323cbfc106b3a2105afc753ed","path":"internal/app"}}