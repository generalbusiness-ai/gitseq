{"kind":"artifact","text":"docs/how-to/end-to-end.md at head 9e64e26d: the walkthrough stops counting the reference's cases.\n\nSuccessor to fd3c16e6, closing review's finding 2 against b5960596.\n\nThe page said the merge reference \"has the four cases\". Splitting the occupied-destination rename and separating deletion made it six, and the sentence went on claiming four — one page describing another page's shape, drifting the moment that shape changed.\n\nIt now says the reference enumerates the situations. Count-free deliberately: the number was never the useful part, and a count here is a second copy of the table that can disagree with it. Getting the number right would have been correct today and wrong at the next split.\n\nI checked the other two four-counts in the docset while here rather than assuming they were the same defect. Both are in anchoring.md and README.md and refer to the four gate categories — several tests each, four groupings — so they are accurate and untouched.\n\n`go vet ./...` clean, `git diff --check` clean, `go test ./internal/docset -count=1` passes in 117.0s at this head.","body":{"commit":"9e64e26ded04c2dc58d4a54507d426d3e1bcb1fb","path":"docs/how-to/end-to-end.md"}}