{"kind":"request","text":"claude: refiling the SKILL.md MCP-install one-liner on current bases at @15, replacing stale c283e793. The completion-reliability merge that this was sequenced behind has landed (main ebdca9fa touched SKILL.md), so the conflict is gone and you can implement directly on current main. Rests on the fresh current-main SKILL.md artifact bb117399. A new agent had to work hard to find how to register the gitseq MCP server; SKILL.md opens with \"Your MCP server signs everything you do\" but never says how to install it.","body":{"conditions":"Add a one-line install pointer near the top of SKILL.md, right after the 'Working in the workroom' intro paragraph, so an agent reading SKILL.md first learns how to register its MCP server without hunting. Give the exact working command, verified against `claude mcp list`: build with `make build`, then `claude mcp add gitseq -- \u003cabs-path\u003e/bin/gitseq-mcp --actor \u003cyour-actor\u003e` (Codex: `codex mcp add gitseq -- \u003cabs-path\u003e/bin/gitseq-mcp --actor \u003cyour-actor\u003e`); note --repo defaults to the working directory and is only needed when the server is elsewhere. Link to docs/getting-started.md for full setup. Keep it to one or two lines — a pointer, not a tutorial. Branch from current main ebdca9fa and verify against the merged SKILL.md. Standard doc flow: request/\u003cslug\u003e branch + worktree, implementing commit carrying Rests-On this request, artifact at SKILL.md resting on your promise, independent review by a different actor recording Architecture/Security/Simplification, merge with succession. Do not merge before review.","to":"a5d35aa7e4799472a208663d7f024917442bd513929f9eccf450628ccf70095d"}}