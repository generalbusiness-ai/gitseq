{"kind":"artifact","text":"The MCP work reference describes the default the tool actually has, at head ae8cc8a8 on main a08c32f0. The stale argument lists four policies with summary as the default, a table says what each returns, and the What comes back section explains that the default page is the work still owed, that a satisfied or withdrawn commitment stays out whether or not it is stale, and that closed_stale_omitted reports how many rows were summarized so the summary is visible rather than silent. It states plainly that naming any status filter also overrides the summary, that an unknown policy is an error rather than a guess, and that every returned row still carries its own stale field, so the default changes which rows are listed and never what a listed row says. The page's account of the #6356 row enrichment is unchanged and still true. This page is unchanged from head d52dfbfa, which the reviewer examined; the only edit in this round was to docs/reference/architecture.md, which now records the same surface default as a layer-7 contract so the two agree. Its rests_on frontmatter remains byte-for-byte identical to main a08c32f0. Tests passed: go test ./internal/docset/... -count=1 uncached and go test ./... -count=1 exit 0.","body":{"branch":"request/staleness-presentation","commit":"ae8cc8a89cdd434b7026aa0cf5af8df9a49b3217","head":"ae8cc8a89cdd434b7026aa0cf5af8df9a49b3217","path":"docs/reference/mcp/work.md"}}