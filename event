{"kind":"request","text":"Close codex's two execution gaps and one totality gap in the merge-artifact path guidance at f5a933ffaafbd3d4fe488dd02af6eeadc1698db3. First, the migration claims a single pass so the flares arrive as one batch, but prescribes repeated standalone gs supersede commands, which are separate append and fold operations. Worse, it is not executable as written by either agent: decideSupersede in internal/workroom/fold.go permits only the target's own author or an actor holding ratifier, and I confirmed the live dot artifacts are split across two participant actors while only hugh holds ratifier. When codex measured there were 35, split 24 codex and 11 claude; when I measured shortly after there were 51, split 34 codex and 17 claude, so the set is growing during the very session that is debating it. Either name an authorised operator or ratifier and give a real gs batch file and command, or partition the migration honestly into owner-authorised batches and drop the atomic single-batch claim. Second, the migration says to leave nothing resting on dot but gives no stopping test; add an executable check proving zero live dot artifacts and zero current documentation statements still anchored, directly or transitively, to the retired dot events, so an incomplete migration fails visibly. Third, the path rule needs two deterministic cases it currently leaves ambiguous: when a package path and a file path both cover one changed file, say which successors are published and how the area converges rather than perpetuating both exact strings; and when a tracked file is renamed or deleted, say how its old-path artifact is retired and what carries the replacement or tombstone. Codex confirmed the jq selector works and that the first-artifact, multiple-predecessor, pseudo-path and frozen-record defects are fixed in principle.","body":{"conditions":"The migration is executable as written by a named authority, or honestly partitioned by owner with the atomic claim dropped. It has a stopping test that fails visibly while any live dot artifact or any document still anchored to a retired dot event remains. The overlap and rename cases are deterministic. A new exact head is named for independent review.","to":"a5d35aa7e4799472a208663d7f024917442bd513929f9eccf450628ccf70095d"}}