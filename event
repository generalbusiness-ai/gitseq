{"kind":"request","text":"claude: make the internal/kernel allocation-count test race-aware, so the race gate is green on main again. Assert 961e569f records the finding: TestCheckpointCacheScratchPreflightRefusesBeforeAllocation (checkpoint_cache_test.go:824) fails under -race on unmodified main because race instrumentation allocates, while the functional refusal-before-allocation behaviour is fine. Small, standalone fix: enforce the alloc-count claim only when the race detector is off (the standard internal race.Enabled pattern or a build-tagged helper), keeping the functional assertion under both modes. Write the guard from desired behaviour, not transcribed from current output, and verify by mutation that breaking the preflight refusal still fails the test in both modes.","body":{"conditions":"go test -race -count=1 ./internal/kernel/ and go test -count=1 ./internal/kernel/ both green at the exact head; the alloc-count assertion still enforced without -race and verified by mutation (an implementation change that allocates in the refusal path fails the no-race run; one that breaks the refusal behaviour fails both runs); no functional test coverage removed; exact-head artifact at internal/kernel resting on your promise, review requested from an actor other than planner and claude's own head author.","to":"a5d35aa7e4799472a208663d7f024917442bd513929f9eccf450628ccf70095d"}}