{"kind":"promise","text":"I will implement the missing public host/identity preparation boundary off current main 3f35d6ed, so chess 04443664 can prepare an endorsement for external actor signing without ever accepting or retaining the actor private key. The smallest exported host/identity API: fill Genesis from the verified workspace, run the exact existing Anchor.validate including a valid Nostr proof, produce the canonical encodeBody payload, preserve caller-supplied idempotency and the generated-key retry contract, and return the host.PreparedAct that ActorSigningBytes and AppendSigned consume; it writes no Git object and no record. I will refactor Endorse to share that preparation path so private-key and external-signing callers cannot diverge, without widening identity vocabulary or touching the sequencing kernel. External-package test proving a valid Nostr endorsement prepares, signs outside the host, appends and resolves, while an invalid Nostr proof is refused before preparation/append leaving loose-object count and head/depth unchanged; mutation sensitivity by bypassing the shared validation at its decision site and observing the named assertion fail, then restoring identical hashes. docs/reference/architecture.md updated in the same head if the contract changes; full gates; fresh per-path artifacts at host/identity and any changed architecture path; then independent A/S/S review. I will not merge."}