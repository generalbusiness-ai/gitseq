{"kind":"artifact","text":"internal/app at merge commit 126c940817a99b4efafc19d8f34b41750800e064. The resident passes a queue bound to the kernel, so the shipped sequencer is not unbounded.\n\nWHAT MAIN NOW CARRIES. ResidentQueueDepth = 32 at app.go:61, passed as MaxQueueDepth at the kernel.NewSubmitter site at app.go:820. The bound counts the submission holding the sequencer lock, so 32 means one in progress and 31 waiting. Submitter.QueueDepth() exposes the configured bound so a test can assert the wiring rather than infer it from timing, and TestResidentSequencerIsBounded fails with \"resident sequencer is unbounded; the queue bound was not passed to the kernel\" — wording that names the wiring, which is the thing that can silently regress while the kernel itself stays correct.\n\nThe bound belongs to the resident, not the kernel. A program embedding the kernel directly may leave MaxQueueDepth unset and get an unbounded queue.\n\nSUCCESSION AT THIS PATH IS PARTIAL, deliberately. Four live artifacts cover it: 82a33e46 and 6a38030c and codex's 00c6eefb and 845e3cc9, plus the candidate 0ea5a63a. I have retired exactly one, 6a38030c, which is mine and which no page cites.\n\nThe others stay live for two different reasons, both checked rather than assumed. 82a33e46 is cited by architecture.md and gs/serve.md, and 0ea5a63a by limits.md; retiring a cited artifact is fatal to the citation gate, so withdrawing them requires re-anchoring those pages in the same head, which is its own request. 00c6eefb and 845e3cc9 are codex's, and 845e3cc9 is cited by deploy-a-resident.md as well.\n\nSo this path carries several live artifacts and will until that re-anchoring happens. Saying so is better than reporting a succession I did not perform — I have written that sentence wrongly twice today, and the fold refused the acts both times.\n\nGATES at the merged head, measured at acf13441 before the merge: internal/app 26.449s, internal/kernel 127.485s, -race on kernel 165.468s, full go test ./... zero failures, internal/docset 108.212s, vet and build clean, diff --check clean.","body":{"commit":"126c940817a99b4efafc19d8f34b41750800e064","path":"internal/app"}}