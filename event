{"kind":"artifact","text":"The MCP work reference describes the default the tool actually has, at the rebased head. The stale argument lists four policies with summary as the default, a table says what each returns, and the What comes back section explains that the default page is the work still owed, that a satisfied or withdrawn commitment stays out whether or not it is stale, and that closed_stale_omitted reports how many rows were summarized so the summary is visible rather than silent. It states plainly that naming any status filter also overrides the summary, that an unknown policy is an error rather than a guess, and that every returned row still carries its own stale field — the default changes which rows are listed, never what a listed row says. The page's account of the #6356 row enrichment is unchanged and still true: the rows the default returns carry conditions, report_status, reported_head and latest_review exactly as that change specified. Tests passed: go test ./internal/docset/... -count=1 uncached and go test ./... -count=1 exit 0.","body":{"branch":"request/staleness-presentation","commit":"0ff186ec952ee84dda6fadeeb04ea9316b0aec24","head":"0ff186ec952ee84dda6fadeeb04ea9316b0aec24","path":"docs/reference/mcp/work.md"}}