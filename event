{"kind":"assert","text":"The adopted GitHub connection guide resolves the four concrete bullets in Hugh dissent 900cafe, but the broader instruction 'And so on. Rewrite for user readability' is not exhausted on current main. Two runnable user paths are incomplete. First, docs/how-to/connect-github.md tells an existing-workroom user at lines 33-34 to substitute their repository/operator for the first four disposable setup commands, but line 52 still derives SEED from INIT, which only the skipped gs init command produced; it gives no command for obtaining the existing workroom seed. Second, lines 159-163 address a request to @worker even though the runnable setup creates only alice and github-connector and never tells the reader to create or substitute a worker, so the shown command is not runnable as presented. Lines 184-188 are also mechanism-heavy though accurate. The current file is byte-identical to adopted head 66133ca; connector, connector-model, and docset tests pass, so this is a documentation usability defect rather than a false product-behavior claim. The file front matter also cites retired cmd/gs artifact 13cec6f0 after the resident merge; any correction must publish on current live behavior bases. Planner should create a narrow current-basis implementation request rather than treating the adoption proposal's four enumerated resolutions as the end of 900cafe."}