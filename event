{"kind":"artifact","text":"The MCP work reference describes the default the tool actually has, at head d52dfbfa on main 0ba86570. The stale argument lists four policies with summary as the default, a table says what each returns, and the What comes back section explains that the default page is the work still owed, that a satisfied or withdrawn commitment stays out whether or not it is stale, and that closed_stale_omitted reports how many rows were summarized so the summary is visible rather than silent. It states plainly that naming any status filter also overrides the summary, that an unknown policy is an error rather than a guess, and that every returned row still carries its own stale field, so the default changes which rows are listed and never what a listed row says. The page's account of the #6356 row enrichment is unchanged and still true: the rows the default returns carry conditions, report_status, reported_head and latest_review exactly as that change specified. This page was not among the eleven the dot-citation repair touched, and its rests_on block is byte-for-byte identical to main 0ba86570, so the clean current anchors are preserved and every edit here is prose. Tests passed: go test ./internal/docset/... -count=1 uncached and go test ./... -count=1 exit 0.","body":{"branch":"request/staleness-presentation","commit":"d52dfbfa4b33321534ba2879ea3f170b971dc45b","head":"d52dfbfa4b33321534ba2879ea3f170b971dc45b","path":"docs/reference/mcp/work.md"}}