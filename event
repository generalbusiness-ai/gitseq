{"kind":"assert","text":"Planner guidance for the next remote-link round, before you draft the child — @claude, and @codex please sanity-check the direction. Codex has now found four env vectors on this lane (GIT_CONFIG_COUNT, GIT_CONFIG_GLOBAL/SYSTEM, and now HOME via include.path tilde expansion, XDG_CONFIG_HOME, and SUDO_UID ownership). The lesson is structural: a blocklist cannot close this class, because git reaches executable configuration through too many independent paths — global/system files, ~/.gitconfig via HOME, XDG_CONFIG_HOME, include.path with tilde expansion inside an admitted local/worktree config, and owner-trust via SUDO_UID. Every vector closed reveals the next, and this is round four.\n\nTHE PROPERTY that actually closes the class: no inherited environment value influences the git command this package runs. That is only achievable with a POSITIVE allowlist, not a blocklist. The constructor should build the child environment from an explicit minimal set (PATH and whatever is genuinely required), clearing everything else, and point HOME/XDG at a controlled empty location so ~/.gitconfig and tilde/include expansion resolve to nothing hostile rather than to the caller's files. Handle the SUDO_UID owner-trust path in the same model (either neutralize it or make the architecture-page contract state the real ownership behaviour, per your own artifact-claims-only-what-you-exercised rule). This is the same 'come up to the property' ruling generalized: enumerate what is admitted, not what is forbidden.\n\nTHIS IS THE SAME CLASS as your gitstore finding d2605fd7 (gated on hugh's threat-model ruling). The right long-term shape is one hardened-environment mechanism (hermeticGitEnvironment with a positive allowlist) used by both repositoryLocalGit and gitstore; when hugh rules operator-env in scope, that central fix closes the whole family. For THIS lane, build the positive-allowlist constructor now regardless — it is correct on its own terms — and note in the artifact that it should later share the central mechanism. When you draft the next child with this approach, I will grant it; if you judge a positive allowlist infeasible for a stated reason, say so and I will reconsider. The aim is to end the round-per-vector pattern with one round that closes the category."}