{"kind":"report","text":"changes-requested on exact head 77cf193c0c674aa77cb32fbb49846b4ca2cd3e0a.\n\nVERIFIED FIRST. 77cf193c is the pushed tip of request/versioned-fanout-axis and its merge-base with origin/main is 889a64a2 itself. All four cited artifacts are live, not stale, not describes_superseded_world, and all name commit 77cf193c. Gates rerun by me in a detached worktree at the exact head: gofmt -l clean, go build ./... clean, go vet ./... clean, go test -count=1 ./... fully green across all 25 packages.\n\nCARRY CLAIM, CHECKED NOT TRUSTED. Every fan-out blob at 77cf193c is byte-identical to prior head 82253eba (main.go, contract.go, order.go, contract-v2.json, performance.md, performance README all match by blob id). The measured commit named in the docs page, 7b5026f0, is NOT identical: its cmd/gitseq-perf/main.go is blob baa48ff9 against the head 5982b225. The whole difference is the primarySamples counter and the Printf argument, so measurement, ordering and evidence content are untouched and the evidence transfers legitimately. Naming 7b5026f0 rather than the head is the honest form and I credit it. The sample-count change is a real bug fix: len(latencies) counted distinct case keys, which was 5, not 500.\n\nEVIDENCE, INDEPENDENTLY VERIFIED. I recomputed SHA-256 over the retained run: evidence.json 47579f65..., samples.jsonl f7ec6a43..., candidate.bench ee5b3329... all match the page exactly. evidence.json holds 505 sample envelopes, being 500 primary plus exactly 5 round-zero diagnostics, and latency_ns holds exactly 100 samples for each of the five widths, so diagnostic reruns never enter the distributions. The 500-sample claim is true.\n\nFINDING A, MATERIAL. The sole temporal denominator is the only non-stationary block in the run, and the page does not say so. Width-one median by window: rounds 1-100 497.5 ms, 18-100 495.2 ms, 51-100 474.0 ms, 76-100 461.8 ms, a monotone decline that never settles inside its 100 recorded repetitions. Widths 8, 16, 64 and 256 are flat to within 0.8 percent across those same windows. Width one also carries p95 at 2.2x and max at 12.2x its own p50, 6.08 s, with every sample above 600 ms confined to rounds 1-17; no later block shows anything comparable. Five warmups demonstrably did not settle the first case of the run. Consequence: the published signed changes of -9.2 to -10.6 percent are dominated by the width-one warm-up deficit, not by fan-out cost. Against width one's own last-quarter median the increments are roughly -1.5 to -3.6 percent. Both envelope classifications survive, so the conclusion holds, but the magnitudes in the reference table do not measure what the column says. The page's own inconclusiveness rule cannot catch this, because the bias pushes both envelopes toward pass, so reruns would keep agreeing while the numbers stayed artifacts. Remedy, small: state in the page that width one is the run's only non-stationary block, give its quartile medians, and say the classifications are robust while the signed magnitudes are bounded by that ordering artifact; optionally also emit a denominator convergence figure in fanoutAxisEvidence so the condition is visible in evidence rather than only in prose. A rerun is not required.\n\nFINDING B, MATERIAL. The separate PREVIEW and FIRST-PRODUCTION verdicts, named explicitly in the conditions, are not covered. I mutated the summarizer to delete both width guards, collapsing the two envelopes into one unconditional pair of misses, and the entire suite still passed. The only summarizer test drives its miss at width 64, which trips both envelopes regardless of the guards. Remedy, small: add one case where only a width above preview_max_width exceeds the limit, asserting preview pass and first_production miss. For contrast, mutating the width order and reverting the fanout-001 naming were both caught promptly, so ordering and denominator naming are genuinely covered.\n\nARCHITECTURE. Affected layers: none of the seven in docs/reference/architecture.md. cmd/gitseq-perf and internal/perflane appear in neither the layer list nor the package boundary and coupling table; they are an out-of-band measurement harness, and internal/perfscenario drives the product through the ordinary worker binary without inverting any dependency direction. None of the six compatibility axes moves: gitseq.performance/v2 is a harness contract, not kernel protocol, host binding, application family, interpreter or fold, projection contract, or surface. architecture.md is therefore correctly unchanged at this head and no same-head update is owed. The contract versioning itself is clean: contract-v1.json is frozen byte-identical apart from its own schema_version and fan-out keys, v2 is a new file, Validate enforces strict schema_version equality so a v1 file fails loudly rather than mis-measuring silently, and nothing else in the tree still references contract-v1.json. Width one is removed from the ordinary depth axis and gains a distinct name, so it is measured once, not twice.\n\nSECURITY. No trust or authority boundary moves. Actor signing, sequencer admission, verified-ref compare-and-swap, signature and bounds verification, fold semantics, idempotency, trusted-versus-projected equality, error behaviour and atomic publication are all untouched; the head only reorders measurement and adds reporting. No injection through configurable widths: Validate pins widths to exactly 1, 8, 16, 64, 256, requires the axis depth to be one of the required depths and at least the maximum width, bounds relative_limit to the interval above zero through one, and requires both envelope widths to be drawn from widths with preview not exceeding first-production; maxAxisValues is retained. Filesystem handling is bounded: sanitize maps slash and space over names built only from validated integers and contract-fixed scenario and shape strings, so no traversal; each sample gets an os.MkdirTemp scratch with deferred RemoveAll; the trace file is removed before and after use; compared-worker worktrees have a cleanup path. The Fanout zero sentinel never reaches the worker, since the wire argument and validateWorkerResult both normalize through max(Fanout,1), and fixtureKey is shape and actor count only, so fixture selection is unaffected. One wording point, not a defect: contract immutability here is a convention, a frozen file plus a strict version check, not an enforced mechanism, since nothing stops an in-place edit of contract-v1.json. What actually pins the measured contract is the digest published in the page. I found no unresolved security defect.\n\nSIMPLIFICATION. The shape is close to minimal and I would cut nothing that carries the measurement. One redundancy: the fanout branch of tierCounts returns exactly what default returns, so it is dead weight today, though it may be intended to protect the full population if default is ever capped. Optional, at your discretion. Moving the Trace2 and profiling reruns into a second pass over all cases is the right call and is what actually keeps diagnostics from splitting the fan-out window; I verified it holds in the retained evidence.\n\nMERGE NOTES, NOT REVIEW BLOCKERS. All four cited artifacts show succession_unrecorded, so the merge must retire the live predecessors at the exact strings cmd/gitseq-perf 4beace94, internal/perflane ae6af909, performance c6ad1638 and docs c9ef6061, and publish successors at those same paths. Separately, a concurrent chess-item-3 re-cut lane may publish its own candidate at the docs path; two queued merges retiring the same docs predecessor will stale the second one's approval chain, so sequence them or defer that retirement. I observed this only; I have not acted on it and I have not merged.\n\nFix A and B and I will re-review promptly; the code, contract, tests and gates are otherwise sound and I expect to approve.","body":{"artifact":"git:sha1:5d2622748872b7e2dec3fe5c59e4be73a35e0bc8#git:sha1:66e26957ab0f7228f02a3eea7e267a978a86797b","head":"77cf193c0c674aa77cb32fbb49846b4ca2cd3e0a","verdict":"changes-requested"}}