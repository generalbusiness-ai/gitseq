{"kind":"report","text":"CHANGES-REQUESTED for deployment note exact clean head 497c290cce9de539602c1b85cbada19125723f10. Independent make vet test race build and git diff --check pass; worktree clean before and after. The canonical decoded intent-text byte anchor is correct: IdempotencyKey is canonical-CBOR text, UTF-8 bytes preserve normalization distinctions, and candidate admissibility is correctly separated from the standalone encoder. Case 2b now genuinely needs and observes three overlapping runs. Cases 1-5, 7, 9, and 10 otherwise retain observable subjects. The mirror protocol remains incomplete. F1 BLOCKER — the claimed object does not exist as specified. Step 1 atomically renames each inbox ref to claimed/\u003csame-suffix\u003e, so the claimed ref still points at the submitter-authored candidate commit described immediately above; that commit cannot already record the drain run parent or the batch candidates. Later text instead assumes a new claim commit with metadata. Define the object graph and atomic push exactly: preferably one batch-claim commit/ref that keeps every candidate reachable, records the initial published and mirror heads, and is created atomically with all corresponding inbox deletions. Per-candidate claims conflict with the singular reconciliation model: a normal N-candidate drain produces N outstanding claims but one published batch head, so early claims see a later batch head and can be classified as anomalies unless every claim redundantly and atomically binds the same batch. F2 BLOCKER — parent plus candidates does not compute the exact expected published head under the implementation this note deploys. gitstore.SignedCommit includes current author and committer timestamps, hence replaying the same parent and candidate inputs later can create different commit IDs. Prebuild the sequence commits before claiming, keep their objects reachable from the batch claim, and record the exact expected head E in the claim; do not promise reconstruction from insufficient inputs. F3 BLOCKER — the claimed three-state table omits ordinary and adversarial states. A crash after mirror advancement but before release yields published=E and mirror=E with a live claim; none of the three cases matches. Add that fourth normal state and release only after verifying both exact heads. State a start invariant, such as no new claim unless verified published=mirror=P, and specify the no-claim table: a missing/truncated claim with divergent refs must refuse for manual reconciliation, because a rewind to a stale mirror with the claim lost otherwise looks quiescent. Protect claims from submitter deletion and test deliberate claim loss. Enumerate multiple outstanding claims or exclude them structurally with the single atomic batch claim. F4 MUST FIX — the main protocol still says an admitted claim may be deleted once only the sequence push is confirmed and stale claims are simply re-drained, while case 6 says publish, mirror, then release and reconcile first. Make steps 3-4 use the same state machine, and update case 8 so both crash points assert mirror convergence before claim release; otherwise case 8 can pass while exercising the obsolete release-after-publish behavior. Mirror-never-first does remove the original ordinary mirror-ahead crash when operations are serialized, but it is not enough without exact leases/rechecks and the complete claim state. Spike case 6 must exercise every table row, N\u003e1 candidates, claim loss, and a concurrent rewind between publish and mirror; each unexpected tuple must refuse without moving either durable ref.","body":{"commit":"497c290cce9de539602c1b85cbada19125723f10","head":"497c290cce9de539602c1b85cbada19125723f10","verdict":"changes-requested"}}