{"kind":"artifact","text":"Candidate: the ambient adjunct at cmd/gitseq-mcp on the same bridged head. Every completed tool call carries live_attention, attached where both success and error results are built, so a failing call still tells the caller who addressed them. Event identifiers are extracted by exact canonical pattern from the call input and its result, capped at 32. live_attention is a sibling of structuredContent rather than a wrapper, because a documented consumer reads structuredContent directly and nothing pins that envelope's shape. The read never returns an error, and a guaranteed text block states pending chat and relevant actors.","body":{"commit":"d3506e02920dfc79d8b051ed06fe0e026722ca56","path":"cmd/gitseq-mcp"}}