{"kind":"artifact","text":"internal/app at exact head 1f62e8209195f0be232be88ee8ca476d9352061f on request/custody-lost-update, covering request #8762 condition 2 for every caller of apphost.SaveConfig. The three update sites — AddActor, RetireActor and rememberVerifiedFrontier (the reported defect at the old app.go:448 region) — now persist through Workspace.updateConfig over apphost.UpdateConfig and adopt the merged view, so stale custody in memory refreshes from disk even when a save changes nothing. The three creation sites — initHosted, AttachConfig and host.Init — keep SaveConfig, each having verified the configuration file's absence first, which is now SaveConfig's documented contract. Merge rules are the caller's declared intent rather than last-writer-wins under another name: AddActor refuses different custody for a name already held and treats identical custody as a no-op; RetireActor is an idempotent delete; mergeVerifiedFrontier states the frontier rule — the deeper verified depth stands whoever recorded it, an unchanged audit writes nothing, and two different heads claiming one depth are a genuine conflict refused loudly, not a race resolved by arrival order. custody_lost_update_test.go is the requested regression written from desired behaviour across two separate config openings: process A opens the workroom, process B opens independently and records actor custody, A then persists an unrelated field through Verify, and B's custody must survive on disk. Gates at this exact head: gofmt -l empty; go vet ./... clean; go test ./... ok; go test -race ./... ok; git diff --check clean. Mutation-checked: reverting rememberVerifiedFrontier to the old whole-config save makes this test fail with exactly the lost-custody message. The full suite caught one real gap in the in-flight fix, repaired here: an attached view whose metadata directory holds no configuration yet could not record its first frontier, so UpdateConfig accepts the caller's memory as base solely for that absent-file case, decided inside the lock.","body":{"branch":"request/custody-lost-update","commit":"1f62e8209195f0be232be88ee8ca476d9352061f","conditions":"every SaveConfig caller covered; conflict rule stated explicitly, not last-writer-wins; regression across two separate config openings, mutation-checked","gates":"gofmt -l . empty; go vet ./... clean; go test ./... ok; go test -race ./... ok; git diff --check clean","path":"internal/app"}}