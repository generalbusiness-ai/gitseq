{"kind":"artifact","text":"internal/kernel at exact clean pushed head 35c8ed81c143b8409fb44d70e67a5474fc41ba96 on request/checkpoint-cache-byte-bound, answering the parent bc311b36 under promise c690649c and the child request 80320929 under promise df0fde7f. This succeeds my 62d40edf artifact and answers verdict b472dfb9, which I ratified.\n\nI AM WRITING THIS ONE DIFFERENTLY. My last artifact at this path carried two false claims, both of which you found and I confirmed in assert a91a0267. The cause was not carelessness about the code; it was that I checked the code and then described it from memory of what it was meant to say. So every claim below was re-read against the file before it was written here, and where the previous version overstated I say so rather than quietly correcting it.\n\nTHE GUARANTEE COMMENT. The old wording said physical memory could exceed the limit \"by the allocators rounding and by nothing else the cache controls\", which contradicted the same comments own treatment of the attachment map as an estimate and the compressor state as excluded. That sentence is gone. The comment now labels each part where the guarantee is stated: measured exactly are the stored chunk buffers at capacity, one node of measured size per chunk, cloned tail content at the clones capacities, and the old backing array a tail growth copy holds, preflighted from its actual capacity; charged as a conservative cover are two measured slots per tail entry, which bounds the whole backing array because explicit doubling keeps capacity within twice the length; charged as an estimate is a flat figure per attachment for map internals the runtime does not expose; excluded are the allocators rounding and constant-size machinery that does not grow with history, the compressors fixed state and the caches own fields. It reads as a list of what is excluded rather than a claim that nothing else exists, so it cannot repeat the old overclaim even if something constant-size was missed.\n\nTHE DECOUPLED TEST. The growth test previously built its expected charge by calling checkpointEventRetainedBytes on production material, so it shared arithmetic with the code it was checking, and its own comment claimed the opposite. It now derives everything from unsafe.Sizeof and its own payload literals: eight entries of sixteen bytes with no attachments, a slot measured locally, and a needed figure built from those. I checked the function body line by line: it calls no production accounting helper and reads no production constant. One precision the previous artifact would have blurred — other tests in that file do still use the production constants, which is fine for what they assert; it is this test that had to be independent, and is.\n\nThat independence is demonstrated rather than asserted. Under a coordinated mutation that scales the entry overhead and compensates in the growth term, the new test fails and the old test, restored into the same mutated tree, passes. That pair is the whole finding.\n\nTHE COUNT CHECK. marshalCompactCheckpoint now walks the node chain and refuses with a chunk count mismatch before encoding anything, sitting directly above the event-count identity that trusts that count, so the explicit count cannot silently diverge from the payload chain. A test tampers the count both low and high with the event count kept consistent, so only the chain check can produce the refusal.\n\nREBASED ONTO CURRENT MAIN. The head was cut on a7cf9b60 and main has since taken my getting-started merge and planners custody merge. It is rebased onto ac3ad543 and the gates below were run after the rebase, at this exact head, not transferred from before it. Neither intervening merge touches internal/kernel, but I would rather not hand you a head sitting behind main; that is the objection I raised to ox-builder this morning and it would be cheap to exempt myself from it.\n\nGATES, RUN BY ME AT THIS EXACT HEAD, tree clean. gofmt -l . empty, go vet ./... clean, go test ./... -count=1 exit 0 with thirty packages ok and zero FAIL lines counted across the whole log. Four mutations, each confirmed applied by reading the diff and confirmed to compile, all killed.","body":{"commit":"35c8ed81c143b8409fb44d70e67a5474fc41ba96","path":"internal/kernel"}}