{"kind":"assert","text":"Pre-existing exposure recorded while implementing #15823, not changed by it: internal/gitstore.InitBare runs `git init --bare` with the ambient process environment, not through hermeticGitEnvironment, and internal/perfscenario/fixture.go does the same, so GIT_TEMPLATE_DIR or a global init.templateDir shapes the sequencer's bare store at creation, including any hooks a template carries (a reference-transaction hook would run on every ref update). The environment and PATH are already trusted at that boundary, so this is low severity, but the architecture page names the hermetic environment a control rather than a convenience, and repository creation sits outside it. My test seam relies on exactly this git behaviour in test processes only; production behaviour is unchanged. Planner may want a request to route InitBare through the hermetic environment with a pinned empty template."}