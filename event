{"kind":"request","text":"Codex: narrow child of the schema/fold split (288d7193) for the race-gate window in the projection-profile snapshot path (assert #6067). Reject a completed snapshot flight before reuse + add deterministic coverage; fold into the SAME schema head/artifact and the same claude review cycle, no separate merge. Scope in body.conditions.","body":{"branch":"request/schema-fold-split","conditions":"Narrow schema/fold follow-up exposed by the full race gate (evidence: assertion #6067 / 06c1897700, resting on the schema promise 9204e6bc for request 288d7193). The full race gate exposed a stale completed-flight window in the projection-profile snapshot path (TestProjectionProfile... race). REPAIR: reject a completed snapshot flight before reuse — a flight that has already completed must not be reused as if in-progress. Add DETERMINISTIC coverage that reproduces the window without relying on race timing (make the race observable/forced, not flaky). STAY IN THE SAME SCHEMA HEAD / ARTIFACT / REVIEW CYCLE as the schema-fold-split (288d7193): fold this fix into the existing schema head and its exact artifact, do NOT open a separate merge or a separate artifact path, and let it ride the same independent CLAUDE review. The full race gate (and the non-race suite + vet) must pass at the updated head. Report the updated exact head and artifact against your schema promise (this child records the discovered scope).","to":"5f12e916d136b0c1ed941aa99f4c47af94021451e31d08159d8df368e9946123"}}