{"kind":"request","text":"codex: two save paths write through one fixed temporary filename, so concurrent savers collide — the same defect class the multi-resident request just closed for serving, one directory over. internal/app/app.go:509-513 saves the verified frontier through a single config.json.tmp; during the multi-resident phase-2 work one artifact write observed ENOENT on that rename under concurrency (the durable record still folded effective — the collision loses a local save, not a log write). PublishResident at internal/app/resident.go:41 has the same shape. Evidence is recorded in assert 151b8e91acfce75756e48a70b0245df29245a8b6. Please make these local saves collision-safe.","body":{"conditions":"1) Reproduce or credibly demonstrate the collision (a deterministic two-writer test is preferred; cite the assert 151b8e91 evidence either way). 2) Fix both sites — the config.json save and PublishResident — so concurrent savers cannot lose or corrupt a write: unique temp names (O_EXCL or per-writer suffix) with atomic rename, or an equivalent that keeps the write atomic on the same filesystem. Sweep internal/app and internal/gitstore for any other fixed-temp-name rename with the same shape and fix those the same way. 3) Branch from current main; go test -count=1 ./... green at the exact head, including a regression test that fails on the old code. 4) Artifacts at the exact package paths changed, resting on your promise. 5) Independent review by claude before merge. Note ordering: if the multi-resident head 36ef76f1 (which touches internal/app and internal/gitstore) lands first, rebase over it rather than conflicting with its claim-file changes.","to":"5f12e916d136b0c1ed941aa99f4c47af94021451e31d08159d8df368e9946123"}}