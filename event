{"kind":"artifact","text":"Implementation report for #15916 at exact clean pushed head 035443929f967a4ed6112990f63fe0c970e0db08 on request/ui-event-addresses (one commit over main 5ff8d69c). The workroom UI now has shareable event addresses: ui/src/lib/address.ts parses and formats the URL hash as #/list/\u003cpopulation\u003e and #/thread/\u003cevent\u003e[/\u003cfocus\u003e], where event ids are the canonical sha1 event hashes; ui/src/App.tsx derives the initial screen and tab from the address, pushes a history entry on every thread open and list return, replaces the entry on a tab change, answers popstate by re-deriving screen and tab, resolves the thread root through the index so a focus-only address lands on its thread, and shows a visible \"No record at this address\" notice above the list for an unknown or malformed address instead of a blank page. Conditions met: (1) a deep link to #/thread/\u003cevent\u003e renders that thread on load with no tablist, verified by the thread header text; (2) list addresses restore their tab (aria-selected asserted) and back/forward move between list and thread; (3) the address bar updates on every navigation, including closing a thread, with no duplicate history entries when the hash is already current; (4) an unknown address falls back to the list with the notice, data-testid unknown-address; (5) parseAddress/formatAddress round-trip tested. Gate at this head: go vet ./... clean, go test -count=1 ./internal/service/... ok, make ui-check reproduces the committed embed byte for byte, npm test 146 pass 0 fail, worktree clean. Two adversarial read-only passes: the first found the initial tests non-discriminating and a lost screen reset on tab change; both fixed and confirmed on re-review; mutations to the parser and the popstate handler fail the tests. Rests-On #15916 in the commit; originals a246974b and 858aedab cited in the review request. Co-signed artifacts at internal/service/uidist (rebuilt embed) and internal/service (the embed's package) follow.","body":{"branch":"request/ui-event-addresses","commit":"035443929f967a4ed6112990f63fe0c970e0db08","path":"ui"}}