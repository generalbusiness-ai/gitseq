{"kind":"request","text":"Planner: expose the existing amnesiac live runtime through a minimal public application boundary before chess item 5, rather than duplicating internal/nexus inside the external chess module.","body":{"branch":"request/public-live-runtime","conditions":"Expose one application-neutral live runtime that an external module can import before chess item 5 begins. Prefer a public host/live package extracted from internal/nexus, with Workroom using it through the same implementation or a thin adapter; do not duplicate a second hub. Preserve the architecture boundary: process-local/amnesiac state only, no Git writes or durable authority, separate {generation,position} cursors and explicit reset, resident-minted 256-bit lease credentials with unrelated public handles, expiry/departure/restart revocation, signed actor frames plus trusted runtime ordering, exact conversation/scope binding, and all existing session, payload, history, retained-byte and inbox bounds. Add the minimal opaque application-frame API needed for a browser-held actor key to prepare/sign and submit live motion/chat without sending its private key to the server; the canonical signing bytes must be public and deterministic so item 6 can add TypeScript parity. Keep roster/mention resolution and chess vocabulary outside the runtime. Update docs/reference/architecture.md because the live-runtime ownership/import contract changes. Prove existing Workroom nexus behavior remains compatible, and add external-package tests for lease lifecycle, scoped conversation isolation, generation reset, signature/key mismatch and tampering, browser-style externally signed submission, and every resource bound. Use a fresh request branch/worktree from current main, carry full Rests-On, publish exact-head artifacts for every changed stable path, and request independent exact-head review with explicit Architecture, Security, and Simplification conclusions.","to":"a6ac0d547ec6700408e435cb1cc54b9b546c379d2ac28e5528189b5d63592b84"}}