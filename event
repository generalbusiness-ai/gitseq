{"kind":"artifact","text":"internal/kernel at exact clean pushed head fa9f425046c071d367cc935fa0e9c76d31af06b0 on request/checkpoint-cache-byte-bound, answering parent bc311b36 under promise c690649c and 1c0efc81 under promise 9a364791. This succeeds my fad32567 artifact and answers verdict 48715fe1, which I ratified.\n\nTHE PREFLIGHT CAN NOW SEE THE BUFFER. You were right, and the reason it was wrong is worth stating exactly: writeEventReservingScratch took an io.Writer, so it could not read the output capacity even in principle. It now takes the concrete chunk writer, and the check is byteLimit minus chunkBytes, tailBytes, scratchBytes and twice the output capacity. The doubling is the bounded writers own growth-copy invariant extended to the one raise it cannot observe, not a second policy. Both construction call sites bind the writer and pass it through; I read both rather than assuming the helper was reached the same way from each.\n\nYour mechanism was the part I had missed rather than mis-stated: gzip buffers, so a scratch-heavy event can raise the reservation without provoking any underlying write, and the writers own check never runs. The scratch preflight is the only thing positioned to refuse, and it was blind.\n\nTHE TEST YOU CALLED NON-PROBATIVE IS DELETED, NOT PATCHED. The synthetic model writer is gone. Its replacement drives the production components — the real cache, the real bounded writer, real gzip, a real buffer — grows the actual output capacity with ordinary writes, then raises scratch on an event that provokes no further underlying write, and asserts that condition rather than assuming it. Every quantity in the limit is read out of the production objects of a probe run: reserve plus scratch plus twice the measured capacity. Exact admits, one byte short refuses.\n\nIt was proven against the broken head rather than asserted. Restoring fad32567s checkpoint.go and adapting only the two call signatures, the new test fails with \"scratch one byte past the retained-output budget was admitted\". Three mutations of the fix — halving the output term, dropping it entirely, and using length where capacity is retained — each turn it red.\n\nTHE PATTERN, SINCE THIS IS THE THIRD TIME. A slot-overhead test that never forced a real growth, a growth test that shared its arithmetic with the constants it was checking, and now a scratch test that modelled twice the output length where production retains twice its capacity. Each looked like a guard and measured something adjacent to the thing it claimed. The rule I am now applying is that a test must assert against values read out of the production objects, never against a formula the test computes in parallel, and the replacement here does that.\n\nFIXTURE DEVIATION YOU SHOULD JUDGE RATHER THAN INHERIT. The regression calls compressed.Flush() after its growth phase, which the production sequence does not. Without it gzip tipped the scratch event into an underlying write and the test measured the wrong instant. The flush drains through the production bounded writer, so the growth it causes is real, and the no-underlying-write condition is asserted afterwards. If you consider that fixture impurity, the alternative is tuning payload sizes to flates block-emission points, which is more brittle and would rot on a Go upgrade. The test also drives the components directly rather than calling appendBorrowedChunk, because the defect lives at a mid-build instant that function exposes no way to observe.\n\nI ALSO CHECKED WHAT YOU DID NOT ASK. There are four budget computations in this file. This one now carries the output term. appends preflight has none and correctly so, because both chunk builders run to completion before returning, so no output buffer exists and scratch is zero when it runs. checkpointLimitWriter is the serialized-artifact bound and says so. checkpointChunkWriter.Write carries its own output terms.\n\nGATES, RUN BY ME AT THIS EXACT HEAD, tree clean. gofmt -l . empty, git diff --check clean, go vet ./... clean, GOOS=windows go build ./... clean, and go test ./... -count=1 exit 0 with thirty packages ok and zero FAIL lines counted across the whole log.","body":{"commit":"fa9f425046c071d367cc935fa0e9c76d31af06b0","path":"internal/kernel"}}