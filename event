{"kind":"assert","text":"One condition of this request is not achievable as written, and the reason is worth having before someone treats the remainder as mechanical.\n\nThe conditions say the head \"merges cleanly with current main, which has moved through five merges since the branch point\". It does not merge cleanly. Main restructured the documentation after the branch point, and two of the branch's four files no longer exist where it left them: docs/reference.md was replaced by a docs/reference/ directory, and docs/getting-started.md was split into docs/how-to and docs/reference pages. Merging produced four conflicts, one per file.\n\nI have carried it across at 2bd4dc46 on request/dot-artifact-migration, resolving each deliberately rather than by preference. AGENTS.md step 5 keeps both sides — the branch's per-path retire-and-publish rule and the self-initiated-work exception main gained today — because taking either alone silently drops the other, and I merged that exception myself this afternoon so I would have been reverting my own work without noticing. SKILL.md keeps both for the same reason. docs/getting-started.md takes main's version, because the branch carried a copy predating the restructure and restoring it would have undone that split. docs/reference.md stays deleted.\n\nThe consequence is real follow-up rather than a tidy-up. The branch's merge-artifact guidance — the four cases for choosing a path, the bare-supersede authority rule, the warning about path dot claiming the whole repository — lived in the two files main re-homed. That prose is not in the tree any more, and it is the operative part: without it the AGENTS.md rule says retire every live artifact and publish a successor at the path each area keeps, and never says how to choose the path. It needs writing into the new structure, most likely docs/how-to/end-to-end.md and a reference page for merge.\n\nI have not done that yet, and I have not started the three findings. This note exists so the state is legible if the session ends here: the carry is done and committed, the guidance re-homing is open, and F1, F2 and F3 are untouched."}