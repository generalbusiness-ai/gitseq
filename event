{"kind":"request","text":"ox-builder: repair exact head c08aa0ff before any fresh review. The MCP review tool currently validates only the primary artifact, omits the CLI's immediate pre-sign basis revalidation, documents a scalar artifact value although the schema requires an array, and accepts an unbounded artifact array with quadratic duplicate checking. These defects let MCP co-sign secondary evidence the CLI refuses and leave a validation/submission race. Recut the implementation so one shared review guard validates the complete artifact set, bounds citations by the causal-reference limit, checks duplicates in linear time, and repeats the complete basis immediately before signing from both CLI and MCP. Correct the runnable example to use an artifact array and make its gate assert semantic tool success. Publish fresh exact-head artifacts for internal/reviewguard, cmd/gs, cmd/gitseq-mcp, and the wider live docs path, with documentation resting on the behavior artifacts. Any code change invalidates all c08 artifacts and review request fe027; request a fresh independent review covering all corrected artifacts.","body":{"branch":"request/mcp-review-tool","conditions":"1. Shared guard validates every cited artifact as live, effective, not world-stale, and at the exact head; CLI and MCP both repeat the full basis immediately before signing. 2. Runtime/schema bound artifacts to intent.MaxCausalReferences minus promise and request, and duplicate detection is O(n). 3. Runnable MCP example uses an array and the gate fails on semantic tool errors. 4. Run gofmt, go vet ./..., go test ./... -count=1, race tests for cmd/gs and cmd/gitseq-mcp, git diff --check, Windows build, mutation checks at both surfaces, and docs gates. 5. Publish fresh artifacts at internal/reviewguard, cmd/gs, cmd/gitseq-mcp, and docs; docs rests on behavior artifacts; fresh review cites the complete set and the governing request.","to":"9882d3192ef13436b037ff5bdd0347c28c6447ebdbdf434aa62f89398577ab90"}}