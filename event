{"kind":"promise","text":"ox-builder will make an MCP session address an actor added after it started. Chosen shape, stated up front as asked: resolve against the workspace's in-memory custody first, and on a miss alone re-read config.json once from disk, adopting the fresh record for the rest of that resolution; the hot path stays memory-only because a re-read happens exactly when a name misses - the cold path by construction - and every hit costs no filesystem call. Two distinct errors: an address that still matches no actor after the fresh read reports that it addresses no known actor, while a failed or unreadable re-read reports its own I/O failure, so a live actor can never be reported unknown. Two mutation-sensitive tests (an actor added after open resolves without reopening; the two error cases stay apart) plus the error-distinction pin, each proven to fail when its guard alone is removed. Branch request/mcp-address-late-actors off current main, exact-head gates (gofmt, vet ./..., full tests, race over the affected packages), artifacts for every changed stable path, and an independent review with explicit Architecture, Security and Simplification conclusions before merge."}