{"kind":"request","text":"claude: two small board-UI fixes hugh asked for today (2026-09-01), one head. Facts verified at current main c3169d83 (and origin/main).\n\nTITLE. ui/index.html:6 hardcodes \u003ctitle\u003egitseq workroom\u003c/title\u003e, so every installation's tab says \"gitseq workroom\" regardless of the repository it serves. Hugh wants \"\u003cproject-name\u003e workroom\" — generally the folder name: \"gitseq workroom\", \"tailapp workroom\", and so on. The service already exposes what is needed: /v0/worktrees returns repo, the absolute path of the checkout being served (internal/service/ui.go:130, consumed at ui/src/lib/api.ts:287-295). Derive the display name from that path's basename and set document.title once the value arrives; keep a truthful static fallback (\"workroom\") for before it loads or when the endpoint fails. Use your judgment for linked worktrees — the project identity should be stable across worktrees of one repository where the served path makes that determinable.\n\nFAVICON. The UI ships no favicon at all: no link rel=icon in index.html and no icon asset in ui/. Create an appropriate one — your design, kept simple and legible at 16px in both light and dark browser chrome; an inline or served SVG is preferred over binary formats. Something evoking the project's nature (an append-only sequence / chain of records) would be apt, but the design is yours.\n\nANCHOR NOTE. No fresh artifact exists at the ui path — every live one is stale — so this request rests on the fresh internal/statusview artifact (the projection surface the board renders) and states its facts self-contained; your implementation artifact for ui becomes the fresh successor that repairs that gap.\n\nCONDITIONS: (1) the tab title renders the served repository's folder name plus \"workroom\" against a live service, with the static fallback when the name is unavailable; a UI test covers the derivation including the fallback. (2) A favicon is served and referenced from index.html and renders in light and dark tabs. (3) The embedded UI is rebuilt (make ui) so the served bundle carries both changes — the embed is not covered by the Go gates — and the UI gates pass at the exact head (npm ci first in a fresh worktree). (4) Publish the exact-head artifact for ui and request independent review citing this request.","body":{"conditions":"Tab title shows the served repository's folder name + \" workroom\" with a truthful static fallback, covered by a UI test including the fallback; a favicon is served, referenced from index.html, legible at 16px in light and dark chrome; make ui embed rebuilt and UI gates green at the exact head; exact-head artifact for ui published and independent review requested citing this request.","to":"a5d35aa7e4799472a208663d7f024917442bd513929f9eccf450628ccf70095d"}}