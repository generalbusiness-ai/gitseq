{"kind":"request","text":"claude: add outputSchema to the gitseq MCP tool definitions. hugh reports that some MCP clients only recognize structuredContent when the tool advertises an outputSchema, and gitseq's server hand-builds all 14 tool definitions in cmd/gitseq-mcp tools() (main.go ~579+) with inputSchema but no outputSchema, while it does return structuredContent (main.go ~444). This is additive and interoperability-only; no behaviour changes. One important caveat to honour: declaring an outputSchema makes conformant clients VALIDATE structuredContent against it and reject non-conforming responses, so the schema must not be stricter than what the tools actually return across all variants.","body":{"conditions":"Add an outputSchema (JSON Schema, type object per MCP spec) to each of the 14 tool definitions in cmd/gitseq-mcp tools(), so clients that gate structuredContent recognition on outputSchema presence work. Scope it to the tool's structuredContent object (the tool's own payload), NOT the result envelope — isError and live_attention are siblings of structuredContent, not inside it. Start permissive so no conformant client rejects a valid response: at minimum {\"type\":\"object\"} (optionally additionalProperties:true and naming well-known top-level keys the tool always returns, e.g. resultType), rather than a strict schema that must match every response variant; a per-tool tightening can follow later. Verify against a real client that structuredContent is now recognized. Update cmd/gitseq-mcp/main_test.go (the tools-list test currently asserts 14 tools and inputSchema shape) to assert every tool now carries an object-typed outputSchema. SEQUENCING: cmd/gitseq-mcp is being rebuilt by the completion-reliability lane (candidate on request/completion-reliability-current, @15); branch from main AFTER that merge lands and verify against the merged file so every basis is fresh. Standard flow: request/\u003cslug\u003e branch + worktree, implementing commit carrying Rests-On this request, artifact at cmd/gitseq-mcp resting on your promise, independent review by a different actor recording Architecture/Security/Simplification, merge with succession. Do not merge before review.","to":"a5d35aa7e4799472a208663d7f024917442bd513929f9eccf450628ccf70095d"}}