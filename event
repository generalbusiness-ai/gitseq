{"kind":"request","text":"Claude: re-cut ambient live attention on current main under a fresh non-stale authorization. Preserve the substantively unreviewed ea570287 tree, integrate current main without behavior drift, and keep every shipped same-path artifact live until the approved replacement merges.","body":{"branch":"request/ambient-live-attention-v2","conditions":"Every completed Gitseq MCP tool call carries a bounded live_attention adjunct when the resident can answer, including tool-specific error results where the attention read succeeds. The adjunct reports the live cursor; this exact session’s bounded unacknowledged addressed frames with pending and omitted counts; and actors whose leased focus exactly matches EventIDs explicitly named by the tool input or returned in its result. Matching sessions are filtered before actor aggregation, and each actor row carries the full fingerprint, name, matching-session count, matched EventIDs, status, note when present, and a resident-observed activity_changed_at that changes only when status, focus, or note changes, not on heartbeat renewal. Priority frames repeat until explicit ack, and the guaranteed text block states pending chat and relevant actors so clients that ignore structured content still see the interruption. No resident yields available=false and never causes a durable operation to fail. Presence, focus, timestamps, chat delivery, and acknowledgement remain ephemeral and advisory: they create no ownership, promise, authority, completion, or durable read receipt. Existing status, wait, presence, say, and ack behavior remains available; waking an idle host and relationship inference beyond exact EventID matches are out of scope. Update affected schemas, architecture, reference pages, SKILL.md, and limits; test boundedness, expiry, renewal age, multi-session aggregation, no impersonation, repetition until ack, text summaries, tool errors, and degraded resident across adapter, service, and Nexus. The implementing commit must carry this request’s full canonical ID in Rests-On. Candidate artifacts must cite this request and its promise plus the current live same-path predecessor; the docs artifact must also cite the three behavior candidates. Keep shipped predecessors live through exact-head review. After approval and merge, publish merged successors on this request and promise before retiring predecessors and candidates in the same succession step. Run make test, make race, make docs, git diff --check, and obtain a fresh independent exact-head review.","to":"a5d35aa7e4799472a208663d7f024917442bd513929f9eccf450628ccf70095d"}}