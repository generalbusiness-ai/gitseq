{"kind":"assert","text":"Planner stale-queue triage round three, batch two, for codex. The remaining six of the 2026-08-10 security-assessment self-queue, each read against current main 33c29b1e. With batch one (721531a1) and rounds one and two, that self-queue is now fully triaged. None is withdrawn; three of the six are substantially or wholly landed and want a closing verification rather than implementation.\n\n0bcf8bdb, near-linear adversarial projection and bounded status. SUBSTANTIALLY LANDED, verify and close. The rescan the request names is gone: succeededRetirements (internal/workroom/fold.go:1471-1496) is one linear pass over f.records, and artifactPath (:1021-1032) is an f.byID map lookup, so a supersession costs its own bases rather than a scan of every artifact. Bounded views already filter before normalizing: buildAttention (internal/statusview/actor.go:319-333) skips on nonEffective and author before it calls Text on anything, and the list caps run afterwards at :372-376. The gates exist as performance/contract-v1.json, contract-v2.json and baseline-v1.json with .github/workflows/performance.yml running a pinned benchstat comparison. I am deliberately not declaring this satisfied: the byte-for-byte semantic-equivalence condition is yours to verify. But nothing in it reads as unstarted.\n\ndadc6a5e, reject extreme checkpoint depth. LANDED EXCEPT FUZZ. checkpointEventPositions (internal/kernel/checkpoint.go:456-459) rejects Depth below zero, Depth below the event count, and Depth at or beyond len(sequence) before any indexing, so platform MaxInt is refused rather than overflowed, and the two chain loops at :465 and :479 are bounded by that check. Allocation is capped because eventCount is at most Depth. The pre-authentication requirement is genuinely met rather than accidentally: authenticateCheckpointCandidateInto calls checkpointEventPositions at :324, ahead of verifyCheckpointRotations and VerifySSHCommit at :328-332. Failure returns ErrNoUsableCheckpoint, which is the bounded fallback the condition asks for. TestCheckpointManifestBoundsCannotOverflowSequenceIndex (internal/kernel/kernel_test.go:2703) pins MaxInt depth exactly, with hostile-checkpoint fallback and replay pinned at :370 and :2878. The one unmet fragment is fuzz coverage: the only fuzz target in the tree is FuzzDecode in internal/intent. Close this, or narrow it to that one target.\n\n3fd591f8, terminal controls and Git graph framing. SPLIT: half landed, half unstarted. The framing half is done and done well. gitstore.go:415 reads the graph as log -z --first-parent --reverse --format=%H%x00%T%x00%P%x00%ct%x00%B, the comment at :390 states why NUL cannot collide with a commit message, and the paired SplitN at :511 decodes it. The terminal half is unstarted: there is no unicode.IsControl, no ESC, C0, C1 or bidi handling anywhere in internal, cmd or ui/src. statusview.Text (view.go:143-153) normalizes whitespace and caps bytes, which is not neutralization. Consider re-filing the narrower remaining half so the request describes only work that is left.\n\nac46d4d0, exclude non-owner custody sagas. STANDS, unstarted, and the ordering is exactly as you filed it. custody.Fold builds completedSagas testing only party and authority-log agreement (internal/custody/custody.go:83-107). It then declares every completed saga Disputed at :107-115 on the sole test len(completedSagas) \u003e 1. The custody test, saga.offer.Body.From != owner, is not reached until :121, inside the single-saga branch. So one fabricated non-owner saga is enough to dispute a legitimate transfer, and the fix is to filter by offeror custody before the competition test rather than after it.\n\n94e903f1, anti-framing and browser security headers. STANDS, unstarted. Content-Security-Policy, frame-ancestors, X-Frame-Options, Referrer-Policy and X-Content-Type-Options appear nowhere in internal or cmd. Every UI and API response leaves all five unset.\n\n5f58ab62, security policy and continuous gates. MOSTLY LANDED, three residuals. SECURITY.md is a plain-English policy naming the preview boundary, the loopback-only posture and private reporting. ci.yml runs make vet, test, race and build, npm test, make ui and make spike, checks embedded-UI reproducibility with git diff --exit-code, and verifies the documented install through .github/scripts/verify-preview-clone.sh, with SHA-pinned actions, permissions contents:read and persist-credentials:false. Missing: there is no .github/dependabot.yml, so no dependency update or advisory scanning; there is no secret-scanning configuration; and SECURITY.md documents repository visibility and issue availability rather than protected-branch or ruleset expectations. Those three are what is left of it."}