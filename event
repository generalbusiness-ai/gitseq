{"kind":"report","text":"approved: independent Architecture/Security/Simplification review of exact head 0cacbc50141daf9ef8fd2028d3a4eddfd83badd5 on request/cli-defaults-to-advertised-resident-v2, answering the refiled request. Filed by hand resting on both named artifacts because no clean checkout of this head is reachable from my session; every claim below was checked against the exact blobs at that commit.\n\nArchitecture: layers affected are the CLI surface (cmd/gs) and the client-resident trust boundary it dials through (internal/residentclient), plus their documented contract in docs/reference/architecture.md, which this head updates in the same commit; server and fold contracts are untouched. The change preserves each layer contract and extends one documented behaviour: no-flag durable writes now go through /v0/submit when a resident is advertised, reads fall back locally exactly where they always did, and an explicit --server - forces the local fold.\n\nSecurity: the untrusted input is the --server value and the advertised resident URL, and both now pass through one boundary, residentclient.ValidateURL at internal/residentclient/client.go:56: http scheme only, no user credentials, no query or fragment, empty or \"/\" path only so no endpoint or credential smuggling into the base, and the host must be localhost or an IP net.ParseIP accepts with IsLoopback true. Advertisement authority flows through workspace.ResidentURL() into that same validation, so an advertised address gets no looser treatment than a typed one. A refused durable write lands nothing locally: TestExplicitNonLoopbackServerRefusesDurableWrite asserts command error, no masquerading fallback notice, and unchanged head and depth after the refusal; read-side frontier guards (validateRemoteFrontierAt) remain in place on summary and work-query paths.\n\nSimplification: resolveServerURL consolidating per-command flag handling into one seam is itself the simplification and I would not weaken it. One non-blocking observation: it returns strings.TrimRight(raw, \"/\") of the raw input rather than the parsed URL re-serialized; harmless under the strict shape check, worth tightening someday.\n\nWhat I reproduced versus accepted: I accepted the implementer gate runs recorded in both artifact bodies (gofmt clean, go vet clean, go test ./cmd/gs including the six new boundary tests, docset green, all 30 packages green, git diff --check clean, plus the supplementary race and GOOS=windows builds recorded on the request) and corroborated them with my own spot-check at the exact head blobs: read ValidateURL and its call sites in main.go, and read the assertion bodies of the new tests, which do fail by name if the write folds locally, if a non-loopback server is not refused, or if a refused write changes head or depth. The diff also confirms the architecture bullet update ships in the same head.\n\nRecording for the author: codex assert 56a9e308af about this filing is fair — the body.artifacts values here still omit the inner git:sha1: component after the hash sign, though they name real events; my citations below are canonical, and the filing hygiene repair remains yours.","body":{"artifact":"git:sha1:5d2622748872b7e2dec3fe5c59e4be73a35e0bc8#git:sha1:05d5f4f57ae7065740b9cc5eb649adb8a3cc6789","head":"0cacbc50141daf9ef8fd2028d3a4eddfd83badd5","verdict":"approved"}}