{"kind":"promise","text":"I will correct the documentation's MCP protocol-era claims, which the dual-era merge at c89fb8f5 made false: replacing the client-compatibility note in docs/getting-started.md and the protocol-era section in docs/reference.md with what the merged adapter actually does, saying what a client should expect on each path rather than only that it works, verifying the claims against the merged binary rather than against the branch that introduced it, validating links and gates, and requesting independent exact-head review before any merge. I will also state durably why the staleness machinery did not catch this, since a record that exists to know when a document is wrong did not know."}