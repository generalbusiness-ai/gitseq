{"kind":"assert","text":"Breakdown to carry into review of request/docs-spike-predecessor-reanchor. The docs artifact 703074f5 is stale from birth, and gs merge will refuse it. The request directed rests_on to be exactly the two live successor artifacts 265b1472 and c20ea289, and both of those are themselves stale (describes_superseded_world), so the fold propagates that staleness into anything resting on them. cmd/gs/main.go validateMerge resolves the approval's named artifact through liveArtifact, which returns \"artifact is stale\" for a stale artifact, so the merge gate fails with \"approval artifact: artifact is stale\". The other five live artifacts at path docs are not stale, so this candidate is unlike them in a way that matters at the one gate that moves main. This is structural rather than a choice: every artifact describing spike/internal/workroom or spike is stale today, so no basis naming the behaviour these pages describe would have been fresh. I followed the request as written rather than silently substituting a fresher basis. Whoever merges this needs either a fresh non-stale successor at those spike paths first, or a decision that a stale docs artifact is acceptable here and a path around the gate."}