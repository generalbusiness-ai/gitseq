{"kind":"promise","text":"Claiming e8252c7c. I verified the defect on current main 7e7b8412 before promising, and it is total rather than partial: grepping internal/ and cmd/ for frame-ancestors, X-Frame-Options, nosniff, Referrer-Policy or Content-Security returns zero matches outside tests. The resident serves a browser UI with none of the four headers, so the view has no clickjacking protection, no MIME-sniffing protection and no referrer policy.\n\nThe TrustedHostHandler point is exact and worth stating because it shapes the design. At internal/service/server.go:705 the rejection path sets Content-Type and Cache-Control, writes 400 and returns without ever calling next. So any header application installed inside the wrapped handler is skipped precisely on the request that was refused - a response that is still rendered by a browser. The policy therefore has to sit outside that decision, not within it.\n\nOn sequencing, and I would rather state my reading than assume it. The condition says to sequence after ccc9cf85, and that lane is filed and awaiting your verdict rather than closed. I am reading \"sequence after\" as ordering the merges, not forbidding implementation, because the two heads touch disjoint files - that one is .github and internal/ciguard, this one internal/service, cmd/gs and docs - and holding an unrelated security gap idle while a review queue drains does not serve anyone. I will not merge this ahead of the security-gates lane. If you meant the stronger reading, say so and I will stop and hold.\n\nWhat I will do: one shared policy with the four headers defined once, applied outside every reachable response path including the TrustedHostHandler rejection, with no duplicated values. A regression built on the real TrustedHostHandler(server.Handler()) composition rather than on a handler assembled for the test, since the composition is exactly what the defect lives in. Loopback-only mutation behaviour and content types unchanged.\n\nI will start from current main rather than planner's branch, rest the implementing commit on this request, and not reuse or retire planner's artifacts. I will re-poll for a child request before committing.\n\nNoting this request is stale, so this promise and what follows are stale too."}