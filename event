{"kind":"artifact","text":"internal/apphost at exact head 1f62e8209195f0be232be88ee8ca476d9352061f on request/custody-lost-update (cut from main 55926dd2), implementing request #8762. Chosen fix: reload-and-merge-before-every-save. New UpdateConfig holds an exclusive advisory flock across one load-modify-store of config.json (.config.lock beside it; the kernel drops it if a process dies); mutate declares only what this save owns on a fresh copy loaded inside the lock, everything else carries forward unchanged, an update that changes nothing writes no file, and when no configuration file exists yet the caller's memory is only the starting point for that first write, decided under the same lock so concurrent creators serialise onto one file instead of clobbering. SaveConfig stays for creation only (initHosted, AttachConfig and host Init all verify absence first) and its doc comment is corrected per condition 3: atomic replace prevents torn reads and nothing else — it never prevented lost updates, which is exactly how recorded actor custody was lost. Rejected alternatives, per condition 1: patching only VerifiedFrontier narrows this one instance and leaves the shape, so the next field anyone persists brings the defect back; per-actor custody files remove the shared mutable object entirely but cost a format change and a migration to close what one lock closes. New config_lock_unix.go/config_lock_other.go (non-unix refuses updates loudly rather than lose updates silently) and config_update_test.go: the requested regression written from wanted behaviour across separate config openings, plus no-write-on-no-change, failed-mutate-leaves-file-unchanged, and create-from-base cases. Gates at this exact head: gofmt -l empty; go vet ./... clean; go test ./... ok; go test -race ./... ok; git diff --check clean. Mutation-checked per condition 4: removing UpdateConfig's reload makes its own regressions fail; reverting the caller to the old whole-config save fails the two-openings workspace regression in internal/app. Durable log and .git/gitseq contents untouched per condition 5. Predecessor at this path is claude's #6874 (155abbdb, live); I hold no ratifier role and asked claude in chat to record the succession by retiring it.","body":{"branch":"request/custody-lost-update","commit":"1f62e8209195f0be232be88ee8ca476d9352061f","conditions":"reload-and-merge-before-save chosen; field-patch and per-actor custody files rejected with reasons; SaveConfig doc comment corrected","gates":"gofmt -l . empty; go vet ./... clean; go test ./... ok; go test -race ./... ok; git diff --check clean; mutation checks: removing UpdateConfig's reload fails TestUpdateConfigPreservesConcurrentCustody and TestUpdateConfigNoChangeWritesNothing; reverting rememberVerifiedFrontier to whole-config save fails the workspace regression in internal/app","path":"internal/apphost"}}