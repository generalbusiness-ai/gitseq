{"kind":"request","text":"claude: make the CLI use the resident the repository already advertises. This refiles 73efec60, reassigned from ox-builder to you at hugh's direction after ox-builder superseded its promise (released at my request, recorded in the withdrawal assert this rests on); ox-builder had not started, so no work transfers. The finding: internal/app/resident.go publishes the advertised resident URL under refs/gitseq/resident, and gs CLI commands ignore it unless --server is passed explicitly, so every local act pays the full local fold-and-verify cost the resident exists to remove. Re-derive at current main before building: the resident-writer-boundary decision lanes are in flight, so check what has landed and coordinate rather than collide — absent an explicit flag the CLI should use the repository advertisement, --server - should force local deliberately, and an advertised resident that is unreachable or invalid should refuse rather than fall back silently. If the in-flight resident-writer-boundary work already covers part of this, say so in an assert and scope down rather than duplicating.","body":{"branch":"request/cli-advertised-resident","conditions":"Behavior re-derived at then-current main and stated in the artifact, including what any landed resident-boundary work already covers; absent an explicit flag, gs commands that append durable acts resolve and use the advertised resident; --server - deliberately forces local; unreachable or invalid advertisement refuses with a named recovery instead of silent local fallback; loopback validation and exact-frontier response checks preserved; mutation-sensitive tests for advertised, overridden-local, no-advertisement, malformed, non-loopback, and unreachable cases; exact-head per-path artifacts; independent review by an actor other than planner and claude.","to":"a5d35aa7e4799472a208663d7f024917442bd513929f9eccf450628ccf70095d"}}