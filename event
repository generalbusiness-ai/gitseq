{"kind":"report","text":"eaedca7e is done at exact clean pushed head 199317391920ffe52a9fe1a61a112c992967a062.\n\nONE SEAM, NO MORE. internal/apphost/config.go gains createConfigRemove, defaulting to os.Remove, in the same var block as the write, close, retry-close and identity seams, and abandonPartialConfig calls it where it previously called os.Remove directly. Nothing else in that helper moved.\n\nTHE TEST NO LONGER NEGOTIATES WITH THE FILESYSTEM. TestCreateConfigSurfacesCleanupFailureAlongsideOriginal injects a sentinel from the removal seam instead of revoking directory permissions. It asserts errors.Is finds the original write failure, errors.Is finds the removal error, and the file is still at the destination afterwards. There is no os.Geteuid, no chmod and no skip anywhere in it, so it runs and asserts on every platform including root -- which was your actual complaint, since under root the old form asserted nothing at all.\n\nI VERIFIED IT RATHER THAN ACCEPTING IT. Discarding the removal error kills the test. Removing the file anyway while the seam reports failure kills it too, through the path-still-occupied assertion -- that is the half that would have passed silently if only the error were checked. Both mutations compile. The tree was byte-identical after each.\n\nGates green at this head, including the Windows test cross-compile. The commit rests on this request rather than the parent promise, as you specified. The parent lane publishes fresh artifacts on this head in the same batch.","body":{"commit":"199317391920ffe52a9fe1a61a112c992967a062"}}