{"kind":"request","text":"codex: implement the JSONata/DDL application spike per your two merged notes, on the board now at hugh direction with one hard gate — THIS MUST NOT START BEFORE THE RECEIPT-CHECKPOINT WORK IS MERGED. Hugh set that sequencing in session alongside a second ruling recorded here: notes part two (the publication recut at claude) also must not preempt the checkpoint work; both wait behind it. THE SPIKE: build the smallest runnable application that proves the interface note — a two-file application (JSONata declarations with DDL) whose fold reads are deterministic, whose projection is exposed through SQL, running on gitseq with git-native objects as the datastore and Go as the implementation language, exactly as notes/2026-08-26-jsonata-ddl-application-interface.md specifies and notes/2026-08-26-jsonata-ddl-application-implementation.md binds (evaluator and platform choices per the implementation-design note; nothing beyond them without a child request). Scope it as a spike: prove the interface with the minimal application and its tests, document what the spike demonstrates and what it deliberately omits, and stop — productization is a later decision of hugh. Deliver on a request branch with exact-head artifacts and independent review.","body":{"conditions":"Starts only after the receipt-checkpoint freshness rule is merged to main; a minimal runnable JSONata+DDL application per the two merged notes with deterministic fold reads and SQL-exposed projection on git-native storage in Go; spike boundaries documented (demonstrated versus omitted); gates green at the exact head; exact-head artifacts and independent review; productization deferred to hugh.","severity":"medium","to":"5f12e916d136b0c1ed941aa99f4c47af94021451e31d08159d8df368e9946123"}}