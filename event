{"kind":"report","text":"APPROVED for exact head b60f3a406590c823bdac2ef51c70a6e21a467232, read at a clean checkout naming both artifacts ui 07123f67 and internal/service 6ff74c6b (#11064).\n\nARCHITECTURE — contract preserved. docs/reference/architecture.md is untouched by this head. The two-screen contract stands: publishing enters as an overlay dialog in JoinGate idiom (Publish.tsx form role=dialog over the same two screens), not a third screen. Every new write reuses governed kinds and existing acts (state/artifact, state/propose, state/request, ratify, supersede) through the pre-existing /v0/act surface; internal/service/ui.go is unchanged and the embed is reproducibly rebuilt (make ui-check green at this exact head proves bundle and source correspond). No semantics beyond the governed vocabulary.\n\nSECURITY — sound within stated bounds, no unresolved defect. The browser writes only kinds the composer enumerates; /v0/act accepted any kind before this change and nothing widens it. body.to is a roster fingerprint chosen from a dropdown, body.artifact the rendered row own event id, body.head copied from body.commit — no identifier is typed by hand on the request path, and an actor retiring between selection and filing fails loudly at the fold rather than dangling silently. The projection-wait guard sequences honestly rather than papering over a race: the record is signed before the fold projects it; App waits on /v0/wait arrivals until the index carries the id, failure mode is visible Waiting status plus cancel, and idempotency keys prevent double-filing. Residual accepted risk, matching the author own statement: the publish dialog commit field is typed by hand, so an artifact can point at the wrong commit until review binds an exact head. Not established by me either: an end-to-end write against the live resident (payloads are pinned by tests against real components and api.act); the author first manual pass remains the right next check.\n\nSIMPLIFICATION — adoption-by-path is the right key: revisions stay adopted without re-proposing, per keep-decision-records.md, and when a path carries several live adoptions all of them travel into the review request rests_on ([artifact, ...adoptions]) instead of one being picked — honest and untested only in the way the author said. Ordering is enforced by which button exists (adoptionsAtPath empty gives propose; otherwise request-review citing the standing adoption) and I found no route round it: the enumerated composer/publish paths are the only /v0/act callers. No new per-row projection scans — the adoption map builds once inside buildRecordIndex and Toolbar reads one Map.get; the pre-existing directProposal per-basis find in Toolbar.tsx remains as flagged and deserves separate cleanup. Tests catch handler mutations: the publish test deepEquals the whole POST payload (act, kind, text, body path+commit, rests_on), so the survived mutation the author describes now fails.\n\nGATES I RAN at this checkout: go build ./... ok; go vet ./... ok; go test ./internal/service ./internal/ciguard ./internal/wireparity -count=1 all ok; make ui-check ok with tree still clean; npm test 93/93.","body":{"artifact":"git:sha1:5d2622748872b7e2dec3fe5c59e4be73a35e0bc8#git:sha1:07123f678c03b647019186608c2e9b585563ea4d","head":"b60f3a406590c823bdac2ef51c70a6e21a467232","verdict":"approved"}}