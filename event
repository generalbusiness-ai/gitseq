{"kind":"artifact","text":"spike/forge/compose_test.go at exact 0bb06c06d06d409d0a25ac1b1b12cea67bda110a on request/forge-compose-port-regression (pushed; one commit over main 2cc23f19, rebased so the diff against main is this change alone), the reporting artifact under promise c800b95e on request 90243449 (#21314). Corrects all three findings of changes-requested verdict e56ed060, each with the control that would have caught it. R1, the blocking one: the previous head called yaml.Unmarshal once and therefore read only the first document, so a safe first document followed by a second carrying 0.0.0.0:9000:9000 returned no findings at all - the same silent omission this check exists to remove, arriving one layer further out. It now decodes the whole stream through end of file, matching what Compose does, and reports a document that will not parse rather than stopping quietly. R2: malformed entries whose first field was an allowed host were cleared, so 127.0.0.1:3300:not-a-port and a protocol Docker does not name both passed; ports are now validated as a number or a range of two in both forms, the protocol must be one Docker names, and a long-form field given a list rather than a scalar is reported. R3: the documented unbracketed IPv6 loopback was refused because the entry was split from the left; the host is now whatever precedes the last two colons, which accepts ::1:6000:6000 and the bracketed spelling alike and is shorter than the code it replaces, and the allowed set is still exactly the two loopback literals. Simplification as asked: no Compose resolution is implemented, the existing gopkg.in/yaml.v3 dependency is the only one used, and the shipped loopback configuration, install lock and administrator workflow are untouched. Twenty-three fixtures written independently of the shipped file now sit on the boundaries, including the four codex added as overlay controls and three range and ephemeral-port cases. The attached fixture is asserted to be rejected exactly once, named by its wildcard address. One test reintroduces the parser this replaces and asserts what it did with that same file - safe entry found, nothing else, no error - so the original gap is demonstrated rather than claimed. Gates at this head: gofmt, go vet, go build and git diff --check clean; the whole spike tree passes with all twenty-three subtests reported run, and internal/docset passes. Mutation evidence against the real shipped file, restored from HEAD after each: appending an unsafe second document fails naming 'sneaky publishes 0.0.0.0:9000:9000 on 0.0.0.0'; a malformed container port fails naming 'names port not-a-port, which is not a number or a range'; the attached long-form addition fails naming its wildcard; a wildcard short form fails naming itself; and an unbracketed ::1 host passes, as it should. The committed compose.yaml is untouched. No container was started and no Compose runtime was exercised, matching the request's own stated limit. Succeeds the same-path sibling c63e5e75 at 36131662, which was never merged.","body":{"branch":"request/forge-compose-port-regression","commit":"0bb06c06d06d409d0a25ac1b1b12cea67bda110a","path":"spike/forge/compose_test.go"}}