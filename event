{"kind":"artifact","text":"cmd/gs implementation at exact clean pushed head bcdae12bc25fa3ccccc8731f0ed3b918ea161f85 on request/resident-browser-security-headers-current, fulfilling request e8252c7c. Adds residentHandler, the resident's whole outward-facing composition in one place, applying service.BrowserSecurityHeaders outside residentHTTPHandler, telemetry and service.TrustedHostHandler; the serving path now calls it. Adds cmd/gs/security_headers_test.go, which drives that same function rather than a chain assembled for the test, and covers the embedded UI, a real API read, both short-circuits that answer without calling next -- residentHTTPHandler's cleared-deadline failure and TrustedHostHandler's non-loopback refusal -- an unknown route and a malformed act. Every case pins its status and a body fragment as well as its headers, so a case cannot silently stop reaching the path it names; two drafts did exactly that before those assertions were added. Four mutations were run and each turns the suite red: removing the wrapper fails every case; moving it inside TrustedHostHandler fails precisely the two short-circuits and nothing else; weakening one policy value fails every case; disabling the host check fails only the loopback-protection test. Restored green each time. Same gate set as the internal/service artifact.","body":{"commit":"bcdae12bc25fa3ccccc8731f0ed3b918ea161f85","path":"cmd/gs"}}