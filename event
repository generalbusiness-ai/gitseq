{"kind":"request","text":"Codex: repair the cross-process merge transaction boundary exposed by the failed SKILL.md landing. Deliver the fix to refs/heads/main before beginning the thread-actions recovery. The repair must make one gs merge invocation unable to erase, abort, reset, or misread another invocation's tentative Git state, receipt reservation, commit, or succession. Prefer one simple repository-scoped crash-released operating-system lock around the whole mutating merge command unless measured evidence proves a narrower identity covers every shared Git and workroom mutation; reuse apphost.WithMetaLock rather than inventing another locking mechanism. State plainly that direct non-Gitseq Git mutations remain outside this cooperative boundary.","body":{"conditions":"Start after Security and CI for main a30406b3 are green and the querysandbox delivery worktree/branch are safely cleaned. Branch from then-current refs/heads/main in a new request/merge-transaction-lock worktree. Acquire the exclusion before existing-receipt lookup, validation or planning can observe mutable merge state, and hold it through target remeasurement, approval reservation, tentative merge, commit, receipt-ref publication, durable succession, and every failure cleanup; no defer may release the lock before abort/reservation cleanup completes. Use the existing host advisory-lock primitive, fail closed on unsupported platforms, avoid same-name nested acquisition, and document lock ordering relative to publication/config locks. Add deterministic cross-process tests that overlap two mutating merge invocations against one repository and prove the second cannot enter the transaction until the first has either sealed succession or completely rolled back and then remeasures current HEAD/frontier. Add a mutation-sensitive test whose failure is exposed if the lock call is removed or narrowed incorrectly, plus interruption/crash-release and failure-cleanup coverage. Preserve existing merge authorization, exact-head, hooks/signing, receipt idempotency, change-path succession, linked-worktree, and rollback behavior. Update architecture/reference docs if the host or CLI contract changes. Run formatting, focused repeated and race tests, full tests, vet, build, docs/generated-output/diff/clean gates. Publish exact path artifacts, obtain independent Architecture/Security/Simplification review, ratify approval, obtain structured target-aware authorization if required, merge the exact approved head to refs/heads/main, push, verify both CI workflows, and clean the worktree/branch. A report alone cannot close delivery.","to":"5f12e916d136b0c1ed941aa99f4c47af94021451e31d08159d8df368e9946123"}}