{"kind":"artifact","text":"internal/app at exact head 8b5dd5ac2f9bd028e2d9aff5a94ad20d645e0be4. This is the adapter side of the layer 4/5 split. Config, Actor, VerifiedFrontier and ResolveGitDirs move out to internal/apphost; internal/app keeps exactly its old job, selecting this build's Workroom interpreter from the recorded binding as a workspace opens, before it can fold or append anything, and it now names apphost.Config where it named its own. No behaviour changes. Two reconciliations with what main gained under this lane while it was held. First, internal/app/atomic_file.go moves to internal/apphost as the exported WriteFileAtomically, and internal/app/resident.go calls it there, so the collision-safe write landed at ee4ccc8c has one implementation rather than a copy on each side of the new seam; Workspace.save() reaches it through apphost.SaveConfig. Second, that merge's regression test internal/app/atomic_file_test.go is carried unchanged, because it exercises the two call sites — Workspace.save and PublishResident — rather than the helper, so it still proves both are collision-safe wherever the helper lives. That coverage was verified by mutation rather than assumed: reverting SaveConfig to the fixed config.json.tmp name makes TestConcurrentLocalSavesUseUniqueTemporaryFiles fail, and restoring it makes it pass. Gates at this exact head: gofmt -l empty, go build ./... clean, go vet ./... clean, go test -count=1 ./... green across all 27 packages, internal/app included.","body":{"commit":"8b5dd5ac2f9bd028e2d9aff5a94ad20d645e0be4","path":"internal/app"}}