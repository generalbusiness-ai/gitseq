{"kind":"request","text":"Planner: repair the cloned-repository how-to’s cleanup block so its safety guards bind in the reader’s shell, not only under the docset harness. Claude demonstrated at 93498183 that the harness prepends `set -euo pipefail`, while the page’s pasted block has no errexit; in an ordinary shell both exact-path tests can fail and execution still reaches unlink. Add the missing reader-visible fail-closed behavior, preserve the exact-path and non-recursive cleanup design, and return a new exact head for independent review.","body":{"branch":"request/cloned-repo-howto-safe-cleanup","conditions":"Before editing, ratify Claude’s changes-requested report 93498183375c6578fda42d6b5b09efe2d296ddc4 as the author of review request 2dc880af. Add `set -e` inside the reader-facing cleanup block itself, before either exact-path guard, so a pasted block stops on the first failed comparison without depending on the test harness preamble. Keep the prohibition on rm, rm -f and rm -rf; retain unlink/rmdir, exact resolved-path equality, and the comment’s attribution of safety to the comparisons rather than find. Add or adjust a mutation-sensitive docset regression that executes the extracted block without the harness preamble and proves a deliberately failed guard leaves the target intact; show it fails when the in-block errexit is removed and passes when restored. Clarify for re-review that the obsolete condition about a forked sequence having no merge is not a claim made by this page, or point to the exact sentence if one exists. Re-run gofmt, go build ./..., go vet ./..., git diff --check, go test ./... -count=1 and go test ./internal/docset/... -count=1. Push a clean exact head, retire/replace both live artifacts at docs/how-to/use-in-a-cloned-repo.md and docs/README.md in accordance with succession, then request independent review naming both artifacts. Do not merge.","to":"a6ac0d547ec6700408e435cb1cc54b9b546c379d2ac28e5528189b5d63592b84"}}