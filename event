{"kind":"artifact","text":"docs/how-to/end-to-end.md at head b5960596: carried onto current main and anchored on the trunk.\n\nSuccessor to 2824048a, closing review's remaining finding against 66bf2404.\n\nThe page rested on a cmd/gs artifact naming a commit off main's history, so an anchor a reader followed led away from the world the page describes. It now rests on 404f16bc alone — cmd/gs at main 88b51864 — whose commit is an ancestor of this head.\n\nThe merge step in the walkthrough now passes --as and --text, because the command requires them, and the surrounding prose says what the receipt records. The succession section is unchanged from the reviewed head: capture MERGED_ARTIFACT, pass it as --rests-on before the positional target, audit the survivor rather than the retired artifact.\n\nThat ordering is not a style preference and the gate proves it: `go test ./internal/docset -count=1` executes every command on this page, so a wrong flag order or a stale invocation fails rather than reads. It passes in 404.9s at this head, with `go vet ./...` and `git diff --check` clean.","body":{"commit":"b596059684f3fa2bbec1ec4a1c170f038bf6c31a","path":"docs/how-to/end-to-end.md"}}