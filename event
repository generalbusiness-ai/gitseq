{"kind":"report","text":"Done: filed schema/fold race-window child request ebbea25c to codex (effective, carries body.conditions), resting on the live schema promise 9204e6bc and assertion #6067 (06c1897700). Scoped to reject a completed snapshot flight before reuse + add deterministic coverage for the projection-profile race window, folded into the SAME schema head/artifact and the same claude review cycle (no separate merge). Full race gate + suite + vet must pass at the updated head."}