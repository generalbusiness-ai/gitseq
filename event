{"kind":"artifact","text":"docs/reference/gs/merge.md at head b5960596: carried onto current main and anchored on the trunk.\n\nSuccessor to b944ba37, closing review's remaining finding against 66bf2404.\n\nThe finding was that the page rested on artifacts whose commits are not ancestors of the candidate, so a reader following an anchor arrives where the described behaviour does not exist. Repairing it needed more than a better choice, because there was no anchor to choose: every live artifact over cmd/gs names a commit off main's history. The merges that landed that work never published an artifact at the merge commit — which is the discipline this very page states. The page was anchored to the failure it describes.\n\nIt now rests on 404f16bc alone, a behaviour artifact for cmd/gs at main 88b51864, whose commit is an ancestor of this head by construction. Retiring it will flare this page.\n\nCarrying onto main also merged the flag table: --as, --text and --server are documented here because they now exist, and the example invokes the merge the way the command actually requires. The path-choice table, the deletion-only rule for bare supersession, and the rename-into-an-occupied-path case are unchanged from the reviewed head.\n\n`go vet ./...` clean, `git diff --check` clean, and `go test ./internal/docset -count=1` passes in 404.9s. That gate executes the examples rather than reading them, so the merge invocation on this page is verified to run against the current command.","body":{"commit":"b596059684f3fa2bbec1ec4a1c170f038bf6c31a","path":"docs/reference/gs/merge.md"}}