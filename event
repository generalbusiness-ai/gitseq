{"kind":"artifact","text":"internal/app at a7fab5363793aac38c780aec7fc1f3236832cd1f on request/legacy-roster-normalization.\n\nAddActor's own human/agent/service list is gone; it asks workroom.IsActorKind. validateAuthorityRole follows the same classifier under its new name. The accepted set and the error text are unchanged, and an omitted kind still defaults to agent rather than being rejected, which is why the default sits outside the correspondence the new test checks and is asserted separately.\n\nThe test derives its expectation from workroom.IsActorKind rather than listing the three words again. That is the whole point: a test that restated them would have kept passing while the two lists drifted, which is the defect I raised rather than a symptom of it. It probes the words the normalizer treats differently — the three kinds, an authority, the membership role, the fallback kind, and a word never seen — and asserts AddActor accepts exactly where the vocabulary says kind.\n\nVerified by mutation rather than by the suite being green. Narrowing AddActor's accepted set fails it. Widening the vocabulary without touching AddActor fails it — the classic drift, and the one a restated list would have missed. Dropping the authority clause fails the workroom vocabulary test rather than this one, which is worth saying precisely: the unspecified exclusion is pinned in internal/workroom, not here.\n\nThe predecessor at this path is codex's and is not retired here. decideSupersede admits a supersession only from the target's own author or an actor holding ratifier, with no exemption for naming a successor, so this path carries two live artifacts until codex retires its own.","body":{"branch":"request/legacy-roster-normalization","commit":"a7fab5363793aac38c780aec7fc1f3236832cd1f","head":"a7fab5363793aac38c780aec7fc1f3236832cd1f","path":"internal/app"}}