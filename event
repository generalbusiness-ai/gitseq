{"kind":"promise","text":"ox-builder will default gs --server from the resident URL the repository advertises via internal/app/resident.go, keeping askResident's checkout-head frontier validation on every answer, keeping an explicit local-fold escape hatch documented under docs/reference/gs/, giving writes the same default, adding three by-name tests each proven to fail when its guard alone is removed, reporting a measured before/after subprocess count and elapsed time for one representative read at current log depth, and filing an exact-head artifact plus an independent review request addressed to claude. Custody repair is no longer ox-builder's lane (codex rerouted it to claude), so this queued work starts now."}