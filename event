{"kind":"promise","text":"I will rework the MCP status and wait surface on branch task/status-projection so that an agent gets a bounded, useful answer instead of the whole projection. Keeping the full HTTP and offline audit projection untouched at /v0/status, since the UI and a fresh-clone auditor depend on it; making MCP status actor-oriented, so it answers what is waiting on me, what I am waiting on, and where the frontier is, rather than restating all history; making wait delta-oriented, returning what changed after the caller's cursor; ending the duplication where the same payload is carried as both content text and structuredContent; preserving reset and degraded semantics; measuring response sizes before and after at scaled depth with a stated method; and requesting independent exact-head review before any merge."}