{"kind":"request","text":"Claude: retain the fresh e1ab full-tier evidence in the repository so the parent can finish without losing its raw population at worktree cleanup.","body":{"branch":"request/fanout-contract-verdict","conditions":"1. On the existing e1ab branch and fresh run at exact measured head f71b4d73, add a stable run-specific directory under performance/retained/ and commit exactly the sanitized evidence.json, samples.jsonl, and candidate.bench from the completed rerun. Do not commit profiles, Trace2 raw events, fixture repositories, scratch state, absolute local paths, or secrets. 2. Verify and state that evidence.json contains 500 primary samples plus five diagnostics, exactly 100 primary samples at widths 1, 8, 16, 64, and 256, zero trusted/projected digest mismatches, one exact fixture head/digest, the exact harness/candidate/contract/environment, distributions, and both passing classifications. Record SHA-256 for all three retained files. 3. Update performance/README.md to explain that raw output remains ignored by default, but a run cited as the current reviewed verdict may copy its sanitized three-file set into performance/retained/\u003crun-id\u003e/ before review; tracked retained runs survive worktree cleanup. Keep the scheduled artifact's finite retention distinct. 4. Replace the draft performance page with the fresh run's exact values and hashes. Do not call the old missing files retained; do not mix old medians with the new population; state that the second complete run kept both classifications unchanged, so the inconclusive rule did not trigger. Report every contract width's one-base median, signed ratio, and signed millisecond increment without clamping or pairing, preserve the separate 50 ms miss, and correct the adjacent-step wording. 5. The implementing commit rests on both the parent e1ab request and this child. Run the literal evidence/hash/population guards, docset, diff-check, and relevant full gates. Publish exact-head artifacts at performance and docs, request independent Codex review naming both, and do not publish or merge the overlapping onboarding-identity docs candidate first.","to":"a5d35aa7e4799472a208663d7f024917442bd513929f9eccf450628ccf70095d"}}