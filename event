{"kind":"assert","text":"b5bf2e4e's central premise is false, and I want you to see that before I ask anyone to review a head built on it.\n\nREADME.md IS ALREADY GATED, AND HAS BEEN SINCE 2026-08-09. TestLocalMarkdownLinks in public_surface_test.go walks every .md file in the repository from the module root, skipping only .git, .tmp and node_modules, and fails when a relative link or heading anchor does not resolve. It landed in 35331448, 'Prepare the public technical-preview surface' -- before you filed the request and before I wrote the close report that claimed the gap.\n\nI VERIFIED IT RATHER THAN READING IT. I broke README.md's Getting started link to a nonexistent path and ran the gate: FAIL, 'README.md links to missing docs/getting-startedZZZ.md'. Restored: ok, exit 0. It runs under the ordinary path -- Makefile's test target is go test ./..., and the root package is where this test lives, so it is reached without anything new.\n\nTHE MISTAKE IS MINE, NOT YOURS. My close report said DocsDir is 'docs' and the walker never reaches the root, so nothing checks the README. The first half is true and the conclusion does not follow: I checked the docset walker and stopped, without asking whether anything else in the repository gated that file. You took an accurate observation about one package for a fact about the tree, because I gave it to you that way.\n\nEXTENDING THE DOCSET WALKER TO THE ROOT IS THE WRONG SHAPE ANYWAY, which I only learned by trying it. It would newly pull in AGENTS.md, CLAUDE.md, README.md, SECURITY.md and SKILL.md, and all five go red: the docset parser hard-fails without front matter, which four of them do not have; CLAUDE.md is a single include directive; SKILL.md's front matter carries the Claude skill schema keys, which the parser rejects as unknown; the basis gate would then demand governing acts for each; and the examples gate would execute README's shell blocks, which are make test, make vet, make build and a network clone of the public repository -- the gate recursing into itself, with a network dependency CI deliberately avoids. Those files answer to external contracts, not to docset conventions.\n\nONE THING IN THE REQUEST IS NOT DONE AND I THINK SHOULD NOT BE. You asked for links that resolve AND examples that run. README's only runnable examples are the repository's own full gate and a network clone, so 'examples run' cannot mean what it means for docs pages. I did not add a proxy check on make targets named in prose, because that is the wording-sensitive landmine you told me to avoid.\n\nWHAT THE HEAD AT 655bef02 ACTUALLY CONTAINS, then, is smaller than the request: the docs/why.md three-hunk de-overclaim, the matching docs/README.md hunk, and one genuine link defect I found while checking -- README.md:260 read '* [architecture] in more details,' as a shorthand reference with no definition anywhere in the file, so it rendered as literal bracket text and the inline-link regex could not see it. It is now an ordinary link to docs/reference/architecture.md and therefore under the existing gate. I did not gate the dangling-shorthand class, because an undefined [label] is indistinguishable from deliberate literal brackets.\n\nWHAT I AM ASKING. The head is committed, pushed and green on every gate; I have published no artifacts and requested no review, because the request that authorizes it rests on a premise I now know to be wrong and I would rather you re-scope than have me quietly deliver a third of it. If you want the two de-overclaim hunks and the link fix as they stand, say so and I will publish and request review. If you want something else for the examples half, say what."}