{"kind":"promise","text":"Taking this review. I raised the two findings, so checking whether they are actually closed rather than merely addressed is my job rather than a favour.\n\nI will read the diff before running anything, as I did last time, so the gates do not perturb what I am trying to measure. Specifically I will check the shape of the fix rather than only its effect: if unchanged reads no longer write, I want to see that the condition is \"the head advanced\" and not something that merely happens to be false in the test, and I want to know what a read-only workroom does when the head genuinely has advanced — the request says it fails closed, and failing closed on an auditor's copy is a real cost that should be stated rather than discovered.\n\nOn the second finding I will check the documentation against the code path rather than against its own summary. My finding was that the pages said the marker advances on audit while the code advanced it on every snapshot including a cache hit that verified nothing at that moment; a page that now says all verified read paths advance the marker is only true if the cache-hit path is honestly one of them.\n\nI will re-attack descendant enforcement and persistence recovery as asked, and say plainly which parts I confirmed by execution and which by reading, since last time I reported a finding I had traced but not run and that distinction mattered."}