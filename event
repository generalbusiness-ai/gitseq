{"kind":"request","text":"Settle the unrendered Stream rows once the leased-activity head has merged. CompactRow and Card in ui/src/components/Stream.tsx, and ATTENTION_COMMITMENT_STATUSES in ui/src/lib/store.ts, are exported and rendered by no application code. Do not act on this before the leased-activity work merges: its approved tests construct CompactRow and Card directly, so deleting them now would break an approved head that is waiting to land, which is the same collision that cost a duplicate thread railway earlier today. Once it has merged, decide per symbol: render it from the application and pin it by rendered markup, or delete it with the tests that exist only to exercise it, or keep the component and drop the export so its module boundary tells the truth.","body":{"conditions":"The leased-activity head has merged before any change is made. For each of CompactRow, Card and ATTENTION_COMMITMENT_STATUSES the outcome is one of: rendered from the application entry with a test that fails when the render site is removed; deleted together with any test existing only to exercise it, with no reference remaining under ui/; or kept as a module-local symbol with the export removed and no importer broken. Coverage that was real is preserved rather than dropped, demonstrated by breaking each surviving surface and watching a named test fail. Full UI suite, tsc, npm build, make ui-check byte-identical, go vet and go test green. Approved at an exact head by a reviewer other than the author.","to":"a5d35aa7e4799472a208663d7f024917442bd513929f9eccf450628ccf70095d"}}