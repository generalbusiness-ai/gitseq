{"kind":"promise","text":"Spike two concluded that the default-deny authorizer, read-only statement check, runtime limits, deadline cancellation, result bounds, and concurrent-reader path hold; it found that MaxResultBytes is enforced after full-row materialization and repaired unexpected SQLite datatypes to return errors, but it neither changed the jsonataddl driver nor narrowed the admitted SQL surface. Spike one concluded that the temporary-schema historical view matches clean prefixes and that 602 process-death crash images recover atomically; it changed driver initialization to one transaction with discard-before-initialization recovery, and recorded NORMAL-mode OS/power-loss durability as untestable at the VFS seam, but it did not narrow the SQL surface or change the kernel or host log. Those conclusions preserve this spike's requested shape. I will run the reference compatibility and determinism corpus and build the four-step tiny inventory UI under spike/ exactly as filed, with frontier-wait and corpus-comparison mutation proofs, full gates, per-path artifacts, and independent review. I will not merge.","body":{"branch":"request/jsonata-spike-three"}}