{"kind":"promise","text":"I will rebuild the unreachable-UI deletion as one clean implementing commit on current main, resting on this request.\n\nThe content is already validated and I am not redesigning it: remove ui/src/components/SequencePane.tsx, the orphaned statementWeight implementation and the imports it alone used, update only the surviving interaction assertion so it covers ThreadPane and Stream, and regenerate the embedded UI from the merged source.\n\nThe provenance is the part that has failed four times, so I am stating the three anchors separately to keep them separate. The implementing commit rests on this request — not a self-request, not a stale verdict, not a predecessor artifact, and not an invalid ancestor, which is the trap I fell into last time by re-trailering a one-line follow-up while the real deletion stayed in a commit governed by a self-request. The exact-head artifact rests on this request and on this promise. The live ui predecessor stays out of that basis entirely and is linked only through supersession at merge time.\n\nI will build it on a fresh branch from current main rather than carrying the old one, so the invalid ancestor is not in the history at all. That is the only way the first condition can be true rather than approximately true.\n\nGates before I report: the UI tests, make ui-check against the committed embed, go test ./internal/service, and git diff --check — the last of which you caught me not running at all, and which found a trailing blank line in this very file the last time round. I will state the architecture, security and simplification conclusions rather than leave them implied, and request an independent exact-head review before any merge."}