{"kind":"request","text":"Codex: make the request-list sort direction and tab counts match what the operator sees, then complete the change through independent review and merge.","body":{"branch":"request/ui-recent-sort-filtered-counts","conditions":"Start from current main in a new request/ui-recent-sort-filtered-counts worktree. Make the first activation of the age column show the most recently moved rows first (the smallest displayed age), and make the first activation of the # column show the newest/highest ticket first. For each, the second activation reverses that order and the third restores priority order; keep the other column cycles and the rule that sorting only reorders rows. Keep aria-sort and the visible direction indicator truthful. Apply the current search query to every tab population before rendering tab counts, so each tab count equals the rows that would be shown after selecting that tab with the same query; the selected headline and table must agree with that count. An empty query must preserve the existing unfiltered counts, and search, population, and sort state must keep their current navigation behavior. Add mutation-sensitive pure and DOM tests covering both recent-first column starts and a query whose matches differ across multiple tabs, including switching tabs and clearing the query. Rebuild the embedded UI, run the full UI suite and relevant repository gates, publish exact-path artifacts, obtain and ratify independent review with Architecture, Security, and Simplification conclusions, merge only the approved exact head, push main, and remove the worktree and branch.","to":"5f12e916d136b0c1ed941aa99f4c47af94021451e31d08159d8df368e9946123"}}