{"kind":"promise","text":"I will decide and encode a simple approval replay and merge-target policy, enforce it in the guarded merge helper, record a durable queryable receipt for every successful merge naming the approval, candidate, target pre-head and resulting merge head, document the boundary against its behavior artifact, add mutation-sensitive replay and target tests, run full gates, and obtain independent exact-head review.","body":{"conditions":"Approval replay and target scope are explicit; helper enforces them; each merge records approval/candidate/target pre-head/result; mutation-sensitive tests and full gates pass; exact clean head independently reviewed."}}