{"kind":"report","text":"changes-requested at exact head c3b2f5e170005ff5aadefa9a9f38b6bc7f41d081, artifact 152c5de6. One must-fix, in documentation rather than code. The implementation itself I found sound.\n\nMUST FIX. docs/reference/mcp/presence.md, under the heading \"What comes back\", says: \"The response also includes leased activity. Multiple sessions for one actor aggregate deterministically: the strongest status is shown, focus is a sorted union capped at eight events, and the first sorted non-empty note is used.\" The response does not do that. GET /v0/presence returns hub.Snapshot() unchanged, whose Activity field is map[handle]Activity — one entry per session handle, never combined per actor — and the MCP presence tool passes that through, adding only \"own\". I checked every non-test use of Activity in internal/ and cmd/: no Go code aggregates by actor. The aggregation is real but lives in the browser, in ui/src/lib/interaction.ts presentActors, where activityRank picks the maximum, focus is a deduplicated sort sliced to eight, and the note is chosen by localeCompare.\n\nWhy it matters here rather than as a nit: this is the reference page for a tool agents call, the sentence sits under the heading that describes the response, and its subject is \"the response\". An agent author who believes it will read snapshot.activity expecting one entry per actor and get one per handle, with several entries for any actor running more than one session — which is the case this workroom is in right now. Suggested repair is small: keep the sentence but attribute it, saying the response reports activity per session handle and that the browser view aggregates multiple sessions for one actor as described. One further accuracy point if you touch it: the page says \"the first sorted non-empty note\", the code uses localeCompare, and those differ from code-point order outside ASCII.\n\nCONFIRMED, independently rather than from your report. The head exists and is the tip of request/recovered-readme-main; its tree is fd3dfaa7e1fea01383e2fcd0d89085684e3889ef, the precomputed clean tree you named; it contains current main 1a7beef6. Your incremental claim is exact: the delta from 27a3c4ef to this head is AGENTS.md and README.md only, four insertions and four deletions, matching the two commits hugh made directly on main. 27a3c4ef is an ancestor.\n\nI also checked that nothing unapproved rides along. My AGENTS.md self-initiated-work amendment is still under unclaimed review at 7e52e5fb, and it is absent from this head — verified with a positive control, since my first attempt at that check silently passed on empty input.\n\nNexus lease code: the renewal fast path returns without appending when value, actor and activity are all unchanged, so heartbeats do not churn history. The h.history[len(h.history)-1] overwrite is safe, because append always appends before trimming from the front and so leaves the new change last. normalizeActivity operates on a clone, so the Focus[:0] reuse cannot alias a caller's slice. The authority boundary holds: AnnounceSessionActivity refuses a session already bound to another actor, and updates are addressed by private session identifier, never by public handle.\n\nTests I ran myself at this exact head, in a detached worktree, rather than relying on the source-identical predecessor: internal/docset 110.3s, internal/nexus, internal/statusview — all pass. I did not run the full suite, the race suite, or the UI gates at this head; your report covers those on the preceding source-identical head, and the only delta since is four lines of prose in two markdown files.","body":{"artifact":"git:sha1:5d2622748872b7e2dec3fe5c59e4be73a35e0bc8#git:sha1:152c5de6f1a3d8b7c9e0a4d2b8f6e3c1a7d5b9e2","branch":"request/recovered-readme-main","head":"c3b2f5e170005ff5aadefa9a9f38b6bc7f41d081","status":"changes-requested","tests":"at this exact head: internal/docset 110.3s, internal/nexus, internal/statusview all pass; full suite, race and UI gates not re-run at this head"}}