{"kind":"assert","text":"Bound check on child dab36850 before any code changes; nothing is committed. The two-file bound cannot hold, and the reason is structural, verified at 50cffa6a. Admission can identify a guarded review (body.review_path, admission.go:64) but has no way to identify a merge receipt: successionActs (cmd/gs/succession.go:776-790) builds the receipt as a plain state act with AllowDeadBasis and body stale plus staleness, byte-for-byte the shape an ordinary caller can send, and AllowDeadBasis is reachable by any caller through gs state --allow-dead-basis. Recomputing the stamp for every act also fails: the receipt note is built over approval and artifact (cmd/gs/main.go:927-931) but the artifact is not a basis of the receipt, so a note derived from admitted bases cannot reproduce it, and cmd/gs/main_test.go:2089 pins exact equality between the receipt trailer and the durable body. The smallest correct fix follows the GuardedReview precedent (app.go:581-585, process-local, never decoded from input): one such marker field on app.Act set only by the receipt builder, and admission treating stale and staleness as admission-owned for every act that is neither guarded review nor so marked (delete both, then set from computed bases through reviewguard.StalenessNote, so a caller can neither preserve, forge nor suppress), which removes the defective early return rather than patching it. That needs two named paths beyond the two allowed: internal/app/app.go (the field, and passing it into stateAdmission at app.go:1275-1278; not decoded in batchAct so batch JSON cannot claim it) and cmd/gs/succession.go (one line setting it on the receipt act). It also makes two documented sentences false, which I read as the child escape clause applying: docs/reference/mcp/state.md:67 \"yours is kept\" and docs/reference/gs/state.md:110 \"kept as you wrote it\" must say the fields are not caller-writable. Tests: caller-supplied bogus staleness and stale=false on a stale basis lands with stale=true and the canonical note; caller-supplied stale and staleness on a fresh basis land with neither field; a guarded review keeps its internal note; the receipt keeps its note (in-package once the marker exists). Please confirm this bound, or amend the child, before I proceed; the alternative that stays within two files knowingly regresses merge receipts and I will not ship it."}