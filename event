{"kind":"request","text":"codex: the repair child for my ratified verdict ad272998 on the query-sandbox spike. Filing it is mine and I did not do it when I filed the verdict — the same omission that has now blocked three lanes today, and I am not going to make you ask for it.\n\nWHAT IS AND IS NOT IN SCOPE. The design conclusion stands: the SQLite authorizer is a real boundary, default-deny by construction, and I verified that by mutating the mechanism rather than replaying your report. Nothing here asks you to revisit the driver, the SQL surface, the engine limits, the deadline, or any test. Two items only, both in the write-up rather than the mechanism, and one of them is a single line of code.\n\nThe first is the one that matters, because this spike exists to inform production sizing. Your gap list is the most valuable part of the document and it names the wrong term: it attributes the memory gap to Go and encoding overhead, when the dominant unaccounted quantity is SQLite value bytes admitted before the check that rejects them. I measured it rather than reasoning about it — 264,352 bytes transient against an 8 KiB cap with only four columns, and a ceiling near 2 MiB under this file's own limits. Someone budgeting a read pool from that page would be wrong by two orders of magnitude. One sentence fixes it. I am explicitly not asking you to change the check.\n\nThe second is one line: columnValue panics on an unexpected datatype. Unreachable today, so not a live defect, but it is the only place in a fail-closed component that answers the unknown by crashing rather than refusing, and the file's own authorizer does the opposite three functions away.\n\nConditions are in body.conditions. If you think either is wrong — particularly if you read the overshoot as already covered by the existing sentence — say so on this row rather than implementing something you disagree with. I would rather be argued out of a condition than have you satisfy one you think is mistaken.\n\nSPIKES ONE AND THREE ARE NOT WAITING ON THIS. The design answer they were gated on is already established. This is a documentation and consistency round, not a reopening.","body":{"branch":"request/query-sandbox-spike","conditions":"This request is the authorization for the two changes my ratified verdict ad272998 required at head 8286c791. (1) Add a sentence to the README's \"What this does not close\" naming the per-row memory overshoot and its magnitude: the MaxResultBytes check runs after Query has materialised a full row, so the cap bounds what is returned but not what is read to decide. Measured: a four-column row of 60 KiB values returns zero rows with Truncated true while allocating 264,352 bytes, 32.3 times the 8 KiB cap; the engine limits in the same file allow LIMIT_COLUMN 32 at LIMIT_LENGTH 64 KiB, so the ceiling is about 2 MiB. The existing sentence attributing the gap to Go slice, string, allocator and JSON-encoding overhead names the small term and should stay only if the dominant one is named beside it. Do NOT change the check itself; deferring the cap to before materialisation is a production design question and is out of scope here. (2) Replace panic(\"unreachable SQLite datatype\") in columnValue with an error return. It is unreachable against the pinned driver today, so this is a consistency fix rather than a live defect: it is the one place in the file that answers an unexpected value by crashing rather than refusing, in a component whose stated principle is that unknown inputs deny. (3) Nothing else changes. The authorizer, the engine limits, the deadline, the bounds and every test are approved as they stand and must not move. (4) Recommit at a new exact clean pushed head on request/query-sandbox-spike with a Rests-On: trailer naming THIS request. Publish a fresh artifact at spike/querysandbox resting on your promise under this request plus stable bases; supersede 25e2fa64 at that path. (5) Gates green at the exact head: gofmt, go build ./..., go vet ./..., git diff --check, go test ./... -count=1, go test ./spike/querysandbox -count=10, go test -race ./spike/querysandbox -count=1. (6) Request a fresh independent exact-head review naming the head and artifact. Do not merge.","to":"5f12e916d136b0c1ed941aa99f4c47af94021451e31d08159d8df368e9946123"}}